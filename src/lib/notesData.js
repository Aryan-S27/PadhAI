// src/lib/notesData.js
// Pre-parsed university lecture notes with reconstructed diagrams and formatted tables.

export const notesData = {
  "2014112-DBMS": {
    "Module 1: Introduction to Database Concepts": "# Module 1: Introduction to Database Concepts\n\n## Introduction\n\n### 1. Data\n\nData usually refers to raw or unprocessed data. It is the basic form of data that hasn't been analyzed or processed in any manner.\n**Example:** A student's mid-term marks.\n\n### 2. Information\n\nOnce data is analyzed, it is considered information. Data and information are interrelated and are often mistakenly used interchangeably. Once data is processed, organized, structured, or presented in a given context, it becomes information.\n**Example:** Average mid-term marks of the class.\n\n**Fig. Processing System**\n\n```mermaid\ngraph LR\n    A[Data] --> B[Processing]\n    B --> C[Information]\n```\n\n## Difference between Data and Information\n\n| Feature        | Data                                                               | Information                                                        |\n| :------------- | :----------------------------------------------------------------- | :----------------------------------------------------------------- |\n| Role in System | Used as input for the computer system.                             | The output of data.                                                |\n| Nature         | Unprocessed facts, figures.                                        | Processed data.                                                    |\n| Specificity    | Not specific.                                                      | Specific.                                                          |\n| Meaning        | Doesn't carry a logical meaning on its own.                        | Carries a logical meaning.                                         |\n| Form           | Raw material.                                                      | Product.                                                           |\n\n## Knowledge\n\nKnowledge defines what we understood or got to know from the information.\n**Example:** Based on mid-term marks, we can conclude that one class is performing better than others.\n\n**Fig. Levels of Info. System**\n\n```mermaid\ngraph TD\n    K[Knowledge]\n    I[Information]\n    D[Data]\n\n    D --> I\n    I --> K\n\n    style K fill:#fff,stroke:#333,stroke-width:2px,shape:trapezoid-up\n    style I fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style D fill:#fff,stroke:#333,stroke-width:2px,shape:trapezoid-down\n```\n\n## Database\n\nA database is a collection of related data organized in a way that data can be easily accessed, managed, and updated. In a database, data is strictly organized in row and column format.\n\n**Fig. Student Table Example**\n\n| Roll no. | Name    | Age |\n| :------- | :------ | :-- |\n| 1        | Priyanka | 19  |\n| 2        | Snehal  | 22  |\n| 3        | Vipul   | 28  |\n\n*   The rows are called **Tuple** or **Record**. The data items within one row may belong to different data types.\n*   The columns are often called **Domain** or **Attribute**. All the data items within a single attribute are of the same data type.\n*   The columns of the relations are called **Fields**, **Attributes**, or **Domains**.\n*   The rows are also called **Tuples** or **Records**.\n*   Therefore, a Database is also called a collection of related relations.\n\n## Database Management System (DBMS)\n\nA Database Management System is a set of rules and procedures that help us to create, organize, and manipulate databases. It is basically a large software package that helps us to add, modify, and delete data items in the database.\n\n### Need for DBMS\n\nConsider a scenario where there is no database system in a college. Accessing information about a particular student among hundreds would be a difficult task. An individual would have to manually search through all the records of the students to find the relevant person and access their information on grades, which would be more complicated.\n\nMaintaining a database system overcomes this problem. Users can simply type and get all the information regarding the relevant person quickly without any effort. This saves valuable time.\n\nMost modern enterprises have data that they need to store in ways that will be easy to retrieve later. One common type of database is a list of names and addresses of people who regularly deal with the enterprise. Originally, when a business was small, such lists might have been written on paper, then in a word processor or spreadsheet. As the business and list grew, it became difficult to deal with the information stored in a word processor or spreadsheet, therefore Database technology is needed.\n\nWith the advent of the World Wide Web (WWW), vast amounts of data are being generated and stored by companies like Amazon.com, eBay, Google, YouTube, and Wikipedia, all of which need to be managed. On the other hand, consider the immense amount of data being generated by mobile phones in India. A lot of data must be stored for accounting purposes, while other data, like the content of various SMS messages, must also be stored for retrieval in case of security checks or more serious criminal proceedings. Thus, the amount of data being generated in the modern world using new technologies is mind-boggling, and therefore the need for DBMS has emerged.\n\n### Unique Problems if Not Using a Database\n\n1.  **Size of Data:**\n    Storing a small amount of data in a spreadsheet is fine. However, if it turns into a large amount of data, a spreadsheet solution will not work. If the size of data records goes into millions, storing data into multiple spreadsheets will create problems with speed. It will take a long time to find a record from multiple spreadsheet files.\n\n2.  **Ease of Updating Data:**\n    Multiple people cannot edit the same file at the same time. Other people must wait until files are available to update, which results in a wastage of time.\n\n3.  **Accuracy:**\n    When a user is doing data entry in files, it might be possible to enter incorrect data due to no validation present. For instance, one can enter wrong spelling, wrong dates, or wrong amounts. So, data accuracy is hard to maintain, and accuracy is in question.\n\n4.  **Security:**\n    You cannot secure data in text files and spreadsheets. Anyone can access the file and read any data present in the file. Storing data this way will not work with banking, healthcare applications, or payroll departments where privacy is difficult to maintain.\n\n5.  **Redundancy:**\n    The duplication of data cannot be prevented using text files or spreadsheets. Chances of adding multiple copies of data cannot be limited here. This will lead to accuracy issues. Maintaining and updating multiple copies is not an easy task.\n\n6.  **Incomplete Data:**\n    Some of the data is not considered important and so such data is not entered in the files. As there is no validation in place, this leads to data integrity in question.\n\n## Difference between File System and Database Management System\n\n| S.No. | Difference Factor        | File System                                                                                                                                                                                                                                                                                                  | DBMS                                                                                                                                                                                                                                                                                                                                                                                                         |\n| :---- | :----------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| 1.    | **Definition**           | A file system is an abstraction to store, retrieve, manage, and update a set of files.                                                                                                                                                                                                                       | DBMS is a collection of interrelated data and a set of programs to access these data. Some very well-known DBMS are Microsoft Access, Microsoft SQL Server, SAP, Oracle, dBase.                                                                                                                                                                                                                            |\n| 2.    | **Data Redundancy**      | In a file system, each user defines and implements the needed files for a specific application. <br> _Example:_ In a sales department of an enterprise, one user will be maintaining the details of how many sales personnel there are and their grades. Another user will be maintaining the details of salespersons. Data such as salesperson name and address could be duplicated in both sets of files. | Although the database approach does not remove redundancy completely, it controls the amount of redundancy. <br> _Example:_ In a DB approach, a single repository of data is maintained, i.e., defined once and then accessed by many users. The fundamental characteristic of a DB System is that it not only contains data but also a complete definition or description of the DB structure and constraints. |\n| 3.    | **Sharing of Data**      | File system doesn't allow sharing of data or data sharing is very complex.                                                                                                                                                                                                                                   | In DBMS, data can be shared very easily due to a centralized system.                                                                                                                                                                                                                                                                                                                                             |\n| 4.    | **Data Consistency**     | When data is redundant, it is difficult to update. <br> _Example:_ If we want to change an employee's address, then we have to make changes at all the places where that employee's data is stored. If by mistake, we forget to change or update the address at one or more places, then data inconsistency will occur, i.e., the appearance of the same data will differ from each other. | In DBMS, as there is no or less data redundancy, data remains consistent.                                                                                                                                                                                                                                                                                                                                       |\n| 5.    | **Difficult to Search/Access Data** | In a conventional file system, if we want to search/retrieve/access some data item, it becomes very difficult because searching and querying operations are not already available. For every operation, we have to make different programs.                                                                    | In DBMS, searching, retrieval, and accessing a data item is very easy and user-friendly because searching and querying operations are already available in the system.                                                                                                                                                                                                                                                |\n| 6.    | **Data Isolation**       | In a file system, there is no standard format of data, or we can say data is scattered in various formats or files, which also makes data retrieval difficult.                                                                                                                                                      | In DBMS, due to a centralized system, the format of data remains the same.                                                                                                                                                                                                                                                                                                                                       |\n| 7.    | **Data Integrity**       | In a file system, there is no procedure to check these constraints automatically.                                                                                                                                                                                                                            | The value of data in DB must follow the data or satisfy some integrity rules or consistency enforced by constraints. <br> _Example:_ A company may have a policy that the appropriate age of an employee must be >18. A value that does not satisfy this constraint must not be stored in the respective column.                                                                                                     |\n| 8.    | **Security Problems**    | In a file system, there is very less security, typically general security provided by the file system (e.g., locks, guards, etc.).                                                                                                                                                                            | DBMS has high-level security features like encryption, passwords, biometric (fingerprint matching, face, and voice detection, etc.) security.                                                                                                                                                                                                                                                                  |\n| 9.    | **Atomicity**            | _Example:_ If A wants to transfer 5000 Rs to B's account. In this case, A's account should be debited and B's account should be credited with the same amount. Let's suppose A's account is debited with 5000 Rs, and then the transaction fails. Now the transaction is incomplete because B's account is not credited. These type of problems occur in a file system because there is no procedure to stop such anomalies. | Atomicity means a transaction must be all-or-nothing, i.e., the transaction must either fully happen or not happen at all. It must not complete partially. Atomicity is a special feature of DBMS. In DBMS, either a transaction is completed fully or none of the actions are performed. For this, DBMS maintains a transaction log in which intermediate values are stored.                                    |\n| 10.   | **Concurrent Access Anomalies** | _Example:_ If user Y is editing a data item, and saves changes, then user X saves changes to the same item. The changes made by user Y are lost unless something prevents user X from blindly overwriting the item. A file system does not provide any procedure to stop such anomalies.                                | Any multi-user DB application requires a method for dealing with concurrent access. DBMS, along with an appropriate application, provides safety for concurrent access. For example, if two users access the same data at the same time, locks are available in DBMS. If user X reads a row for editing, and user Y wants to change/update or write a data item, an exclusive lock is issued to one of these transactions. Until the transaction releases that lock, no other transaction can acquire the lock and hence cannot update/write the data item. |\n\n## Database Architecture\n\nDB architecture defines the manner in which the components of DB are organized and integrated. A DBMS can be:\n*   **Centralized:** Data stored at one location.\n*   **Decentralized:** Multiple copies of DB at different locations.\n*   **Hierarchical:** Organized in a tree-like structure.\n\n### (a) 1-Tier Architecture\n\nIn a 1-tier architecture, the database is directly available to the users to perform various operations. Any changes made here are directly done on the DBMS itself. Mainly, DB designers and programmers prefer to use a single-tier architecture.\n\n**Fig. 1-Tier Architecture**\n\n```mermaid\ngraph LR\n    U[User/Client]\n    D[DBMS]\n\n    U <--> D\n```\n\n### (b) 2-Tier Architecture\n\nA 2-tier DBMS architecture includes an Application layer between the user and the DBMS. This layer is responsible for communicating the user's request to the database management system and then sending the response from the DBMS to the user.\n\nAn application interface, known as ODBC (Open Database Connectivity), provides an API that allows client-side programs to call the DBMS. Most DBMS vendors provide ODBC drivers for their DBMS.\n\nSuch an architecture provides extra security for the DBMS as it is not directly exposed to the end user. Also, security can be improved by adding security and authentication checks in the application layer too.\n\n**Fig. 2-Tier Architecture**\n\n```mermaid\ngraph LR\n    U[User/Client]\n    A[Application Layer]\n    D[DBMS Server]\n\n    U <--> A\n    A <--> D\n```\n\n### (c) 3-Tier Architecture\n\nThe 3-tier architecture is the most commonly used architecture for web applications. It is an extension of the 2-tier architecture.\n\nIn the 2-tier architecture, we have an application layer which can be accessed programmatically to perform various operations on the DBMS. The application generally understands the Database Access Language and processes end-users' requests to the DBMS.\n\nIn a 3-tier architecture, an additional Presentation or GUI layer is added, which provides a graphical user interface for the end user to interact with the DBMS. For the end user, the GUI layer is the DB system, and the end user has no idea about the application layer and the DBMS system. PHPMyAdmin is a good example of a 3-tier DBMS.\n\n**Fig. 3-Tier Architecture**\n\n```mermaid\ngraph LR\n    U[User/Client]\n    P[GUI/Presentation Layer]\n    A[Application Layer]\n    D[DBMS]\n\n    U <--> P\n    P <--> A\n    A <--> D\n```\n\n## Three-Level Architecture of DBMS or Three Views of Data\n\nThe database can also be viewed from different levels of abstraction to reveal different levels of details.\n\n**Fig. 3-Level Architecture of DBMS**\n\n```mermaid\ngraph TD\n    subgraph End Users\n        EV1[External View 1]\n        EV2[External View 2]\n        EV3[External View 3]\n    end\n\n    subgraph Schema\n        IC[Internal Conceptual Schema]\n        PS[Physical Storage (Database Items)]\n    end\n\n    DB[(Database)]\n\n    EV1 -- External/Conceptual mapping --> IC\n    EV2 -- External/Conceptual mapping --> IC\n    EV3 -- External/Conceptual mapping --> IC\n\n    IC -- Conceptual/Internal mapping --> PS\n    PS --> DB\n```\n\nThe word **schema** means arrangement – how we want to arrange things that we have to store. **Mapping** is the process of transforming a request and response between various database levels of architecture.\nIn external/conceptual schema, a request goes from external to conceptual schema, and in conceptual/internal mapping, the request goes from conceptual to internal levels.\n\n### External/View Schema\n\nIt is the view that an individual user has. Each individual user has his/her own view of his/her enterprise database. This view may be a restricted view of the database, and the same database is likely to provide a number of different views for different classes of users.\n\n**Example:** A department head in a university may only be interested in the departmental finances and student enrollments in his/her department but not in the buildings and grounds information. The librarian would not be expected to have any interest in the information about departmental finances. The staff payroll office may have no particular interest in student enrollments in computer science unless those numbers have an impact on staff salaries.\n\n### Conceptual/Logical Schema\n\nIt deals with the structure of the entire database. It is the community view of the database, which contains the view of the whole enterprise without any concern for its physical implementation. This view is normally more stable than the other two. In this schema, we define the attributes of each table, the common attributes in different tables that help them to be combined, what kind of data can be input into these attributes, and so on. It represents entities, attributes, and their relationships. The conceptual or logical schema is very useful for database administrators whose responsibility is to maintain the entire database.\n\n### Internal/Physical Schema\n\nIt is the physical representation of the database on the computer. It describes how data is physically stored in the database. It also describes the data type of these data items, the size of the items in the storage media, the location (physical address) of the items in the storage device, and so on. This schema is useful for database application developers and database administrators.\n\n## Data Independence\n\nData Independence is the ability to modify a schema definition in one level without affecting a schema definition in the next higher level. A DBMS may change the structure of data without having to change the application programs (e.g., Accounting systems whose primary purpose is to enter and retrieve information from a computerized database). This is not possible in conventional file processing systems, which have a very low level of data independence, as the file structure and the application programs are tightly coupled together.\n\n### Logical Data Independence\n\nIt is data about the database, i.e., it stores information about how data is managed inside. For example, a table stored in the DB and all its constraints applied to the relation (table).\nLogical data independence is a mechanism that changes itself from actual data stored on the disk. It refers to the capacity of changing the conceptual schema without any changes to the external schema.\n**Example:** Adding a new column to a table without affecting the external views.\n\n### Physical Data Independence\n\nPhysical data independence is the power to change the physical data without impacting the logical schema.\n**Example:** Consider a case where the storage device on which the DB files are stored is changed. A user writes a query to retrieve some data, for example, `SELECT Fname, Lname, age FROM records;`. The above query states the table name which contains the data, not the location of the file where the database is stored. So, even when the internal schema is changed, it will not change the conceptual schema. The result of the query will remain the same whether the DB storage device is changed or not.\n\n## Characteristics of Database\n\n### 1. Self-Describing Nature of DB and Metadata\n\nA DB system not only contains the DB itself but also the descriptions of data structures and its constraints. This is stored in a system catalog or data dictionary. The information stored in the system catalog is called **metadata** (data about data).\n**Example:** A data dictionary in Oracle stores information about:\n*   Users in the DB.\n*   The tables they have created.\n*   The files in which data is stored.\n*   Security permissions.\n\nSome examples of the contents of the data dictionary are:\n*   What data is available?\n*   Where data is stored?\n*   Who owns the data?\n*   How the data is issued?\n*   Who can access the data?\n*   What relationship exists between data items?\n\n### 2. Data Independence (Data Abstraction)\n\nThe process of hiding irrelevant details from the user is called data abstraction.\n**Example:** We are storing customer information in customer tables.\n*   At the **physical level**, these records can be described as blocks of storage (bytes, gigabytes) in memory. The details are often handled by programmers.\n*   At the **logical level**, these records can be described as fields and attributes along with their data types, and their relationships can be logically implemented.\n*   At the **view level**, the user just interacts with the system using a GUI and enters details on the server. They are not aware of how data is stored and what data is stored. Such details are hidden from them.\n\n### 3. Multiple Views of Data and Security\n\nA DB typically has many users. Each of them may require a different perspective or view of the DB. A view may be a subset of the database, or it may contain virtual data that is derived from the database files but is not explicitly stored.\nIn modern DB systems, restrictions are placed on the content that users can access unless the user is an authorized user to access some data.\n\n### 4. Persistent Data\n\nA DB only stores persistent data, and once data has been stored in a DB system, it always stays there unless deleted by an explicit request.\n\n## Users Involved in DB Systems\n\nOn the basis of interaction with the system, users are differentiated into the following types:\n\n### 1. Database Administrator (DBA)\n\nUsually, a person (or a group of persons) centrally located with an overall view of the database is needed to keep the DB running smoothly. Such a person is often called the Database Administrator (DBA). The DBA is responsible for managing software and hardware resources. In a large enterprise, the DBA must be assisted by an advisory committee which includes members from different units of the company.\n\nThe DBA performs the following tasks:\n\n*   **a) Developing Conceptual Schema:** The DBA, in consultation with senior management of the enterprise, is responsible for defining the Conceptual Schema. It is used for defining data structures, entities, and relationships used for the DB. It also describes the semantics (logic) for an organization.\n*   **b) Deciding Which DBMS to Use:** Once the conceptual schema has been decided, a decision regarding which DBMS software to be used is made.\n*   **c) Defining the DB and Loading DB Content:** Once the DBMS has been acquired and installed, the DBA would be responsible for implementing the conceptual schema by using DDL (Data Definition Language).\n*   **d) Assisting and Approving Application and Access:** The DBA would normally provide assistance to end users as well as programmers in writing application programs to assess the DB.\n*   **e) Deciding Data Structure:** Once DB contents have been decided, the DBA would normally take decisions regarding the manner in which data is to be stored. The DBA monitors the performance and makes changes to the data structure for performance optimization.\n*   **f) Backup and Recovery:** The DB is a valuable asset, and therefore the DBA must make all possible efforts to ensure that the database is not damaged or lost. Therefore, regular recovery and backups are carried out to check for failures. If any data entry error or hardware failure occurs, the DBA can bring the DB backward in time to its state before the damage was done.\n*   **g) Integrity:** Integrity means the accuracy and consistency of data. For example, a retailer may have a business rule that every order must identify one and only one customer. These constraints are enforced and checked by the DBA.\n*   **h) Security:** Only authorized users can assess and change the policies of the business. This privilege is provided by the DBA.\n\n### 2. Database Designers\n\nDB designers are responsible for identifying the data to be stored in the DB and choosing the appropriate structures to represent and store data. It is the responsibility of DB designers to communicate with all DB users in order to understand their requirements and to come up with designs that meet these requirements.\n\n### 3. End Users\n\nEnd users are responsible for accessing the DB for querying, updating, and generating reports.\n\n*   **a) Casual End Users:** They occasionally access the DB but they need different information each time.\n*   **b) Naive or Parametric End Users:** Their job is to constantly query and update the DB. Examples include railway reservation clerks and persons in ATM services.\n*   **c) Sophisticated End Users:** Sophisticated end users include engineers, scientists, and business analysts who themselves learn the facilities of DBMS to implement their applications.\n*   **d) Stand-alone Users:** They maintain personal DBs by using ready-made packages. Example: A user using a tax package maintains personal financial data for tax purposes.\n\n### 4. Security Officers\n\nThe hackers who try to break into corporate DBs and steal sensitive information. In today's environment, computer resources and databases need specialized professionals whose sole responsibility is to protect data from unauthorized access.\n\n## Database System Structure\n\nA DB is partitioned into modules that deal with each of the responsibilities of the overall system. The functional components of a DB system can be broadly divided into a Storage Manager and a Query Processor component.\n\n### 1. Storage Manager\n\nThe Storage Manager is important because a DB typically requires a large amount of storage space. Since the main memory of computers cannot store much of this information, the information is stored on disks. Data are moved between disk storage and main memory as needed.\n\nThe Storage Manager is responsible for providing an interface between the low-level data stored in the DB and application programs and queries submitted to the system.\n\nIt includes the following components:\n\n*   **a) Authorization and Integrity Manager:** It tests for the satisfaction of integrity constraints and checks the authority of the users.\n*   **b) Transaction Manager:** It ensures that the DB remains in a consistent state despite system failure.\n*   **c) File Manager:** It manages the allocation of space on disk storage and the data structures used to represent information stored on disk.\n    *   **Data Structures:**\n        *   **① Data-files:** Stores data itself.\n        *   **② Data Dictionary:** Stores metadata about the structures of the DB, in particular, the schema of the DB.\n        *   **③ Indices:** It provides fast access to data items that hold particular values.\n*   **d) Buffer Manager:** It is responsible for fetching data from disk storage to Main Memory and deciding which data to cache in main memory.\n\n### 2. Query Processor\n\nIt includes the following components:\n\n*   **a) DDL Interpreter:** It interprets DDL (Data Definition Language) statements and records the definitions in the data dictionary.\n*   **b) DML Compiler:** It translates DML (Data Manipulation Language) statements in a query language into an evaluation plan consisting of low-level instructions that the query evaluation engine understands. A query can usually be translated into any number of alternative evaluation plans that all give the same result. The DML compiler also performs query optimization, i.e., it picks the lowest evaluation plan from among the alternatives.\n*   **c) Query Evaluation Engine:** It executes low-level instructions generated by the DML compiler.\n\n**Fig. System Structure of DBMS**\n\n```mermaid\ngraph TD\n    subgraph Users\n        NU[Naive Users] --> AI(Application Interface)\n        AP[Application Programmer] --> APP(Application Programs)\n        SU[Sophisticated Users] --> QT(Query Tools)\n        DBA[DBA] --> AT(Administrative Tools)\n    end\n\n    subgraph Query Processor\n        AI --> C1(Compiler & Linker)\n        APP --> C1\n        C1 --> APOC(Application Prog Object Code)\n        APOC --> QE(Query Evaluation Engine)\n\n        QT --> DMLQ(DML Queries)\n        DMLQ --> DMLCO(DML Compiler & Organizer)\n        DMLCO --> QE\n\n        AT --> DDL(DDL Interpreter)\n        DDL --> DD(Data Dictionary)\n    end\n\n    subgraph Storage Manager\n        QE --> BM(Buffer Manager)\n        QE --> FM(File Manager)\n        QE --> AIM(Authority & Integrity Manager)\n        QE --> TM(Transaction Manager)\n\n        BM --> DS\n        FM --> DS\n        AIM --> DS\n        TM --> DS\n        DD --> DS\n    end\n\n    subgraph Disk Storage\n        DS_IND(Indices)\n        DS_DAT(Data)\n        DS_SD(Statistical Data)\n        DS_DD(Data Dictionary)\n    end\n\n    DS(Disk Storage)\n\n    BM --> DS_IND\n    FM --> DS_DAT\n    AIM --> DS_SD\n    TM --> DS_DAT\n    DD --> DS_DD\n\n    style NU fill:#fff,stroke:#333,stroke-width:2px\n    style AP fill:#fff,stroke:#333,stroke-width:2px\n    style SU fill:#fff,stroke:#333,stroke-width:2px\n    style DBA fill:#fff,stroke:#333,stroke-width:2px\n    style AI fill:#fff,stroke:#333,stroke-width:2px,shape:ellipse\n    style APP fill:#fff,stroke:#333,stroke-width:2px,shape:ellipse\n    style QT fill:#fff,stroke:#333,stroke-width:2px,shape:ellipse\n    style AT fill:#fff,stroke:#333,stroke-width:2px,shape:ellipse\n    style C1 fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style APOC fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style DMLQ fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style DMLCO fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style DDL fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style QE fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style BM fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style FM fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style AIM fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style TM fill:#fff,stroke:#333,stroke-width:2px,shape:rectangle\n    style DS_IND fill:#fff,stroke:#333,stroke-width:2px,shape:cylinder\n    style DS_DAT fill:#fff,stroke:#333,stroke-width:2px,shape:cylinder\n    style DS_SD fill:#fff,stroke:#333,stroke-width:2px,shape:cylinder\n    style DS_DD fill:#fff,stroke:#333,stroke-width:2px,shape:cylinder\n    style DS fill:#fff,stroke:#333,stroke-width:2px,shape:cylinder\n```",
    "Module 2: Entity-Relationship Model": "This study guide covers Module 2: Entity-Relationship Model, providing a comprehensive overview of its core concepts, attribute types, relationships, keys, and advanced EER modeling features.\n\n---\n\n## Module 2: Entity-Relationship Data Model\n\n### 1. Fundamental Concepts\n\n#### 1.1. Entity\nAn entity refers to an \"object\" or \"thing\" in the real world. An object can be any person, place, event, etc.\n\n#### 1.2. Entity Type\nA collection of entities having common attributes is termed an entity type.\n\n#### 1.3. Entity Set\nA set of entities of the same entity type. It can be a collection of one or more entities.\n\n#### 1.4. Attribute\nAttributes are the particular properties or characteristics that describe the entities.\n*   **Example:** A `Student` can be described by their name, age, address, height, class, etc. A `Car` can be described by its colour, model, company, etc.\n\n#### 1.5. Value\nValue is the information or data which is stored in attributes of any entity.\n*   **Example:** For a student, values could be 'Priyanka', '27', 'I.T.', '456', etc.\n\n#### 1.6. Domain (Value Set)\nThe domain or value set is the set of all possible values or information for an attribute.\n*   **Example:** For the attribute \"name\", the domain could include 'Priyanka', 'Snehal', 'Vipul'. For \"age\", it could be '27', '25', '23'.\n\n### 2. Illustrative Example: Student Information\n\nConsider the `Student` table shown below:\n\n| Sid | Name     | Age | Branch | Sem | Contact No. |\n| :-- | :------- | :-- | :----- | :-- | :---------- |\n| 1   | Priyanka | 27  | Comp   | VII | 123         |\n| 2   | Snehal   | 25  | I.T.   | IV  | 456         |\n| 3   | Vipul    | 23  | Comp   | VII | 789         |\n\n*   **Enterprise:** Refers to the organization. In this context, the enterprise is a college where students are studied.\n*   **Entity:** Here, an entity refers to any single student with all their values (e.g., the row for Priyanka).\n*   **Entity Set:** A collection of entities. For instance, combining the first and third entities (Priyanka and Vipul) forms an entity set.\n*   **Entity Type:** All students are described by the same set of attributes (Sid, Name, Age, Branch, Sem, Contact No.). Therefore, the entity type is \"Student Information\".\n\n### 3. Types of Attributes\n\nAttributes can be classified based on various characteristics:\n\n#### 3.1. Simple and Composite Attributes\n*   **Simple Attributes:** These are attributes that cannot be divided into subparts.\n    *   **Example:** Colour of a car, branch of a student.\n*   **Composite Attributes:** These are attributes that can be divided into subparts.\n    *   **Example:** The name of a student can be divided into firstname, middlename, lastname. A composite attribute can form a hierarchy:\n\n```mermaid\ngraph TD\n    Address --> Street;\n    Address --> City;\n    Address --> State;\n    Address --> Zip;\n    Street --> Number;\n    Street --> Apartment_No;\n    Street --> Street_Name;\n```\n\n#### 3.2. Single-Valued and Multi-Valued Attributes\n*   **Single-Valued Attributes:** An attribute having only one value for a particular entity.\n    *   **Example:** Age of a student.\n*   **Multi-Valued Attributes:** An attribute having more than one possible value for a particular entity.\n    *   **Example:** Phone number of a student (a student can have more than one phone number).\n\n#### 3.3. Stored vs. Derived Attributes\nIn some cases, two or more attributes are related.\n*   **Example:** The age and birthdate attributes of a person. For a particular entity 'person', the value of age can be determined from the current (today's) date and the value of the person's birthdate. The age attribute is hence called a **derived attribute**, and the birthdate attribute (from which age is derived) is called a **stored attribute**.\n\n#### 3.4. NULL Value Attribute\nNULL stands for nothing. In some cases, a particular entity may not have an applicable value for an attribute.\n*   **Example:** The apartment number attribute of an address applies only to addresses that are in an apartment building and not to other types of residences, such as single-family homes. Similarly, a college degree attribute applies only to persons with college degrees. For such situations, a special value called NULL is created.\n\nNULL can also be used if we do not know the value of an attribute for a particular entity.\n*   **Example:** We do not know the home phone of the person \"John\".\n    *   The meaning of the former type of NULL (not applicable) is different from the latter (unknown).\n    *   The unknown category of NULL can further be classified into two cases:\n        1.  **Value exists but is missing:** When it is known that the attribute value exists, but it is missing (e.g., if a person's height attribute is listed as NULL).\n        2.  **Value may or may not exist:** When it is not known whether the attribute value exists (e.g., if a person's home phone attribute is NULL).\n\n### 4. Relationship Concepts\n\n#### 4.1. Relationship\nA relationship is an association among several entities. It connects different entities through a meaningful interaction.\n*   **Examples:** An employee works in a different department, a student issues a book from a library.\n\n#### 4.2. Degree of Relationship Set\nThe total number of entity sets participating in a relationship set is known as the degree of that relationship set.\n\n#### 4.3. Unary Relationship\nWhen there is only one entity set participating in the relationship, it is called a Unary Relationship.\n*   **Example:** A person is married to another person.\n\n```mermaid\nerDiagram\n    Person ||--o{ \"married to\" }o--|| Person\n```\n\n#### 4.4. Binary Relationship\nWhen there are two entity sets participating in the relationship, it is called a Binary Relationship.\n*   **Example:** A student enrolls in a course.\n\n```mermaid\nerDiagram\n    Student }|--|{ \"enrolled in\" }|--|{ Course\n```\n\n#### 4.5. Ternary Relationship\nA relationship set in which three entity sets are involved is known as a Ternary Relationship Set.\n*   **Example (as drawn in notes, representing multiple binary relationships):**\n    *Note: The provided example diagram illustrates a chain of binary relationships rather than a single ternary relationship connecting three entities through one diamond. A true ternary relationship would involve a single diamond connecting all three entity sets.*\n\n```mermaid\nerDiagram\n    Employee }|--|{ \"works in\" }|--|{ Department\n    Department }|--|{ \"has\" }|--|{ Branch\n    Branch }|--|{ \"has\" }|--|{ Subjects\n```\n\n**Explanation:** An employee works in a department. Each department has many branches. Each branch offers subjects.\n\n#### 4.6. N-ary Relationship\nA relationship set in which more than three entity sets are involved is termed an 'N-ary' relationship set.\n*   **Example (as drawn in notes, representing multiple binary relationships):**\n    *Note: Similar to the ternary example, this illustrates a chain of binary relationships. A true N-ary relationship involves a single diamond connecting N entity sets.*\n\n```mermaid\nerDiagram\n    Teacher }|--|{ \"teacher in\" }|--|{ Class\n    Class }|--|{ \"in\" }|--|{ College\n```\n\n**Explanation:** A teacher teaches in a class, and a class is part of a college.\n\n### 5. Mapping Constraints / Cardinalities\n\nMapping cardinalities specify the number of entities in one entity set that can be associated with entities in another entity set through a relationship set. They are helpful in describing binary relationship sets.\n\n#### 5.1. One to One (1:1)\nIn this, an entity in Set A is associated with at most one entity in Set B, and an entity in Set B is associated with at most one entity in Set A.\n*   **Example:** A country has only one president, and any person may be the president of at most one country.\n\n```mermaid\nerDiagram\n    Country {\n        string name\n    }\n    President {\n        string name\n    }\n    Country ||--o{ \"is president of\" }o--|| President\n```\n\n#### 5.2. One to Many (1:N)\nIn a one-to-many mapping, an entity in Set A is associated with many (zero or more) entities in Set B, and an entity in Set B is associated with at most one entity in Set A.\n*   **Example:** A manager has many employees working under them, but an employee works under only one manager.\n\n```mermaid\nerDiagram\n    Manager {\n        string name\n    }\n    Employee {\n        string name\n    }\n    Manager ||--|{ \"manages\" }o--|| Employee\n```\n\n#### 5.3. Many to One (M:1 / N:1)\nIn a many-to-one mapping, an entity in Set A is associated with at most one entity in Set B, and an entity in Set B is associated with any number of entities in Set A.\n*   **Example:** An employee can work on a single project, while a project can be assigned to more than one employee.\n    *Note: The diagram provided in the notes for M:1 actually depicts a Many-to-Many relationship (M:N) based on the example text.*\n\n```mermaid\nerDiagram\n    Employee {\n        string name\n    }\n    Project {\n        string name\n    }\n    Employee }|--|{ \"works on\" }|--|{ Project\n```\n\n#### 5.4. Many to Many (M:N)\nAn entity in Set A is associated with any number of entities in Set B, and vice versa.\n*   **Example:** A student can study many subjects, and a subject can be studied by many students.\n\n```mermaid\nerDiagram\n    Student {\n        string name\n    }\n    Subject {\n        string title\n    }\n    Student }|--|{ \"studies\" }|--|{ Subject\n```\n\n### 6. Keys in DBMS\n\nA key is an attribute or set of attributes used to identify data in the entity sets. Keys ensure that each record within a table can be uniquely identified by one or more fields.\n\n#### 6.1. Super Key\nA super key is a set of one or more attributes (columns) that can uniquely identify each record (row) within a table. A super key may have redundant attributes. A non-minimal super key is still a super key.\n\n*   **Example Table: Employee**\n\n| EmpID | FName   | LName   | Sal   | DOB      | DOJ      | Passport No. | Dept ID |\n| :---- | :------ | :------ | :---- | :------- | :------- | :----------- | :------ |\n| 1     | Priyanka | Bhilare | 50k   | 13/2/10  | 1/2/12   | 123          | 10      |\n| 2     | Akhila  | Pawar   | 80k   | 20/2/89  | 1/2/12   | 245          | 20      |\n| 3     | Priyanka | Bhilare | 85k   | 13/2/75  | 4/2/12   | 826          | 30      |\n| 4     | Tejaswi | Rane    | 80k   | 16/6/85  | 15/8/14  | 456          | 10      |\n\n*   **Example Table: Department**\n\n| DeptID | Name       |\n| :----- | :--------- |\n| 10     | Sales      |\n| 20     | Marketing  |\n| 30     | Accounts   |\n\nIn the `Employee` table, some super keys are:\n1.  `EmpID`\n2.  `EmpID`, `FName`\n3.  `EmpID`, `LName`\n4.  `Passport No.`\n5.  `DeptID`, `DOJ`\n6.  `Sal`, `DOB`\n7.  `Sal`, `LName`\n8.  `DOJ`, `DOB`\n9.  `DOJ`, `LName`\n...and many more combinations.\n\n#### 6.2. Candidate Key\nWhen an entity has more than one attribute (or set of attributes) that can serve as a primary key, each of such keys is called a Candidate Key.\n*   **Properties of Candidate Keys:**\n    *   Only one of the candidate keys is chosen as the primary key for the entity.\n    *   A candidate key cannot be NULL, and its value should be unique.\n    *   The minimal super key (a super key with no redundant attributes) is known as a candidate key. It must have the minimum number of keys to ensure uniqueness.\n\n*   **Example (for `Employee` table):**\n    1.  `EmpID`\n    2.  `Passport No.`\n\n#### 6.3. Primary Key\nIt is the first and foremost key which is used to uniquely identify a record.\n*   **Properties of Primary Keys:**\n    *   It can be a single attribute or a combination of attributes.\n    *   A primary key must contain unique values and must never be NULL.\n    *   The selection of a primary key for each entity is based on the requirements of the developer.\n*   **Example:** For the `Employee` table, `EmpID` will typically be the primary key.\n\n#### 6.4. Secondary / Alternate Key\nAttributes (or sets of attributes) that are not selected as the primary key from the set of candidate keys are called Secondary Keys or Alternate Keys.\n*   **Example:** For the `Employee` table, if `EmpID` is chosen as the primary key, then `Passport No.` would be an alternate key.\n\n#### 6.5. Foreign Key\nA foreign key is an attribute in any entity set which is also a primary key in another entity set.\n*   **Example:** `DeptID` is an attribute in the `Employee` entity set and also a primary key in the `Department` entity set. Thus, `DeptID` in `Employee` is a foreign key.\n\n### 7. Types of Entity Sets\n\nThere are two main types of entity sets:\n\n#### 7.1. Strong Entity Set\nAn entity set having a primary key attribute is known as a Strong Entity Set.\n*   **Example:** `Student` with `id` as the primary key.\n\n```mermaid\nerDiagram\n    Student {\n        VARCHAR id PK\n        VARCHAR name\n        VARCHAR address\n    }\n```\n\n#### 7.2. Weak Entity Set\nEntity sets having no key attributes are known as Weak Entity Sets. They depend on another entity set (the identifying owner) for their existence and identification.\n*   **Example:** A `Table` entity representing a furniture item might not have a unique identifier on its own but depends on its `Owner` or `Room` for identification. The attributes like `width`, `height`, `shape` are descriptive but not unique identifiers.\n\n```mermaid\nerDiagram\n    Table {\n        VARCHAR width\n        VARCHAR height\n        VARCHAR shape\n    }\n    Owner ||--o{ \"owns\" }|--|| Table\n    Room ||--o{ \"contains\" }|--|| Table\n```\nIn this example, `Table` entity does not contain any primary key and is therefore treated as a weak entity set. Its identification often comes from its relationship with an owning entity or a location entity.\n\n### 8. E-R Model (Entity-Relationship Model)\n\nThe E-R Model is mainly used for communication between database designers and end-users during the analysis phase of database design.\n*   It is a representation of the structure and constraints of a database that is independent of the DBMS and its associated data model to be used for implementation.\n*   It is based on the concept that an organization consists of people, facilities, and objects. These components of an enterprise interact with each other to achieve certain goals.\n*   An E-R diagram describes the logical structure of a database.\n\n### 9. Symbols Used in E-R Diagrams\n\n| Symbol             | Description           |\n| :----------------- | :-------------------- |\n| Rectangle          | Strong Entity Set     |\n| Double Rectangle   | Weak Entity Set       |\n| Ellipse            | Attribute             |\n| Diamond            | Relationship Set      |\n| Double Ellipse     | Multivalued Attribute |\n| Dashed Ellipse     | Derived Attribute     |\n| Underlined Ellipse | Key Attribute         |\n| Ovals in Oval      | Composite Attribute   |\n| Double Line        | Total Participation   |\n| Single Line        | Partial Participation |\n| Triangle with ISA  | Specialization/Generalization |\n\n### 10. E-R Diagram Examples and Problems\n\n#### 10.1. Problem 1: Students Enrolled in Courses\nDraw an E-R diagram showing students enrolled for courses.\n\n```mermaid\nerDiagram\n    Student {\n        string StudentID PK \"Key Attribute (Underlined)\"\n        string FName \"Part of Composite Name\"\n        string MName \"Part of Composite Name\"\n        string LName \"Part of Composite Name\"\n        date DOB\n        int age \"Derived Attribute (Dashed)\"\n    }\n    Course {\n        string CourseID PK \"Key Attribute (Underlined)\"\n        string Type\n    }\n    \"enrolled in\" {\n        date EnrolledDate\n        string EnrollmentNo\n    }\n\n    Student ||--|{ \"enrolled in\" }|--|| Course\n```\n\n#### 10.2. Problem 2: Hospital Management System\nConstruct an E-R diagram for a hospital with sets of patients and medical doctors. Associate with each record of a patient a log of various tests/examinations conducted.\n\n```mermaid\nerDiagram\n    Patient {\n        string Pid PK \"Key Attribute\"\n        string PName_FName \"Composite Name\"\n        string PName_MName \"Composite Name\"\n        string PName_LName \"Composite Name\"\n        string Address\n        date DOB\n    }\n    Test {\n        string TestId PK \"Key Attribute\"\n        string Fees\n        date Date\n    }\n    Doctor {\n        string DocId PK \"Key Attribute\"\n        string DName \"Name\"\n        string Specialist\n        string Qualification\n    }\n    Ward {\n        string Wardno PK \"Key Attribute\"\n        string Name\n        string Capacity\n    }\n\n    Patient ||--o{ \"undergoes\" }|--|{ Test\n    Patient ||--o{ \"treated by\" }o--|| Doctor\n    Doctor ||--o{ \"performs\" }|--|{ Test\n    Patient }|--|{ \"admitted in\" }o--|| Ward\n```\n\n#### 10.3. Problem 3: University EER Diagram\nDraw an EER diagram for a university system, including schools, directors, staff (teaching/non-teaching), courses, subjects, students, and teachers.\n\n```mermaid\nerDiagram\n    School {\n        string ID PK \"Key Attribute\"\n        string Name\n        string Address\n    }\n    Director {\n        string ID PK \"Key Attribute\"\n        string Name\n        string Address\n    }\n    Staff {\n        string ID PK \"Key Attribute\"\n        string Name\n    }\n    Teaching {\n        string PapersChecked\n    }\n    Non_Teaching {\n        string WorkingHours\n    }\n    Course {\n        string ID PK \"Key Attribute\"\n        string Name\n    }\n    Subject {\n        string ID PK \"Key Attribute\"\n        string Name\n        string RegNo\n    }\n    Student {\n        string ID PK \"Key Attribute\"\n        string Name\n        date DOB\n    }\n    Teacher {\n        string ID PK \"Key Attribute\"\n        string Name\n        string ChapterBy\n    }\n\n    School ||--o{ \"headed by\" }o--|| Director\n    School ||--o{ \"has\" }|--|{ Staff\n    Staff }|--|> Teaching : ISA\n    Staff }|--|> Non_Teaching : ISA\n    School ||--o{ \"offers\" }|--|{ Course\n    Course ||--o{ \"has\" }|--|{ Subject\n    Subject }|--|{ \"studied by\" }|--|{ Student\n    Subject }|--o{ \"taught by\" }o--|| Teacher\n```\n\n### 11. EER (Extended ER / Enhanced ER Model)\n\nThe EER model is basically the enhanced version of the E-R model, which includes all the basic concepts of the E-R model with the capability to support additional concepts for complex applications.\n\n**Features of EER Model:**\n1.  Specialization\n2.  Generalization\n3.  Aggregation\n4.  Categorization\n5.  Inheritance\n\n#### 11.1. Generalization\nIt is a process by which two or more entity sets can be combined into a single higher-level entity set by determining similarities between the entities. Generalization proceeds from the recognition that multiple entity sets share some common features.\n*   It is the process of extracting common properties from a set of entities and creating a generalized entity from it.\n*   It is a **bottom-up approach** in which two or more entities can be generalized to a higher-level entity if they have some attributes in common.\n\n*   **Example: Bank System**\n    In a bank, investors and borrowers are two entity sets. They have a common feature: they are customers of the bank. Similarly, employee and customer entity sets can be combined as type `Person`.\n\n```mermaid\nerDiagram\n    Person {\n        string Pid PK \"Person ID\"\n        string Name\n        date DOB\n    }\n    Employee {\n        float Salary\n    }\n    Customer {\n        string CreditRating\n    }\n    Full_Time_Employee {\n        string EmploymentType\n    }\n    Part_Time_Employee {\n        string EmploymentType\n    }\n    Investor {\n        string InvestmentType\n    }\n    Borrower {\n        string LoanType\n    }\n\n    Person ||--|> Employee : \"ISA (Generalization)\"\n    Person ||--|> Customer : \"ISA (Generalization)\"\n    Employee ||--|> Full_Time_Employee : \"ISA (Generalization)\"\n    Employee ||--|> Part_Time_Employee : \"ISA (Generalization)\"\n    Customer ||--|> Investor : \"ISA (Generalization)\"\n    Customer ||--|> Borrower : \"ISA (Generalization)\"\n```\n\n#### 11.2. Specialization\nIn specialization, an entity is divided into sub-entities based on their characteristics.\n*   It is a **top-down approach** where a higher-level entity is specialized into two or more lower-level entities.\n*   **Example: Bank System**\n    Consider the example of a bank. `Person` is an entity set of all people. A person can be classified into `Employee` and `Customer`. So, the `Person` entity set is specialized into `Employee` and `Customer` entity sets. `Employees` are further classified into `Full Time Employees` and `Part Time Employees`. `Customers` are classified into `Investors` and `Borrowers`, and so on.\n\n#### 11.3. Inheritance\nSpecialization and generalization lead to attribute inheritance between higher-level entity sets and lower-level entity sets. Inheritance is a process by which a lower-level entity set inherits some properties of its higher-level entity set.\n*   **Example: Student-Professor-Person**\n    Here, the `Student` entity set and `Professor` entity set inherit the attributes `person-id`, `name`, `address`, `age` from the `Person` entity set.\n\n```mermaid\nerDiagram\n    Person {\n        string ID PK \"Key Attribute\"\n        string Name\n        string Address\n        int Age\n    }\n    Student {\n        string Subject\n    }\n    Professor {\n        string Experience\n    }\n\n    Person ||--|> Student : \"ISA (Inheritance)\"\n    Person ||--|> Professor : \"ISA (Inheritance)\"\n```\n\n#### 11.4. Aggregation\nAggregation is the process when a relationship between two entities is treated as a single entity. This is useful when a relationship set itself participates in another relationship set.\n\n```mermaid\nerDiagram\n    Center {\n        string CenterID\n        string Name\n    }\n    Course {\n        string CourseID\n        string Title\n    }\n    \"offers\" {\n        string OfferID\n    }\n    Visitor {\n        string VisitorID\n        string Name\n    }\n    \"enquires\" {\n        string EnqDate\n    }\n\n    Center ||--|{ \"offers\" }|--|{ Course\n    (\"Center offers Course\") ||--|{ \"enquires\" }|--|{ Visitor\n```\n**Explanation:** In the diagram above, the relationship between `Center` and `Course` (i.e., `offers`) together acts as an entity, which is then in a relationship (`enquires`) with another entity called `Visitor`. In the real world, if a student visits a coaching center, they might inquire about the center only, or just about the course, or about both. This implies the `enquires` relationship connects the `Visitor` to the entire `(Center offers Course)` concept.\n\n#### 11.5. Categorization\nCategorization is a modeling process where a single subclass has a relationship with more than one distinct superclass. The subclass having more than one superclass is known as a category. The process of defining a category is known as categorization.\n\n*   **Symbol for Categorization:** Usually represented by a `U` (union) symbol connecting the superclasses to the category.\n\n*   **Example: Bank System**\n    In a bank, a `Person` can be either an `Employee` or a `Client` (two distinct superclasses). Both `Employee` and `Client` may be `Investors`. Here, the `Investor` subclass acts as a category, being a union of `Client` and `Employee`.\n\n```mermaid\ngraph TD\n    Person --> Client;\n    Person --> Employee;\n    Client --U--> Investor;\n    Employee --U--> Investor;\n```\n\n#### 11.6. Disjoint Constraint\nIt describes the relationship between members of different subclasses. According to the Disjoint constraint, if the subclasses of specialization/generalization are disjoint, then an entity can be a member of only one subclass for that specialization/generalization.\n*   **Disjoint (d):** An entity can belong to at most one subclass.\n*   **Overlapping (o):** An entity can belong to multiple subclasses.\n\n### 12. Participation Constraints\n\nParticipation constraints define the participation of an entity set in a relationship set. There are two types of participation:\n\n#### 12.1. Partial Participation\nIf only some entities from an entity set E are participating in a relationship set R, then it is known as partial participation.\n*   **Notation:** A single line connecting the entity set to the relationship set.\n\n#### 12.2. Total Participation\nIf every entity from an entity set E participates with at least one relation in a relationship set R, then it is known as total participation.\n*   **Notation:** A double line connecting the entity set to the relationship set.\n\n*   **Example: Customer and Loan**\n    Here, `Customer` and `Loan` are two entity sets, and the relationship set is `borrows`.\n    *   Every customer may or may not take a loan. So, the `Customer` entity set is **partially participated**.\n    *   But for every loan, at least one customer is concerned. So, the `Loan` entity set is **totally participated** (a loan cannot exist without being borrowed by someone).\n\n```mermaid\nerDiagram\n    Customer ||--o{ \"borrows\" }|--|| Loan\n```",
    "Module 3: Relational Model & Relational Algebra": "# Module 3: Relational Model and Relational Algebra\n\n## Introduction to Relational Model\n\n### Data Model\n\nA data model is fundamentally a collection of concepts used to describe the structure of a database. It provides the necessary specifications for describing how a database is structured and how it is to be used.\n\nFollowing are some examples of data models:\n\n#### A) Hierarchical Data Model\n\nThe Hierarchical Database (DB) model is one of the oldest database models. This model structures data like a tree, where records form the nodes and fields form the branches of the tree.\n\n**Conceptual Example: Company's Organizational Structure**\n\nConsider a company's organizational structure. At the top, we have a General Manager (GM). Under the GM, there can be several Deputy General Managers (DGM). Each DGM oversees a particular department. Each department, in turn, has one manager and many employees.\n\nThis structure inherently establishes a \"one-to-many\" relationship, typical of a tree structure.\n\n**Illustration of Company's Organization:**\n\n```mermaid\ngraph TD\n    GM --> DGM1\n    GM --> DGM2\n    GM --> DGM3\n    GM --> DGM4\n\n    DGM1 --> Sales\n    Sales --> Emp1_Sales(emp1)\n    Sales --> Emp2_Sales(emp2)\n    Sales --> Emp3_Sales(emp3)\n\n    DGM2 --> Advertise\n    Advertise --> Emp1_Adv(emp1)\n    Advertise --> Emp2_Adv(emp2)\n    Advertise --> Emp3_Adv(emp3)\n    Advertise --> Emp4_Adv(emp4)\n\n    DGM3 --> Production\n    Production --> Emp1_Prod(emp1)\n    Production --> Emp2_Prod(emp2)\n    Production --> Emp3_Prod(emp3)\n\n    DGM4 --> Quality\n    Quality --> Emp1_Qual(emp1)\n    Quality --> Emp2_Qual(emp2)\n    Quality --> Emp3_Qual(emp3)\n\n    DGM5 --> HR\n    HR --> Emp1_HR(emp1)\n\n    style GM fill:#ace,stroke:#333,stroke-width:2px\n    style DGM1 fill:#bce,stroke:#333,stroke-width:2px\n    style DGM2 fill:#bce,stroke:#333,stroke-width:2px\n    style DGM3 fill:#bce,stroke:#333,stroke-width:2px\n    style DGM4 fill:#bce,stroke:#333,stroke-width:2px\n    style DGM5 fill:#bce,stroke:#333,stroke-width:2px\n    style Sales fill:#cfc,stroke:#333,stroke-width:1px\n    style Advertise fill:#cfc,stroke:#333,stroke-width:1px\n    style Production fill:#cfc,stroke:#333,stroke-width:1px\n    style Quality fill:#cfc,stroke:#333,stroke-width:1px\n    style HR fill:#cfc,stroke:#333,stroke-width:1px\n```\n\n#### B) Network Model\n\nThe Network Model is an extension to the Hierarchical Model. In this data model, data is organized like a graph, allowing a node to have more than one parent node. This supports various relationship types, including 1:N (one-to-many), 1:1 (one-to-one), and M:N (many-to-many).\n\nHistorically, this was the most widely used DB model before the Relational Model was introduced.\n\n**Example of Network Model Structure:**\n\n```mermaid\ngraph TD\n    Company --> P1\n    Company --> P2\n    Company --> P3\n    Company --> P4\n\n    P1 --- S1\n    P2 --- S1\n    P2 --- S2\n    P3 --- S2\n    P3 --- S3\n    P4 --- S3\n    P4 --- S4\n\n    style Company fill:#ace,stroke:#333,stroke-width:2px\n    style P1 fill:#bce,stroke:#333,stroke-width:1px\n    style P2 fill:#bce,stroke:#333,stroke-width:1px\n    style P3 fill:#bce,stroke:#333,stroke-width:1px\n    style P4 fill:#bce,stroke:#333,stroke-width:1px\n    style S1 fill:#cfc,stroke:#333,stroke-width:1px\n    style S2 fill:#cfc,stroke:#333,stroke-width:1px\n    style S3 fill:#cfc,stroke:#333,stroke-width:1px\n    style S4 fill:#cfc,stroke:#333,stroke-width:1px\n```\n\n#### C) Entity-Relationship Model\n\nThe Entity-Relationship (ER) Model describes the relationships between different entities. It is primarily a graphical approach used to define the conceptual view of a database.\n\n**Example: College Management System ER Model**\n\nLet's consider a college management system where Student and Teacher are two entities.\n\n```mermaid\nerDiagram\n    STUDENT {\n        varchar sid PK\n        varchar name\n        varchar marks\n    }\n\n    TEACHER {\n        varchar Tid PK\n        varchar name\n        varchar sal\n    }\n\n    STUDENT ||--o{ LEARN_FROM : \"\"\n    TEACHER ||--o{ LEARN_FROM : \"\"\n\n    LEARN_FROM {\n        varchar sid PK,FK\n        varchar Tid PK,FK\n    }\n\n    STUDENT --|> sid\n    TEACHER --|> Tid\n\n    STUDENT }|--|| sid : \"\"\n    TEACHER }|--|| Tid : \"\"\n```\n\n#### D) Relational Model\n\nIn the Relational Model, data is organized in two-dimensional tables. Relationships between data are maintained by storing a common field across related tables.\n\nThe basic structure of data in the Relational Model is a **table**. All information related to a particular type is stored in rows (also known as tuples) of that table. Consequently, tables are also known as **relations** in the relational model.\n\n**Example Tables (from College Management System ER Model):**\n\n**Student Table:**\n\n| sid | name | marks |\n|-----|------|-------|\n|     |      |       |\n|     |      |       |\n\n**Learn From Table:**\n\n| sid | Tid |\n|-----|-----|\n|     |     |\n|     |     |\n\n**Teacher Table:**\n\n| Tid | name | sal |\n|-----|------|-----|\n|     |      |     |\n|     |      |     |\n\n## Relational Schema\n\nA Relational Schema, also referred to as a Database (DB) Schema, is a collection of metadata. It describes the structure and constraints of data representing a particular domain. Essentially, it serves as a blueprint of the database, outlining how data is organized into tables. It defines the logical definition of a table, including its name, column names, and data types associated with each column.\n\nFollowing are the three types of relational schemas:\n\n1.  **Logical Schema:** Describes the database at the logical level, focusing on the data and relationships without specifying physical storage details.\n2.  **Physical Schema:** Defines how data is stored physically on storage devices, including details like file organization, indexing, and data compression.\n3.  **Conceptual Schema:** Represents the overall logical structure of the entire database, integrating different external views into a single, coherent global view.\n\n## Rules for Mapping ER/EER Diagrams into Relational Model\n\nThese rules guide the transformation of an Entity-Relationship (ER) or Extended Entity-Relationship (EER) diagram into a set of relations (tables) in a relational database.\n\n### 1. Entity Mapping\n\n*   **Strong Entity:** For every strong entity in the ER diagram, create a separate table. The attributes of the entity become columns in the table, and the primary key of the entity becomes the primary key of the table.\n*   **Weak Entity:** For every weak entity, create a separate table. The primary key of this table will be a composite key consisting of:\n    *   The primary key of the identifying (strong) entity.\n    *   Any discriminating attribute(s) of the weak entity itself.\n\n### 2. Attribute Mapping\n\n*   **Composite Attribute:** Do not create a separate table for a composite attribute. Instead, include its simple component attributes as individual columns in the table of the entity to which it belongs.\n*   **Multivalued Attribute:** For every multivalued attribute, create a separate table. The primary key of this new table will be a composite key consisting of:\n    *   The primary key of the entity to which the multivalued attribute belongs.\n    *   The multivalued attribute itself.\n*   **Descriptive Attribute:** For a descriptive attribute (an attribute of a relationship), create a separate table. The primary key of this table will include the primary keys of all entities participating in the relationship.\n    *   **Note:** If a descriptive attribute is not present, the decision of whether to create a separate table for a relationship depends on the cardinality existing between the entities involved.\n\n### 3. Relationship Mapping\n\n*   **1:1, 1:N, M:1 Relationships:** For relationships with these cardinalities, the primary key of the \"one\" side (or the entity designated based on participation constraints) is added as a foreign key to the table of the \"many\" side (or the other entity).\n*   **M:N Relationship:** For every M:N relationship, create a separate table. The primary key of this new table will be a composite key formed by combining the primary keys of both participating entities. Any descriptive attributes of the M:N relationship will also be included as columns in this new table.\n\n### 4. Inheritance (for EER Diagrams)\n\nInheritance, typically represented by specialization/generalization in EER diagrams, can be mapped in several ways. One common approach is:\n*   Create a separate table for the superclass (parent entity).\n*   For each subclass (subentity), create a separate table. This subclass table will include:\n    *   The primary key of the parent entity (acting as both its primary key and foreign key referencing the superclass).\n    *   Any specific attributes of the subentity.\n\n**General Rules for Attributes (Refinement from handwritten notes):**\n\n*   **Multivalued Attribute (Revised):** The primary key for the table created for a multivalued attribute will be composed of the primary key of the entity it belongs to, combined with the multivalued attribute itself.\n*   **Descriptive Attribute (Revised):** The primary key for the table created for a descriptive attribute will be composed of the primary keys of all entities involved in the relationship that the descriptive attribute describes, along with the descriptive attribute itself.\n\n---\n\n**Example ER Diagram 1 and Mapping (Referencing handwritten page 9)**\n\nThis ER diagram represents an employee management system with dependents, projects, passports, and loans.\n\n```mermaid\nerDiagram\n    DEPENDENTS {\n        varchar name PK\n        int age\n    }\n    EMPLOYEE {\n        varchar empid PK\n        varchar fname\n        varchar mname\n        varchar lname\n    }\n    PROJECT {\n        varchar pid PK\n        varchar name\n    }\n    PASSPORT {\n        varchar pno PK\n        date issue_date\n        date validity\n    }\n    LOAN {\n        varchar loanno PK\n        varchar type\n    }\n    MALE {\n        varchar empid PK,FK\n        int working_hours\n    }\n    FEMALE {\n        varchar empid PK,FK\n        varchar maternity_leaves\n    }\n\n    EMPLOYEE ||--o{ DEPENDENTS : depends\n    EMPLOYEE ||--|{ PASSPORT : has\n    EMPLOYEE }|--o| PROJECT : works_on\n    EMPLOYEE }|--o| LOAN : takes\n    EMPLOYEE ||--|| MALE : is_a\n    EMPLOYEE ||--|| FEMALE : is_a\n\n    DEPENDENTS ||--|{ EMPLOYEE : \"\"\n    PASSPORT ||--|| EMPLOYEE : \"\"\n    PROJECT }|--o| EMPLOYEE : \"\"\n    LOAN }|--o| EMPLOYEE : \"\"\n    MALE ||--|| EMPLOYEE : \"\"\n    FEMALE ||--|| EMPLOYEE : \"\"\n\n    EMPLOYEE }o--o{ CONTACT : \"emp_contactno\"\n\n    CONTACT {\n        varchar empid PK,FK\n        varchar emp_contactno PK\n    }\n```\n\n**Mapping to Relations:**\n\n1.  **Employee:** (empid (PK), fname, mname, lname)\n2.  **Dependents:** (empid (FK, PK part), name (PK part), age)\n3.  **Emp_Contact:** (empid (FK, PK part), emp_contactno (PK part))\n4.  **Takes:** (empid (FK, PK part), loanno (FK, PK part))\n5.  **Has:** (empid (FK, PK part), pno (FK, PK part), issue_date)\n6.  **Passport:** (pno (PK), validity)\n7.  **Project:** (pid (PK), name)\n8.  **Works_On:** (empid (FK, PK part), pid (FK, PK part))\n9.  **Loan:** (loanno (PK), type)\n10. **Male:** (empid (FK, PK), working_hours)\n11. **Female:** (empid (FK, PK), maternity_leaves)\n\n---\n\n**Example ER Diagram 2 and Mapping (Referencing handwritten pages 8 & 10)**\n\nThis ER diagram represents a company structure with staff, children, and tasks.\n\n```mermaid\nerDiagram\n    COMPANY {\n        varchar cid PK\n        varchar type\n        varchar loc\n        varchar name\n    }\n    STAFF {\n        varchar staffid PK\n        date DOB\n        varchar fname\n        varchar mname\n        varchar lname\n        varchar phoneno\n    }\n    TASK {\n        varchar Tid PK\n        varchar description\n    }\n    CHILD {\n        varchar staffid PK,FK\n        varchar name PK\n        int age\n    }\n    MALE_STAFF {\n        varchar staffid PK,FK\n    }\n    FEMALE_STAFF {\n        varchar staffid PK,FK\n    }\n\n    COMPANY ||--o{ WORK : \"1\"\n    WORK {\n        varchar cid PK,FK\n        varchar staffid PK,FK\n        date since\n    }\n    COMPANY }|--o{ STAFF : \"1\"\n    STAFF ||--o{ CHILD : \"has\"\n    STAFF ||--o{ TASK : \"perform\"\n    STAFF ||--|| MALE_STAFF : \"is_a\"\n    STAFF ||--|| FEMALE_STAFF : \"is_a\"\n\n    TASK ||--|{ STAFF : \"N\"\n    CHILD ||--|| STAFF : \"M\"\n    MALE_STAFF ||--|| STAFF : \"\"\n    FEMALE_STAFF ||--|| STAFF : \"\"\n```\n\n**Mapping to Relations:**\n\n1.  **Company:** (cid (PK), type, loc, name)\n2.  **Work:** (cid (FK, PK part), staffid (FK, PK part), since)\n3.  **Staff:** (staffid (PK), DoB, fname, mname, lname, phoneno)\n4.  **Child:** (staffid (FK, PK part), name (PK part), age)\n5.  **Female_Staff:** (staffid (FK, PK))\n6.  **Staff_Contact:** (staffid (FK, PK part), phoneno (PK part)) - *Note: If phoneno is multivalued, it gets its own table. If it is single-valued and part of STAFF, then this mapping is for a 'contact' entity.* The diagram shows `phoneno` as an attribute of `STAFF`, but also a relationship-like connection `Staff -> phoneno`. The mapping suggests a separate table for contacts, implying `phoneno` as potentially multivalued or a separate entity. Assuming it's a multivalued attribute `phoneno` for `Staff`.\n7.  **Perform:** (staffid (FK, PK part), Tid (FK, PK part))\n8.  **Task:** (Tid (PK), description)\n9.  **Male_Staff:** (staffid (FK, PK)) - *Implicit from the diagram but not explicitly listed in the notes for mapping.*\n\n## Relational Algebra (RA)\n\nRelational Algebra is a procedural query language used to query database tables to access data in different ways.\n\nIn Relational Algebra, both the input (the relation from which data is to be accessed) and the output (a temporary table holding the data asked by the user) are relations.\n\n**Consider the following example:**\n\n**`student_table`**\n\n| id | name | age |\n|----|------|-----|\n| 1  | A    | 17  |\n| 2  | B    | 19  |\n| 3  | C    | 15  |\n| 4  | D    | 13  |\n\nWe use Relational Algebra to fetch data from this table. For example, to select names from the student table where age is less than 17:\n\n```\nσ(age < 17)(student_table)\n```\n\n**Output:**\n\n| id | name | age |\n|----|------|-----|\n| 3  | C    | 15  |\n| 4  | D    | 13  |\n\nNow, if we only want the names, we would project on the `name` column:\n`π(name)(σ(age < 17)(student_table))`\n\n**Output:**\n\n| name |\n|------|\n| C    |\n| D    |\n\nThe output (results) is also in the form of a table, possibly with different columns.\n\n### Primary Operations in Relational Algebra\n\nThe primary operations that can be performed using relational algebra are categorized into unary and binary operations:\n\n**A) Unary Operators (operate on one relation):**\n\n1.  **Select (σ - Sigma):** Filters tuples based on a condition.\n2.  **Project (π - Pi):** Selects a subset of columns.\n3.  **Rename (ρ - Rho):** Renames a relation or its attributes.\n\n**B) Binary Operators (operate on pairs of relations):**\n\n1.  **Join (⋈ - Bow-tie):** Combines related tuples from two relations.\n2.  **Product (X - Cross):** Computes the Cartesian product of two relations.\n3.  **Union (∪ - Cup):** Combines tuples from two relations, removing duplicates.\n4.  **Intersection (∩ - Cap):** Returns tuples common to both relations.\n5.  **Difference (- - Minus):** Returns tuples present in one relation but not in another.\n\n---\n\n### 1. Select Operator (σ)\n\nThe Select operator is used to select a subset of tuples (rows) from a relation that satisfy a specified selection condition. It is denoted by the lowercase Greek letter sigma (σ).\n\n**Example Relations:**\n\n**`emp` table**\n\n| empid | name | job      | sal   | deptno |\n|-------|------|----------|-------|--------|\n| 1     | X    | HR       | 17000 | 30     |\n| 2     | Y    | Analyst  | 20000 | 20     |\n| 3     | Z    | Tester   | 15000 | 20     |\n\n**`Dept` table**\n\n| deptid | Dept name  | Loc       |\n|--------|------------|-----------|\n| 10     | Store      | Mumbai    |\n| 20     | Research   | Pune      |\n| 30     | Testing    | Kolkata   |\n| 40     | Marketing  | Pune      |\n\n**Syntax:**\n\n`σ(condition)(relation_name)`\n\nWe use relational comparison operators (`<`, `>`, `<=`, `>=`, `!=`, `<>`) and logical operators (`AND (∧)`, `OR (∨)`, `NOT (¬)`). Conditions can be combined together using these operators.\n\n**Examples:**\n\ni)  **Find all employees working in department 20.**\n    *   **SQL Query:** `SELECT * FROM emp WHERE deptid = 20;`\n    *   **Equivalent Relational Algebra:** `σ(deptid = 20)(emp)`\n\nii) **Find employees working in department 20 AND whose name is 'Y'.**\n    *   **Equivalent Relational Algebra:** `σ(deptid = 20 ∧ name = 'Y')(emp)`\n\niii) **Find employees having salary greater than 15000.**\n    *   **Equivalent Relational Algebra:** `σ(sal > 15000)(emp)`\n\n---\n\n### 2. Project Operator (π)\n\nThe Project operator is used to select specific attributes (columns) from a relation, effectively creating a vertical subset of the relation. It is denoted by the Greek letter pi (π).\n\n**Note:** The Selection operator (`σ`) selects the required *tuples* (rows), but it does not display them. The Project operator (`π`) is used for *displaying* data by selecting columns.\n\n**Syntax:**\n\n`π(column1, column2, ..., column_n)(relation_name)`\n\n**Examples:**\n\ni)  **Display all employee IDs and their names.**\n    *   **Relational Algebra:** `π(empid, name)(emp)`\n\nii) **Display the ID and name of employees working in department 20 OR 30.**\n    *   **Relational Algebra:** `π(empid, name)(σ(deptid = 20 ∨ deptid = 30)(emp))`\n\niii) **Deduct 2000 from the salary of all employees.**\n    *   **Relational Algebra:** `π(name, sal - 2000)(emp)`\n\n---\n\n### 3. Rename Operator (ρ)\n\nThe Rename operator is used to return an existing relation under a new name. It is denoted by the Greek letter rho (ρ).\n\n**Types of Rename Operations:**\n\na)  `ρA(B)`: Renames relation B to A.\n    *   **Example:** `ρEmployee(emp)` - The `emp` relation is renamed to `Employee`.\n\nb)  `ρ(new_attribute_names)(B)`: Renames only the attributes of relation B, keeping the relation name B.\n    *   **Example:** `ρ(fname/name)(emp)` - The `name` attribute in the `emp` relation is renamed to `fname`. (Here, only a few attributes are renamed, `name` to `fname`).\n\nc)  `ρA(new_attribute_names)(B)`: Renames both the relation B to A AND all of its attributes to `new_attribute_names`. (This notation can be seen as a combination of a) and b)).\n\n---\n\n### 4. Division Operator (÷)\n\nThe Division operator (÷) can be applied under specific conditions between two relations, A and B. It is typically used to find tuples in one relation that are associated with *all* tuples in another.\n\n**Conditions for Applying Division (A ÷ B):**\n\n1.  **Attribute Subset:** The set of attributes of relation B must be a proper subset of the set of attributes of relation A.\n2.  **Resulting Attributes:** The attributes of the relation returned by the division operator will be `(All attributes of A - All attributes of B)`.\n3.  **Resulting Tuples:** The relation returned by the division operator will contain those tuples from relation A that are associated with *every* tuple in relation B.\n\n**Example 1: `emp ÷ Product`**\n\nLet's say we want to find employees who have worked on *all* products listed in the `Product` table.\n\n**`emp` table:**\n\n| empid | productname |\n|-------|-------------|\n| 1     | Shirt       |\n| 1     | Jeans       |\n| 2     | Top         |\n| 2     | Tshirt      |\n| 4     | Jeans       |\n| 4     | Tshirt      |\n\n**`Product` table:**\n\n| productname |\n|-------------|\n| Top         |\n| Tshirt      |\n\n**Applying Division:** `emp ÷ Product`\n\n1.  **Attribute Subset Check:** The attribute `productname` from `Product` is a proper subset of `empid, productname` from `emp`. (Condition 1 satisfied).\n2.  **Resulting Attributes:** `{empid, productname} - {productname} = {empid}`. (Condition 2 determines the schema of the result).\n3.  **Resulting Tuples:** We need `empid`s that are associated with BOTH 'Top' and 'Tshirt'.\n    *   `empid` 1 is associated with 'Shirt' and 'Jeans' (not 'Top' and 'Tshirt').\n    *   `empid` 2 is associated with 'Top' and 'Tshirt'.\n    *   `empid` 4 is associated with 'Jeans' and 'Tshirt' (not 'Top').\n\n**Output:**\n\n| empid |\n|-------|\n| 2     |\n\n**Example 2: `student_table ÷ course_table`**\n\nLet's find students who have taken *all* courses listed in the `course_table`.\n\n**`student_table` (Student enrollment data):**\n\n| student_name | course_name |\n|--------------|-------------|\n| Ashish       | DBMS        |\n| Ashish       | OCPM        |\n| Komal        | DBMS        |\n| Komal        | OS          |\n| Trushali     | OCPM        |\n| Trushali     | ML          |\n| Riya         | OS          |\n\n**`course_table` (Courses of interest):**\n\n| course_name |\n|-------------|\n| DBMS        |\n| OS          |\n\n**Output (student_table ÷ course_table):**\n\n| student_name |\n|--------------|\n| Komal        |\n\n(Komal is the only student who has taken both DBMS and OS from the `course_table`).\n\n---\n\n### Set Operations\n\nSet operations in Relational Algebra are performed on relations that are \"union compatible\" (i.e., they have the same number of attributes, and corresponding attributes have the same domains).\n\n#### 1. Union (∪)\n\nThe Union operation combines all tuples from two relations. It includes all tuples that are either in relation 'R' OR in relation 'S', OR in both. Duplicate tuples are automatically eliminated.\n\n**Conditions for Union Compatibility:**\n\na)  Both relations 'R' and 'S' must have the same number of columns (attributes).\nb)  The domain of each attribute, in column order, must be the same in both 'R' and 'S'.\n\n**Syntax:** `R ∪ S` (or `π(attributes)(R) ∪ π(attributes)(S)` if projecting specific columns before union)\n\n**Example Relations:**\n\n**Relation R:**\n\n| name | number |\n|------|--------|\n| A    | 1      |\n| B    | 2      |\n| C    | 3      |\n| F    | 4      |\n| E    | 5      |\n\n**Relation S:**\n\n| name | number |\n|------|--------|\n| A    | 1      |\n| C    | 2      |\n| D    | 3      |\n| E    | 4      |\n\n**Output (R ∪ S):**\n\n| name | number |\n|------|--------|\n| A    | 1      |\n| B    | 2      |\n| C    | 3      |\n| F    | 4      |\n| E    | 5      |\n| C    | 2      |\n| D    | 3      |\n| E    | 4      |\n\n#### 2. Intersection (∩)\n\nThe Intersection operation returns all tuples that are common to both relations 'R' and 'S'.\n\n**Syntax:** `R ∩ S` (or `π(attributes)(R) ∩ π(attributes)(S)`)\n\n**Example (using R and S from above):**\n\n**Output (R ∩ S):**\n\n| name | number |\n|------|--------|\n| A    | 1      |\n\n#### 3. Difference (-)\n\nThe Difference operation returns all tuples that are present in relation 'R' but are *not* present in relation 'S'.\n\n**Syntax:** `R - S` (or `π(attributes)(R) - π(attributes)(S)`)\n\n**Example (using R and S from above):**\n\n**Output (R - S):**\n\n| name | number |\n|------|--------|\n| B    | 2      |\n| F    | 4      |\n| E    | 5      |\n\n---\n\n### Joins in Relational Algebra\n\nA Join operation combines related tuples from different relations if, and only if, a given join condition is satisfied. It is generally denoted by the symbol `⋈`.\n\n#### 1. Theta Join (⋈θ)\n\nThe Theta Join combines tuples from different relations provided they satisfy a specified theta condition (θ). The theta condition can be any valid comparison operator (`=`, `<`, `>`, `<=`, `>=`, `!=`, `<>`).\n\n**Syntax:** `A ⋈θ B` (where θ is the join condition)\n\n**Example Relations:**\n\n**`emp` table:**\n\n| emp_code | salary |\n|----------|--------|\n| 101      | 50000  |\n| 102      | 30000  |\n| 103      | 25000  |\n\n**`salary_info` table:**\n\n| emp_code | ename    | salary |\n|----------|----------|--------|\n| 101      | Priyanka | 50000  |\n| 102      | Snehal   | 30000  |\n| 103      | Naina    | 25000  |\n\n**Example Theta Join:** `emp ⋈(emp.emp_code > salary_info.emp_code) salary_info`\n\nThis condition selects tuples where the `emp_code` from the `emp` table is greater than the `emp_code` from the `salary_info` table.\n\n**Output:**\n\n| emp.emp_code | emp.salary | salary_info.emp_code | ename    | salary_info.salary |\n|--------------|------------|----------------------|----------|--------------------|\n| 102          | 30000      | 101                  | Priyanka | 50000              |\n| 103          | 25000      | 101                  | Priyanka | 50000              |\n| 103          | 25000      | 102                  | Snehal   | 30000              |\n\n#### 2. Equijoin\n\nAn Equijoin is a special case of Theta Join where the join condition uses *only* the equality (`=`) comparison operator.\n\n**Example Equijoin:** `emp ⋈(emp.emp_code = salary_info.emp_code) salary_info`\n\n**Output:**\n\n| emp.emp_code | emp.salary | salary_info.emp_code | ename    | salary_info.salary |\n|--------------|------------|----------------------|----------|--------------------|\n| 101          | 50000      | 101                  | Priyanka | 50000              |\n| 102          | 30000      | 102                  | Snehal   | 30000              |\n| 103          | 25000      | 103                  | Naina    | 25000              |\n\n#### 3. Natural Join (⋈)\n\nThe Natural Join does not explicitly use any comparison operator in its syntax. It automatically joins relations based on common attributes that have the same name and domain. It also implicitly handles the projection, eliminating duplicate common columns from the result. It does not concatenate attributes like a Cartesian product.\n\nA Natural Join can be performed if there is at least one common attribute between the two relations. The attributes must have the same name and domain.\n\n**Example Natural Join:** `emp ⋈ salary_info`\n\nThe common attribute here is `emp_code` and `salary`. Since `salary` means different things in `emp` (actual salary) and `salary_info` (maybe desired salary or another context), it's a bit ambiguous based on the tables provided. Let's assume the natural join is primarily on `emp_code`. If `salary` is also common and same domain, it would be used.\n\n**Output:**\n\n| emp_code | salary | ename    |\n|----------|--------|----------|\n| 101      | 50000  | Priyanka |\n| 102      | 30000  | Snehal   |\n| 103      | 25000  | Naina    |\n\n#### 4. Outer Join\n\nOuter joins are used to retrieve all matching rows from two or more relations, plus selected unmatched rows from one or both relations. If there's no match for a row from one relation, the attributes from the other relation are filled with NULL values.\n\nThere are three types of Outer Joins:\n\na)  Left Outer Join\nb)  Right Outer Join\nc)  Full Outer Join\n\n**Example Relations for Outer Joins:**\n\n**`emp` table:**\n\n| emp  | street      | city    |\n|------|-------------|---------|\n| Ram  | civil line  | mumbai  |\n| Shyam| Park street | kolkata |\n| Ravi | M.G. Road   | Delhi   |\n| Hari | Nehru Nagar | Pune    |\n| Kuber| NULL        | NULL    |\n\n**`salary_info` table:**\n\n| emp  | branch   | salary |\n|------|----------|--------|\n| Ram  | Infosys  | 10000  |\n| Shyam| Wipro    | 20000  |\n| Kuber| HCL      | 30000  |\n| Hari | TCS      | 50000  |\n\n##### a) Left Outer Join (⋉)\n\nAll tuples from the left relation (R) are included in the resulting relation. If there are tuples in R without any matching tuple in the right relation (S), then the S-attributes of the resulting relation are made NULL.\n\n**Example:** `emp ⋉ salary_info` (joining on `emp` name)\n\n**Output:**\n\n| emp  | street      | city    | branch   | salary |\n|------|-------------|---------|----------|--------|\n| Ram  | civil line  | mumbai  | Infosys  | 10000  |\n| Shyam| Park street | kolkata | Wipro    | 20000  |\n| Ravi | M.G. Road   | Delhi   | NULL     | NULL   |\n| Hari | Nehru Nagar | Pune    | TCS      | 50000  |\n| Kuber| NULL        | NULL    | HCL      | 30000  |\n\n##### b) Right Outer Join (⋊)\n\nAll tuples from the right relation (S) are included in the resulting relation. If there are tuples in S without any matching tuple in the left relation (R), then the R-attributes of the resulting relation are made NULL.\n\n**Example:** `emp ⋊ salary_info` (joining on `emp` name)\n\n**Output:**\n\n| emp  | street      | city    | branch   | salary |\n|------|-------------|---------|----------|--------|\n| Ram  | civil line  | mumbai  | Infosys  | 10000  |\n| Shyam| Park street | kolkata | Wipro    | 20000  |\n| Kuber| NULL        | NULL    | HCL      | 30000  |\n| Hari | Nehru Nagar | Pune    | TCS      | 50000  |\n| Ravi | M.G. Road   | Delhi   | NULL     | NULL   |\n\n##### c) Full Outer Join (⋈)\n\nAll tuples from both participating relations are included in the resulting relation. If there are no matching tuples for either relation, their respective unmatched attributes are made NULL. This is effectively a combination of a Left Outer Join and a Right Outer Join.\n\n**Example:** `emp ⋈ salary_info` (joining on `emp` name)\n\n**Output:**\n\n| emp  | street      | city    | branch   | salary |\n|------|-------------|---------|----------|--------|\n| Ram  | civil line  | mumbai  | Infosys  | 10000  |\n| Shyam| Park street | kolkata | Wipro    | 20000  |\n| Ravi | M.G. Road   | Delhi   | NULL     | NULL   |\n| Hari | Nehru Nagar | Pune    | TCS      | 50000  |\n| Kuber| NULL        | NULL    | HCL      | 30000  |",
    "Module 4: Database Design & Normalization": "# Module 4: Database Design & Normalization\n\n## 3. Structured Query Language (SQL)\n\n### Overview of SQL\n\nSQL is a database computer language designed for the retrieval and management of data in relational databases.\n\nSQL can perform the following operations:\n1.  Retrieve data from a database.\n2.  Insert records into a database.\n3.  Update records in a database.\n4.  Create new databases.\n5.  Set permissions on tables.\n\n### SQL Language Categories\n\nSQL consists of the following language categories:\n1.  **Data Definition Language (DDL)**\n2.  **Data Manipulation Language (DML)**\n3.  **Data Control Language (DCL)**\n4.  **Transaction Control Language (TCL)**\n\n### Data Definition Language (DDL)\n\nDDL is used to define the structure of the database. Before a database can be used, its structure must be created. DDL provides commands for defining, deleting, and modifying the schema of relations (tables).\n\n#### Most Important DDL Statements\n\n1.  **`CREATE`**\n    *   **Purpose:** Helps to create new tables in the database.\n    *   **Syntax:**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size),\n            columnname2 datatype(size),\n            ...\n            columnnameN datatype(size)\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            sid INT(9),\n            sname VARCHAR(10),\n            smarks INT(10),\n            dob DATE\n        );\n        ```\n\n2.  **`DROP`**\n    *   **Purpose:** A table can be dropped or destroyed using the `DROP TABLE` command.\n    *   **Syntax:**\n        ```sql\n        DROP TABLE tablename;\n        ```\n    *   **Example:**\n        ```sql\n        DROP TABLE student;\n        ```\n\n3.  **`ALTER`**\n    *   **Purpose:** It is used to modify an existing table. It manages changes such as adding or dropping columns, changing a column's definition, or adding or dropping table constraints.\n\n    *   **i) To Add a Column:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ADD columnname datatype(size);\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD grade INT(3);\n            ```\n\n    *   **ii) To Add Multiple Columns:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename\n            ADD columnname1 datatype(size),\n            ADD columnname2 datatype(size),\n            ...\n            ADD columnnameN datatype(size);\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD emailid VARCHAR(15), ADD deptname VARCHAR(20);\n            ```\n\n    *   **iii) To Add a Column at the First Position:**\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD branch_name VARCHAR(9) FIRST;\n            ```\n\n    *   **iv) To Add a Column In Between Existing Columns:**\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD contactno INT(10) AFTER sname;\n            ```\n\n    *   **v) To Change Datatype of a Column:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename MODIFY COLUMN columnname datatype(size);\n            ```\n        *   **OR (for multiple columns):**\n            ```sql\n            ALTER TABLE tablename\n            MODIFY COLUMN columnname1 datatype(size),\n            MODIFY COLUMN columnname2 datatype(size);\n            ```\n\n    *   **vi) To Drop a Column:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename DROP COLUMN columnname;\n            ```\n        *   **OR (for multiple columns):**\n            ```sql\n            ALTER TABLE tablename DROP COLUMN columnname1, DROP COLUMN columnname2;\n            ```\n\n4.  **`RENAME`**\n    *   **For Table:**\n        *   **Syntax:**\n            ```sql\n            RENAME TABLE oldtablename TO newname;\n            ```\n        *   **Example:**\n            ```sql\n            RENAME TABLE student TO student_info;\n            ```\n    *   **For Column:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename RENAME COLUMN oldcolumnname TO newcolumnname datatype(size);\n            ```\n            *(Note: In MySQL, `CHANGE COLUMN` is often used for renaming and modifying datatype, while `RENAME COLUMN` is for renaming only.)*\n        *   **Example:**\n            ```sql\n            ALTER TABLE student_info CHANGE COLUMN sname student_name VARCHAR(15);\n            ```\n\n5.  **`DESCRIBE`**\n    *   **Purpose:** Displays the structure of a table, including column names, datatypes, and constraints.\n    *   **Syntax:**\n        ```sql\n        DESC tablename;\n        ```\n    *   **Example:**\n        ```sql\n        DESC student_info;\n        ```\n\n### Data Manipulation Language (DML)\n\nDML commands are used for storing, retrieving, updating, and deleting data. They primarily deal with the manipulation of data present in the database.\n\n1.  **`INSERT` Command**\n    *   **Purpose:** Used to insert new records into a table.\n    *   **Syntax (for all columns):**\n        ```sql\n        INSERT INTO tablename VALUES (value1, value2, value3, ..., valuen);\n        ```\n    *   **Example:**\n        ```sql\n        INSERT INTO student VALUES (1, 'priyanka', 75, '1991-02-06');\n        ```\n    *   **Insert Values for Specified Columns:**\n        *   **Syntax:**\n            ```sql\n            INSERT INTO tablename (columnname1, columnname2) VALUES (v1, v2);\n            ```\n        *   **Example:**\n            ```sql\n            INSERT INTO student (id, name, DoB) VALUES (2, 'Tejas', '1987-13-04');\n            ```\n    *   **To Insert Multiple Values for Specified Columns:**\n        *   **Example:**\n            ```sql\n            INSERT INTO student (id, name, DoB) VALUES\n            (3, 'Snehal', '2004-13-05'),\n            (4, 'vipul', '1990-02-06');\n            ```\n\n2.  **`UPDATE` Command**\n    *   **Purpose:** Used to modify existing data in a table.\n    *   **Syntax:**\n        ```sql\n        UPDATE tablename SET columnname = newvalue WHERE condition;\n        ```\n    *   **Example:**\n        ```sql\n        UPDATE student SET id = 6 WHERE name = 'Priyanka';\n        ```\n    *   **Multiple Updates (conditional):**\n        *   **Example:**\n            ```sql\n            UPDATE student SET id = 7, DOB = '2005-18-12' WHERE name = 'priyanka';\n            ```\n            *(Note: Examples like `SET id=(7,8) WHERE name=('Vipul','Snehal')` are non-standard SQL and would typically require separate UPDATE statements or a `CASE` expression for different rows.)*\n\n3.  **`DELETE` Command**\n    *   **Purpose:** Used to delete rows from a table based on a specified condition.\n    *   **Syntax:**\n        ```sql\n        DELETE FROM tablename WHERE condition;\n        ```\n    *   **Example:**\n        ```sql\n        DELETE FROM student WHERE id = 1;\n        ```\n\n4.  **`SELECT` Command**\n    *   **Purpose:** Used to retrieve data from the database.\n    *   **Syntax:**\n        ```sql\n        SELECT columnname(s) FROM tablename WHERE condition;\n        ```\n    *   **Example (specific columns):**\n        ```sql\n        SELECT sid, name FROM student WHERE sid = 1;\n        ```\n    *   **Example (all columns):**\n        ```sql\n        SELECT * FROM student;\n        ```\n\n### Constraints in DBMS\n\nConstraints are rules placed on columns or tables that limit the data that can be inserted into them.\n\n*   **Integrity Constraints:** These are rules applied to database columns to ensure the validity of data. Every time data is evaluated or checked against the constraint, and only if the result is true, then the data is inserted into the column. Integrity constraints prevent invalid data entry into the table.\n\nIntegrity constraints can be divided into the following categories:\n\n#### A) Domain Integrity Constraints\n\nThese are constraints on the domain value of columns. They are used to impose restrictions on a particular column.\n\n##### Types of Domain Integrity Constraints:\n\n1.  **`NOT NULL` Constraint**\n    *   **Default Behavior:** By default, all columns in a table allow `NULL` values.\n    *   **Effect:** When a `NOT NULL` constraint is enforced on a column or set of columns, it will not allow `NULL` values to be inserted.\n    *   **Syntax (during table creation):**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size),\n            columnname2 datatype(size) NOT NULL,\n            columnname3 datatype(size)\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            sid INT(4),\n            sname VARCHAR(10) NOT NULL,\n            DoB DATE\n        );\n        ```\n    *   **To Add `NOT NULL` after Table Creation:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename MODIFY columnname datatype(size) NOT NULL;\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student MODIFY DoB DATE NOT NULL;\n            ```\n    *   **To Drop `NOT NULL` Constraint:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename MODIFY columnname datatype(size) NULL;\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student MODIFY DoB DATE NULL;\n            ```\n\n2.  **`UNIQUE` Constraint**\n    *   **Behavior:** A `UNIQUE` key constraint does not allow duplicate values in a column, but it *does allow one* `NULL` value.\n    *   **Syntax (during table creation):**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size),\n            columnname2 datatype(size) UNIQUE\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            sid INT(3),\n            sname VARCHAR(9),\n            smarks INT(2) UNIQUE\n        );\n        ```\n    *   **Examples of `UNIQUE` Constraint Behavior:**\n        *   `INSERT INTO student VALUES (1, 'priyanka', 75);` -> Record inserted successfully.\n        *   `INSERT INTO student VALUES (2, 'Aakash', 75);` -> Error: duplicate value for 'smarks'.\n        *   `INSERT INTO student VALUES (3, 'Aakash', NULL);` -> Record inserted successfully. (One `NULL` is allowed).\n    *   **To Add `UNIQUE` after Table Creation:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ADD CONSTRAINT constraintname UNIQUE (columnname);\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD CONSTRAINT u1 UNIQUE (smarks);\n            ```\n    *   **To Drop `UNIQUE` Constraint:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename DROP INDEX constraintname;\n            ```\n            *(Note: In MySQL, `UNIQUE` constraints are typically implemented via unique indexes, so dropping the index effectively drops the constraint.)*\n        *   **Example:**\n            ```sql\n            ALTER TABLE student DROP INDEX u1;\n            ```\n        *   **Note:** If the unique constraint name was not specified during creation, you can find it using `SHOW INDEX FROM tablename;`.\n\n3.  **`DEFAULT` Constraint**\n    *   **Purpose:** Used to provide a default value for a column. This default value is added to all new records if no other value is explicitly specified for that column.\n    *   **Syntax (during table creation):**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size),\n            columnname2 datatype(size) DEFAULT 'value'\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            sid INT(9),\n            sname VARCHAR(10) DEFAULT 'priyanka',\n            DoB DATE\n        );\n        ```\n    *   **Examples of `DEFAULT` Constraint Behavior:**\n        *   `INSERT INTO student VALUES (1, 'Rajesh', '1991-12-07');` -> Record inserted in the table (specified values).\n        *   `INSERT INTO student (sid, dob) VALUES (2, '1987-13-02');` -> Record inserted with default student name as 'priyanka'.\n    *   **To Add `DEFAULT` after Table Creation:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ALTER columnname SET DEFAULT 'value';\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ALTER COLUMN sname SET DEFAULT 'priyanka';\n            ```\n    *   **To Drop `DEFAULT` Constraint:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ALTER COLUMN columnname DROP DEFAULT;\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ALTER COLUMN sname DROP DEFAULT;\n            ```\n\n4.  **`CHECK` Constraint**\n    *   **Purpose:** Defines a condition that each column's value must satisfy.\n    *   **Syntax (during table creation):**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size),\n            columnname2 datatype(size),\n            columnname3 datatype(size),\n            CHECK (Condition)\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            id INT(2),\n            age INT(1),\n            marks INT(9),\n            CHECK (age >= 18)\n        );\n        ```\n    *   **Examples of `CHECK` Constraint Behavior:**\n        *   `INSERT INTO student VALUES (1, 20, 85);` -> Record inserted successfully.\n        *   `INSERT INTO student VALUES (2, 15, 87);` -> Check constraint violated error as age is not `>= 18`.\n    *   **To Add `CHECK` after Table Creation:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ADD CONSTRAINT constraintname CHECK (Condition);\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD CONSTRAINT chk1 CHECK (age >= 18);\n            ```\n    *   **To Drop `CHECK` Constraint:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename DROP CONSTRAINT constraintname;\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student DROP CONSTRAINT chk1;\n            ```\n\n#### B) Entity Integrity Constraints\n\nAs the name implies, entity integrity constraints are applied on each entity (individual row). It specifies that each row should have uniqueness.\n\n##### Types of Entity Integrity Constraints:\n\n1.  **`PRIMARY KEY` Constraint**\n    *   **Purpose:** Used to prevent the duplication of values within rows for a specified column or set of columns. A `PRIMARY KEY` also does not allow `NULL` values.\n    *   **Relationship:** `PRIMARY KEY` = `UNIQUE` + `NOT NULL`.\n    *   **Syntax (during table creation):**\n        ```sql\n        CREATE TABLE tablename (\n            columnname1 datatype(size) PRIMARY KEY,\n            columnname2 datatype(size),\n            columnname3 datatype(size)\n        );\n        ```\n    *   **Example:**\n        ```sql\n        CREATE TABLE student (\n            sid INT(3) PRIMARY KEY,\n            sname VARCHAR(15),\n            smarks INT(1)\n        );\n        ```\n    *   **To Add `PRIMARY KEY` after Table Creation:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename ADD CONSTRAINT constraintname PRIMARY KEY (columnname);\n            ```\n        *   **Example:**\n            ```sql\n            ALTER TABLE student ADD CONSTRAINT pk1 PRIMARY KEY (sid);\n            ```\n    *   **To Drop `PRIMARY KEY` Constraint:**\n        *   **Syntax:**\n            ```sql\n            ALTER TABLE tablename DROP PRIMARY KEY;\n            ```\n            *(A table can only have one primary key, so a name is not usually needed for dropping.)*\n        *   **Example:**\n            ```sql\n            ALTER TABLE student DROP PRIMARY KEY;\n            ```\n    *   **Examples of `PRIMARY KEY` Constraint Behavior:**\n        *   `INSERT INTO student VALUES (1, 'priyanka', 75);` -> Record added successfully.\n        *   `INSERT INTO student VALUES (1, 'Riya', 90);` -> Record not added as duplicate value for sid.\n        *   `INSERT INTO student VALUES (NULL, 'Atul', 80);` -> Error: primary key cannot be NULL.\n\n#### C) Referential Integrity Constraint (`FOREIGN KEY`)\n\nThis constraint is used to establish a parent-child relationship (primary key-foreign key relationship) between two tables. The primary key is defined in the parent table, and the foreign key is defined in the child table.\n\n*   **Example Scenario:**\n    *   **Parent Table: `department`**\n        *   `deptid` (Primary Key)\n        *   `deptname`\n    *   **Child Table: `student`**\n        *   `sid` (Primary Key)\n        *   `name`\n        *   `marks`\n        *   `deptid` (Foreign Key referencing `department.deptid`)\n\n*   **Conceptual Schema (Mermaid ER Diagram):**\n    ```mermaid\n    erDiagram\n        DEPARTMENT ||--o{ STUDENT : \"has_students_in\"\n        DEPARTMENT {\n            int deptid PK \"Department ID\"\n            varchar(20) deptname \"Department Name\"\n        }\n        STUDENT {\n            int sid PK \"Student ID\"\n            varchar(15) name \"Student Name\"\n            int marks \"Student Marks\"\n            int deptid FK \"Foreign Key to Department\"\n        }\n    ```\n\n*   **Syntax (Order of Creation):**\n    1.  **Create Parent Table First:**\n        ```sql\n        CREATE TABLE department (\n            deptid INT(3) PRIMARY KEY,\n            deptname VARCHAR(20)\n        );\n        ```\n    2.  **Create Child Table (with Foreign Key):**\n        ```sql\n        CREATE TABLE student (\n            sid INT(10) PRIMARY KEY,\n            name VARCHAR(15),\n            marks INT(20),\n            deptid INT(10),\n            CONSTRAINT fk1 FOREIGN KEY (deptid) REFERENCES department (deptid)\n        );\n        ```\n*   **Examples of `FOREIGN KEY` Constraint Behavior:**\n    *   *(Assuming `department` table has `deptid` values 10, 20, 30)*\n    *   `INSERT INTO student VALUES (5, 'Tejas', 95, 30);` -> Record added successfully (deptid 30 exists).\n    *   `INSERT INTO student VALUES (6, 'Vedika', 87, 40);` -> Record not added as deptid 40 is not present in the `department` table.\n*   **To Add `FOREIGN KEY` after Table Creation:**\n    *   **Syntax:**\n        ```sql\n        ALTER TABLE student ADD CONSTRAINT fk1 FOREIGN KEY (deptid) REFERENCES department (deptid);\n        ```\n*   **To Drop `FOREIGN KEY` Constraint:**\n    *   **Syntax:**\n        ```sql\n        ALTER TABLE student DROP FOREIGN KEY fk1;\n        ```\n    *   *(Note: You might also need to drop the associated index if it's not automatically removed.)*\n    *   **Example (dropping index if needed):**\n        ```sql\n        ALTER TABLE student DROP INDEX fk1;\n        ```\n\n---\n\n### Transaction Control Language (TCL)\n\nA transaction is a collection of statements between a client and a server. These transactions can be controlled efficiently using TCL commands. TCL commands are used to manage transactions in the database and manage the changes made to data in a table by DML statements.\n\n1.  **`COMMIT` Command**\n    *   **Purpose:** The `COMMIT` command is used to save any changes of an ongoing transaction permanently into the database.\n    *   **Behavior:** When DML commands (like `INSERT`, `UPDATE`, `DELETE`) are used, the changes are not permanent until the current session is closed or a `COMMIT` statement is issued. `COMMIT` explicitly marks the changes as permanent.\n    *   **Syntax:**\n        ```sql\n        COMMIT;\n        ```\n\n2.  **`ROLLBACK` Command**\n    *   **Purpose:** This command restores the database to its last committed state. It can also be used with `SAVEPOINT` to revert to a specific point within an ongoing transaction.\n    *   **Use Case:** If DML changes were made, but it is realized that those changes were not required, `ROLLBACK` can undo them.\n    *   **Syntax (to undo all uncommitted changes):**\n        ```sql\n        ROLLBACK;\n        ```\n    *   **Syntax (to rollback to a specific savepoint):**\n        ```sql\n        ROLLBACK TO savepoint_name;\n        ```\n\n3.  **`SAVEPOINT` Command**\n    *   **Purpose:** The `SAVEPOINT` command is used to save a transaction temporarily, allowing you to `ROLLBACK` to that specific point if required.\n    *   **Syntax:**\n        ```sql\n        SAVEPOINT savepoint_name;\n        ```\n\n#### TCL Commands Example\n\nConsider an `emp` table with the following initial data:\n\n| empid | lastname | fname | sal |\n|-------|----------|-------|-----|\n| 3     | Kale     | Ramesh | 6000 |\n| 7369  | Sharma   | Vijay | 9000 |\n| 7499  | Patil    | Sanjay | 65000 |\n| 7521  | Patel    | Mukesh | 4250 |\n\n**Scenario 1: `UPDATE` and `ROLLBACK`**\n1.  `SELECT * FROM emp;`\n    *(Shows initial table)*\n2.  `BEGIN;`\n3.  `UPDATE emp SET sal = sal + 400 WHERE empid = 3;`\n4.  `SELECT * FROM emp;`\n    *(Shows `Ramesh`'s salary updated to 6400)*\n5.  `ROLLBACK;`\n6.  `SELECT * FROM emp;`\n    *(`Ramesh`'s salary reverts to 6000, as the transaction was rolled back)*\n\n**Scenario 2: `UPDATE` and `COMMIT`**\n1.  `BEGIN;`\n2.  `UPDATE emp SET sal = sal + 400 WHERE empid = 3;`\n3.  `SELECT * FROM emp;`\n    *(Shows `Ramesh`'s salary as 6400)*\n4.  `COMMIT;`\n5.  `SELECT * FROM emp;`\n    *(`Ramesh`'s salary is permanently 6400)*\n6.  `ROLLBACK;`\n7.  `SELECT * FROM emp;`\n    *(`Ramesh`'s salary remains 6400, as changes were committed)*\n\n**Scenario 3: Using `SAVEPOINT`s**\n1.  `START TRANSACTION;`\n2.  `SAVEPOINT sp1;`\n3.  `DELETE FROM emp WHERE empid = 3;`\n4.  `SELECT * FROM emp;`\n    *(`Ramesh` (empid 3) is deleted)*\n\n5.  `SAVEPOINT sp2;`\n6.  `DELETE FROM emp WHERE empid = 7499;`\n7.  `SELECT * FROM emp;`\n    *(`Sanjay` (empid 7499) is deleted)*\n\n8.  `SAVEPOINT sp3;`\n9.  `DELETE FROM emp WHERE empid = 7369;`\n10. `SELECT * FROM emp;`\n    *(`Vijay` (empid 7369) is deleted)*\n\n11. `ROLLBACK TO sp3;`\n12. `SELECT * FROM emp;`\n    *(`Vijay` (empid 7369) is restored, `Ramesh` and `Sanjay` remain deleted)*\n\n13. `ROLLBACK TO sp1;`\n14. `SELECT * FROM emp;`\n    *(`Ramesh` (empid 3) is restored. All operations after `sp1` are undone)*\n\n15. `ROLLBACK TO sp2;`\n    *(Error: `sp2` no longer exists as `ROLLBACK TO sp1` undid `sp2`'s creation.)*\n\n---\n\n### SQL Operators\n\nAn operator is a reserved word or character primarily used in SQL statements to perform operations such as comparisons or arithmetic calculations in a `WHERE` clause.\n\nSQL supports the following types of operators:\n1.  Arithmetic operators\n2.  Comparison operators\n3.  Logical operators\n4.  Set operators\n5.  Range searching operators\n6.  Pattern matching operators\n\n#### 1. Arithmetic Operators\n\nArithmetic operators are used to perform mathematical calculations.\nAssume `X = 20; Y = 10;`\n\n| Operator | Description                                   | Example Result |\n|----------|-----------------------------------------------|----------------|\n| `+`      | Addition: Adds values on either side.         | `X + Y = 30`   |\n| `-`      | Subtraction: Subtracts right-hand from left-hand operand. | `X - Y = 10`   |\n| `*`      | Multiplication: Multiplies values on either side. | `X * Y = 200`  |\n| `/`      | Division: Divides left-hand by right-hand operand. | `X / Y = 2`    |\n| `%`      | Modulo: Returns the remainder of a division. | `X % Y = 0`    |\n\n**Queries using Arithmetic Operators:**\n\n*   **Display details of employees by giving a 5% raise in salary:**\n    ```sql\n    SELECT id, sal AS old_sal, sal + (sal * 0.05) AS new_sal FROM emp;\n    ```\n    **Output:**\n    | empid | old_sal | new_sal |\n    |-------|---------|---------|\n    | 1     | 5000    | 5250    |\n    | 2     | 10000   | 10500   |\n    | 3     | 7000    | 7350    |\n    | 4     | 2000    | 2100    |\n\n*   **Display ID and salary of the employee by dividing salary by 2:**\n    ```sql\n    SELECT id, sal AS old_sal, sal / 2 AS new_sal FROM emp;\n    ```\n    **Output:**\n    | empid | old_sal | new_sal |\n    |-------|---------|---------|\n    | 1     | 5000    | 2500    |\n    | 2     | 10000   | 5000    |\n    | 3     | 7000    | 3500    |\n    | 4     | 2000    | 1000    |\n\n#### 2. Comparison Operators\n\nComparison operators are used to compare two values. They return `TRUE`, `FALSE`, or `UNKNOWN`.\nAssume `X = 20; Y = 10;`\n\n| Operator | Description                                  | Example Result        |\n|----------|----------------------------------------------|-----------------------|\n| `=`      | Equal to: Checks if two operands are equal.  | `(X = Y)` is `FALSE`    |\n| `!=` or `<>` | Not equal to: Checks if two operands are not equal. | `(X != Y)` is `TRUE`    |\n| `>`      | Greater than: Checks if left operand is greater than right. | `(X > Y)` is `TRUE`     |\n| `>=`     | Greater than or equal to: Checks if left operand is greater than or equal to right. | `(X >= Y)` is `TRUE`    |\n| `<`      | Less than: Checks if left operand is less than right. | `(X < Y)` is `FALSE`    |\n| `<=`     | Less than or equal to: Checks if left operand is less than or equal to right. | `(X <= Y)` is `FALSE`   |\n\n**Queries using Comparison Operators:**\n*(Using an `emp` table with `empid`, `fname`, `lname`, `city`, `salary`)*\n\n*   **Find the employee whose salary is 7000:**\n    ```sql\n    SELECT * FROM emp WHERE salary = 7000;\n    ```\n    **Output:**\n    | empid | fname | lname | city | salary |\n    |-------|-------|-------|------|--------|\n    | 3     | Sanjay | Patil | mum  | 7000   |\n\n*   **Display all employees where salary is greater than or equal to 7000:**\n    ```sql\n    SELECT * FROM emp WHERE salary >= 7000;\n    ```\n    **Output:**\n    | empid | fname | lname | city | sal |\n    |-------|-------|-------|------|-----|\n    | 2     | Vijay | Mehta | Pune | 10000 |\n    | 3     | Sanjay | Patil | Mum  | 7000  |\n\n*   **Display records of all employees by adding 2000 to their salary only when salary is less than 5000:**\n    ```sql\n    SELECT empid, fname, city, sal AS old_sal, sal + 2000 AS new_sal\n    FROM emp\n    WHERE sal < 5000;\n    ```\n    **Output:**\n    | empid | fname | city | old_sal | new_sal |\n    |-------|-------|------|---------|---------|\n    | 4     | Jayesh | Pune | 2000    | 4000    |\n\n#### 3. Logical Operators\n\n`NOT`, `AND`, `OR` operators are used to combine two or more conditions in a `WHERE` clause.\n\n*   **`AND` Operator:** Displays a row if *all* conditions listed are true.\n*   **`OR` Operator:** Displays a row if *any* of the conditions listed are true.\n*   **`NOT` Operator:** Returns true if the condition is false.\n\n**Example `emp` table data:**\n| id | fname | lname   | city    | sal   |\n|----|-------|---------|---------|-------|\n| 1  | Ajay  | Sharma  | mum     | 5000  |\n| 2  | Vijay | Mehta   | Pune    | 10000 |\n| 3  | Sanjay| Patil   | mum     | 7000  |\n| 4  | Jayesh| Sharma  | Pune    | 2000  |\n| 5  | Mangesh| Patil   | Kolkata | 15000 |\n| 6  | Ajay  | Patil   | mum     | 7500  |\n\n**Queries using Logical Operators:**\n\n*   **Display information of all employees with first name 'Sanjay' AND last name 'Patil':**\n    ```sql\n    SELECT * FROM emp WHERE fname = 'Sanjay' AND lname = 'Patil';\n    ```\n    **Output:**\n    | id | fname | lname | city | sal |\n    |----|-------|-------|------|-----|\n    | 3  | Sanjay | Patil | mum  | 7000 |\n\n*   **Display information of all employees with first name 'Sanjay' OR last name 'Sharma':**\n    ```sql\n    SELECT * FROM emp WHERE fname = 'Sanjay' OR lname = 'Sharma';\n    ```\n    **Output:**\n    | id | fname | lname | city | sal |\n    |----|-------|-------|------|-----|\n    | 1  | Ajay  | Sharma | mum  | 5000 |\n    | 3  | Sanjay | Patil | mum  | 7000 |\n    | 4  | Jayesh | Sharma | Pune | 2000 |\n\n*   **Display information of all employees with first name 'Jayesh' OR 'Ajay' AND last name 'Sharma':**\n    ```sql\n    SELECT * FROM emp WHERE (fname = 'Jayesh' OR fname = 'Ajay') AND lname = 'Sharma';\n    ```\n    **Output:**\n    | id | fname | lname | city | sal |\n    |----|-------|-------|------|-----|\n    | 1  | Ajay  | Sharma | mum  | 5000 |\n    | 4  | Jayesh | Sharma | Pune | 2000 |\n\n*   **List all employees having salary less than 7000 AND city 'Mumbai':**\n    ```sql\n    SELECT * FROM emp WHERE sal < 7000 AND city = 'mumbai';\n    ```\n    **Output:**\n    | id | fname | lname | city | sal |\n    |----|-------|-------|------|-----|\n    | 1  | Ajay  | Sharma | mum  | 5000 |\n    | 3  | Sanjay | Patil | mum  | 7000 |\n\n#### 4. Range Searching Operators\n\n`IN`, `BETWEEN`, `NOT IN`, `NOT BETWEEN` are range searching operators.\n\n*   **`IN` Operator**\n    *   **Purpose:** Checks if a value is within a set of specified values. It is used when you know the exact values you want to match in a column.\n    *   **Syntax:**\n        ```sql\n        SELECT columnname(s) FROM tablename WHERE columnname IN (value1, value2, ..., valuen);\n        ```\n    *   **Queries using `IN`:**\n        *   **Display details of employees having last name 'Sharma' OR 'Patil':**\n            ```sql\n            SELECT * FROM emp WHERE lname IN ('Sharma', 'Patil');\n            ```\n            **Output:**\n            | id | fname  | lname  | city    | sal    |\n            |----|--------|--------|---------|--------|\n            | 1  | Ajay   | Sharma | mum     | 5000   |\n            | 3  | Sanjay | Patil  | mum     | 7000   |\n            | 4  | Jayesh | Sharma | Pune    | 2000   |\n            | 5  | Mangesh| Patil  | Kolkata | 15000  |\n            | 6  | Ajay   | Patil  | mum     | 7500   |\n            *Note: This query is equivalent to `WHERE lname = 'Sharma' OR lname = 'Patil';`*\n\n        *   **Display details of employees where last name is NOT IN 'Sharma' AND 'Patil':**\n            ```sql\n            SELECT * FROM emp WHERE lname NOT IN ('Sharma', 'Patil');\n            ```\n            **Output:**\n            | id | fname | lname | city | sal |\n            |----|-------|-------|------|-----|\n            | 2  | Vijay | Mehta | Pune | 10000 |\n            *Note: This query is equivalent to `WHERE lname != 'Sharma' AND lname != 'Patil';`*\n\n*   **`BETWEEN` Operator**\n    *   **Purpose:** Selects a range of data between two specified values (minimum and maximum). These values can be numbers, text, or dates. The range is inclusive.\n    *   **Syntax:**\n        ```sql\n        SELECT columnname(s) FROM tablename WHERE columnname BETWEEN value1 AND value2;\n        ```\n    *   **Queries using `BETWEEN`:**\n        *   **Display all employees having salary BETWEEN 2000 AND 7000:**\n            ```sql\n            SELECT * FROM emp WHERE sal BETWEEN 2000 AND 7000;\n            ```\n            **Output:**\n            | id | fname | lname | city | sal |\n            |----|-------|-------|------|-----|\n            | 1  | Ajay  | Sharma | mum  | 5000 |\n            | 3  | Sanjay | Patil | Pune | 7000 |\n            | 4  | Jayesh | Sharma | mum  | 2000 |\n            *Note: This query is equivalent to `WHERE sal >= 2000 AND sal <= 7000;`*\n\n        *   **Display all employees whose salary is NOT BETWEEN 2000 AND 7000:**\n            ```sql\n            SELECT * FROM emp WHERE sal NOT BETWEEN 2000 AND 7000;\n            ```\n            **Output:**\n            | id | fname | lname | city | sal |\n            |----|-------|-------|------|-----|\n            | 2  | Vijay | Mehta | Pune | 10000 |\n            *Note: This query is equivalent to `WHERE sal < 2000 OR sal > 7000;`*\n\n        *   **Select all employees where first name is BETWEEN 'Ajay' AND 'Sanjay':**\n            ```sql\n            SELECT * FROM emp WHERE fname BETWEEN 'Ajay' AND 'Sanjay';\n            ```\n            **Output:**\n            | id | fname | lname | city    | sal   |\n            |----|-------|-------|---------|-------|\n            | 1  | Ajay  | Sharma | mum     | 5000  |\n            | 3  | Sanjay | Patil | mum     | 7000  |\n            | 6  | Ajay  | Patil | mum     | 7500  |\n\n        *   **Select all employees where first name is NOT BETWEEN 'a' AND 'sn':**\n            ```sql\n            SELECT * FROM emp WHERE fname NOT BETWEEN 'a' AND 'sn';\n            ```\n            **Output:**\n            | id | fname   | lname   | city    | sal   |\n            |----|---------|---------|---------|-------|\n            | 2  | Vijay   | Mehta   | Pune    | 10000 |\n\n        *   **Select all employees whose last name is NOT IN 'Mehta' AND 'Patil' AND having salary BETWEEN 2000 AND 4000:**\n            ```sql\n            SELECT * FROM emp\n            WHERE lname NOT IN ('Mehta', 'Patil') AND sal BETWEEN 2000 AND 4000;\n            ```\n            **Output:**\n            | id | fname | lname  | city | sal  |\n            |----|-------|--------|------|------|\n            | 4  | Jayesh | Sharma | Pune | 2000 |\n\n#### 5. Pattern Matching Operators\n\n`LIKE`, `NOT LIKE`, `IS NULL`, `IS NOT NULL` are used for pattern matching.\n\n*   **Syntax:**\n    ```sql\n    SELECT * FROM tablename WHERE columnname LIKE 'pattern';\n    SELECT * FROM tablename WHERE columnname NOT LIKE 'pattern';\n    ```\n*   **Wildcard Characters:**\n    *   `%`: Represents zero or more sequence of characters.\n    *   `_`: Represents exactly a single character.\n\n**Example `emp` table data:**\n| id | fname   | lname   | sal   |\n|----|---------|---------|-------|\n| 1  | Vijay   | Mehta   | 10000 |\n| 2  | Ajay    | Sharma  | 5000  |\n| 3  | Sanjay  | Patil   | 7000  |\n| 4  | Jayesh  | Sharma  | 2000  |\n| 5  | Mukesh  | Patel   | NULL  |\n\n**Queries using Pattern Matching Operators:**\n\n*   **Select all employees whose first name ends with 'y':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '%y';\n    ```\n    **Output IDs:** 1, 2, 3\n\n*   **Select all employees whose first name starts with 'A' AND last name ends with 'a':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE 'A%' AND lname LIKE '%a';\n    ```\n    **Output IDs:** 2\n\n*   **Select all employees whose first name contains 'ja':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '%ja%';\n    ```\n    **Output IDs:** 1, 2, 3, 4\n\n*   **Select all employees whose first name contains 'ja' but does not start with 'ja':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '%ja%' AND fname NOT LIKE 'ja%';\n    ```\n    **Output IDs:** 1, 2, 3\n\n*   **Select all employees having a first name whose second letter is 'a':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '_a%';\n    ```\n    **Output IDs:** 3, 4\n\n*   **Select all employees whose first name length is exactly 6 characters:**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '______'; -- Six underscores\n    ```\n    **Output IDs:** 3, 4\n\n*   **Find the first name of all employees that have 'a' in the second position AND end with 'y' OR 'h':**\n    ```sql\n    SELECT * FROM emp WHERE fname LIKE '_a%y' OR fname LIKE '_a%h';\n    ```\n    *(Output depends on specific data matching this pattern)*\n\n*   **Select all employees where salary IS NULL:**\n    ```sql\n    SELECT * FROM emp WHERE sal IS NULL;\n    ```\n    **Output IDs:** 5\n\n*   **Select all employees where salary IS NOT NULL:**\n    ```sql\n    SELECT * FROM emp WHERE sal IS NOT NULL;\n    ```\n    **Output IDs:** 1, 2, 3, 4\n\n---\n\n### Set Operators\n\nSet operators combine the results of two `SELECT` statements into a single result set (table). Queries containing set operators are called **Compound Queries**.\n\n**The set operators are:**\n1.  `UNION` / `UNION ALL`\n2.  `INTERSECT`\n3.  `MINUS` (or `EXCEPT` in some SQL dialects)\n\n**Rules for Set Operators:**\n1.  Each `SELECT` statement must have the same number of columns.\n2.  The corresponding columns in each `SELECT` statement must have compatible datatypes (or the same datatype).\n3.  The columns in each `SELECT` statement should be in the same order.\n\n#### 1. `UNION` / `UNION ALL`\n\n*   **Effect:** All values from both `SELECT` statements (tables) will be selected.\n*   **Difference:**\n    *   `UNION`: Removes duplicate records from the combined result set.\n    *   `UNION ALL`: Includes all records, even if they are duplicates.\n\n*   **Syntax:**\n    ```sql\n    SELECT columnname(s) FROM table1\n    UNION ALL\n    SELECT columnname(s) FROM table2;\n    ```\n\n*   **Conceptual Diagram:**\n    ```mermaid\n    graph TD\n        A[Result Set 1] --> C\n        B[Result Set 2] --> C\n        C{UNION / UNION ALL}\n        C -- UNION --> D[Unique records from both]\n        C -- UNION ALL --> E[All records from both, including duplicates]\n    ```\n\n*   **Example Tables:**\n    *   **cust.table:**\n        | cid | cname     | ccity   |\n        |-----|-----------|---------|\n        | 1   | priyanka  | mum     |\n        | 2   | vipul     | Delhi   |\n        | 3   | vijay     | Berlin  |\n    *   **emp.table:**\n        | eid | ename     | ecity    |\n        |-----|-----------|----------|\n        | 1   | Kalpana   | London   |\n        | 2   | vijay     | Berlin   |\n        | 3   | Sandhya   | newyork  |\n        | 4   | varsha    | texas    |\n        | 5   | Vijay     | Delhi    |\n\n*   **Query (`UNION ALL`):**\n    ```sql\n    SELECT cname FROM cust\n    UNION ALL\n    SELECT ename FROM emp;\n    ```\n    **Output (cname/ename):**\n    | cname_or_ename |\n    |----------------|\n    | priyanka       |\n    | vipul          |\n    | vijay          |\n    | Kalpana        |\n    | vijay          |\n    | Sandhya        |\n    | varsha         |\n    | Vijay          |\n\n*   **Query (`UNION` - removes duplicates like 'vijay' and 'Vijay')**\n    ```sql\n    SELECT cname FROM cust\n    UNION\n    SELECT ename FROM emp;\n    ```\n    **Output (cname/ename):**\n    | cname_or_ename |\n    |----------------|\n    | priyanka       |\n    | vipul          |\n    | vijay          |\n    | Kalpana        |\n    | Sandhya        |\n    | varsha         |\n    | Vijay          |\n\n#### 2. `INTERSECT`\n\n*   **Effect:** Returns only the values (rows) that are common to both `SELECT` statements.\n*   **Conceptual Diagram:**\n    ```mermaid\n    graph TD\n        A[Result Set 1] --> C\n        B[Result Set 2] --> C\n        C{INTERSECT} --> D[Common records in both]\n    ```\n\n*   **Query (`INTERSECT` on specific columns):**\n    ```sql\n    SELECT cid, cname FROM cust WHERE cid >= 2\n    INTERSECT\n    SELECT eid, ename FROM emp WHERE eid >= 3;\n    ```\n    **Output:**\n    | id | name  |\n    |----|-------|\n    | 3  | vijay |\n\n*   **Query (`INTERSECT` on name only):**\n    ```sql\n    SELECT cname FROM cust\n    INTERSECT\n    SELECT ename FROM emp;\n    ```\n    **Output (common names):**\n    | cname_or_ename |\n    |----------------|\n    | vijay          |\n\n*   **Query (`INTERSECT` on name and city):**\n    ```sql\n    SELECT cname, ccity FROM cust\n    INTERSECT\n    SELECT ename, ecity FROM emp;\n    ```\n    **Output (common name-city pairs):**\n    | name  | city   |\n    |-------|--------|\n    | vijay | Berlin |\n\n#### 3. `MINUS` (or `EXCEPT`)\n\n*   **Effect:** Combines two `SELECT` statements and returns only those values (rows) that belong to the *first* `SELECT` statement and are *not present* in the second `SELECT` statement.\n*   **Conceptual Diagram:**\n    ```mermaid\n    graph TD\n        A[Result Set 1] --> C\n        B[Result Set 2] --> C\n        C{MINUS} --> D[Records in Result Set 1, but NOT in Result Set 2]\n    ```\n\n*   **Query (`MINUS`):**\n    ```sql\n    SELECT cname FROM cust\n    MINUS\n    SELECT ename FROM emp;\n    ```\n    **Output (names in `cust` but not in `emp`):**\n    | cname_or_ename |\n    |----------------|\n    | priyanka       |\n    | vipul          |\n\n---\n\n### String Manipulation Functions\n\nThese functions operate on a set of characters (strings).\n\n| Function                                    | Description                                                          |\n|---------------------------------------------|----------------------------------------------------------------------|\n| `UPPER(columnname)`                         | Converts the string into uppercase.                                  |\n| `LOWER(columnname)`                         | Converts the string into lowercase.                                  |\n| `LENGTH(columnname)`                        | Returns the length of the string (1-based index).                    |\n| `REPLACE(string, 'oldvalue', 'newvalue')`   | Replaces all occurrences of a substring with a new substring (case sensitive). |\n| `CONCAT(string1, string2, ...)`             | Adds two or more strings together.                                   |\n| `CONCAT_WS('separator', str1, str2, ...)`   | Concatenates strings with a specified separator.                     |\n| `SUBSTRING(string, start, length)`          | Extracts a substring from a string. `start` (1-based position), `length` (number of characters). If `start` is negative, count from the end. |\n| `INSTR(string, 'substring')`                | Returns the position of the first occurrence of `substring` in `string` (1-based, often not case-sensitive by default). |\n| `RTRIM(columnname)`                         | Removes trailing (right-side) white spaces from the string.         |\n| `LTRIM(columnname)`                         | Removes leading (left-side) white spaces from the string.           |\n| `TRIM(columnname)`                          | Removes both leading and trailing white spaces from the string.      |\n| `RIGHT(columnname, no_of_chars)`            | Returns the specified rightmost number of characters from the string. |\n| `LEFT(columnname, no_of_chars)`             | Returns the specified leftmost number of characters from the string.  |\n| `REVERSE(string)`                           | Returns the string with the order of characters reversed.             |\n| `BIN(value)`                                | Converts a decimal value to its binary string representation.         |\n| `HEX(value)`                                | Converts a decimal value to its hexadecimal string representation.    |\n| `OCT(value)`                                | Converts a decimal value to its octal string representation.          |\n| `BIT_LENGTH(columnname)`                    | Returns the length of the string in bits (each character is typically 8 bits). |\n\n**Example `emp` table data:**\n| id | fname   | lname   | sal   | dept    | city    |\n|----|---------|---------|-------|---------|---------|\n| 1  | VIJAY   | MEHTA   | 10000 | Sales   | Pune    |\n| 2  | AJAY    | PATIL   | 5000  | Sales   | mumbai  |\n| 3  | SANJAY  | SHARMA  | 7000  | HR.     | mumbai  |\n| 4  | JAYESH  | PATEL   | 2000  | HR      | kolkata |\n| 5  | MUKESH  | PATIL   | NULL  | ACC     | Pune    |\n\n**Queries using String Manipulation Functions:**\n\n*   **Fetch firstname in small letters and lastname in capital letters:**\n    ```sql\n    SELECT UPPER(lname), LOWER(fname) FROM emp;\n    ```\n    **Output:**\n    | UPPER(lname) | LOWER(fname) |\n    |--------------|--------------|\n    | MEHTA        | vijay        |\n    | PATIL        | ajay         |\n    | SHARMA       | sanjay       |\n    | PATEL        | jayesh       |\n    | PATIL        | mukesh       |\n\n*   **Find position of 'a' in firstname:**\n    ```sql\n    SELECT INSTR(fname, 'a') FROM emp;\n    ```\n    **Output:**\n    | INSTR(fname, 'a') |\n    |-------------------|\n    | 4                 |\n    | 1                 |\n    | 2                 |\n    | 2                 |\n    | 0                 |\n\n*   **Find position of 'a' in 'Vijay':**\n    ```sql\n    SELECT INSTR(fname, 'a') FROM emp WHERE fname = 'Vijay';\n    ```\n    **Output:**\n    | INSTR(fname, 'a') |\n    |-------------------|\n    | 4                 |\n\n*   **Replace each 'a' in `lname` with 'aa':**\n    ```sql\n    SELECT REPLACE(lname, 'a', 'aa') FROM emp;\n    ```\n    **Output:**\n    | REPLACE(lname, 'a', 'aa') |\n    |---------------------------|\n    | Mehataa                   |\n    | Paatil                    |\n    | Shaarmaa                  |\n    | Paatel                    |\n    | Paatil                    |\n\n*   **Display length of firstname:**\n    ```sql\n    SELECT LENGTH(fname) FROM emp;\n    ```\n    **Output:**\n    | LENGTH(fname) |\n    |---------------|\n    | 5             |\n    | 4             |\n    | 6             |\n    | 6             |\n    | 6             |\n\n*   **Display length of `fname` in bits:**\n    ```sql\n    SELECT BIT_LENGTH(fname) FROM emp;\n    ```\n    **Output:**\n    | BIT_LENGTH(fname) |\n    |-------------------|\n    | 40                |\n    | 32                |\n    | 48                |\n    | 48                |\n    | 48                |\n\n*   **Display salary in reverse order (as a string):**\n    ```sql\n    SELECT REVERSE(sal) FROM emp;\n    ```\n    **Output:**\n    | REVERSE(sal) |\n    |--------------|\n    | 00001        |\n    | 00005        |\n    | 0007         |\n    | 0002         |\n    | NULL         |\n\n*   **Display cities after removing white spaces from left, right, and both sides:**\n    ```sql\n    SELECT LTRIM(city), RTRIM(city), TRIM(city) FROM emp;\n    ```\n    **Output:**\n    | LTRIM(city) | RTRIM(city) | TRIM(city) |\n    |-------------|-------------|------------|\n    | Pune        | Pune        | Pune       |\n    | mumbai      | mumbai      | mumbai     |\n    | mumbai      | mumbai      | mumbai     |\n    | Kolkata     | Kolkata     | Kolkata    |\n    | Pune        | Pune        | Pune       |\n\n*   **Print first 3 characters of `fname`:**\n    ```sql\n    SELECT SUBSTRING(fname, 1, 3) FROM emp;\n    ```\n    **Output:**\n    | SUBSTRING(fname, 1, 3) |\n    |------------------------|\n    | VIJ                    |\n    | AJA                    |\n    | SAN                    |\n    | JAY                    |\n    | MUK                    |\n\n*   **Select substring (fname, 2, 4) from emp:**\n    ```sql\n    SELECT SUBSTRING(fname, 2, 4) FROM emp;\n    ```\n    **Output:**\n    | SUBSTRING(fname, 2, 4) |\n    |------------------------|\n    | IJAY                   |\n    | JAY                    |\n    | ANJA                   |\n    | AYES                   |\n    | UKES                   |\n\n*   **Select substring (fname, -6, 2) from emp:**\n    ```sql\n    SELECT SUBSTRING(fname, -6, 2) FROM emp;\n    ```\n    **Output:**\n    | SUBSTRING(fname, -6, 2) |\n    |-------------------------|\n    | VIJ                     |\n    | AJA                     |\n    | SAN                     |\n    | JAY                     |\n    | MUK                     |\n\n*   **Select lower(reverse(fname)) from emp:**\n    ```sql\n    SELECT LOWER(REVERSE(fname)) FROM emp;\n    ```\n    **Output:**\n    | LOWER(REVERSE(fname)) |\n    |-----------------------|\n    | yajiv                 |\n    | yaja                  |\n    | yajnas                |\n    | hseyaj                |\n    | hsekum                |\n\n*   **Select first 4 characters from right and left of `fname`:**\n    ```sql\n    SELECT RIGHT(fname, 4), LEFT(fname, 4) FROM emp;\n    ```\n    **Output:**\n    | RIGHT(fname, 4) | LEFT(fname, 4) |\n    |-----------------|----------------|\n    | IJAY            | VIJA           |\n    | AJAY            | AJAY           |\n    | NJAY            | SANJ           |\n    | YESH            | JAYE           |\n    | KESH            | MUKE           |\n\n*   **Display `id` in binary:**\n    ```sql\n    SELECT BIN(id) FROM emp;\n    ```\n    **Output:**\n    | BIN(id) |\n    |---------|\n    | 1       |\n    | 10      |\n    | 11      |\n    | 100     |\n    | 101     |\n\n*   **Combine and display `fname` and `lname`:**\n    ```sql\n    SELECT CONCAT(fname, lname) FROM emp;\n    ```\n    **Output:**\n    | CONCAT(fname, lname) |\n    |----------------------|\n    | VIJAYMEHTA           |\n    | AJAYPATIL            |\n    | SANJAYSHARMA         |\n    | JAYESHPATEL          |\n    | MUKESHPATIL          |\n\n*   **Select `CONCAT_WS('-', fname, lname)` from emp:**\n    ```sql\n    SELECT CONCAT_WS('-', fname, lname) FROM emp;\n    ```\n    **Output:**\n    | CONCAT_WS('-', fname, lname) |\n    |------------------------------|\n    | VIJAY-MEHTA                  |\n    | AJAY-PATIL                   |\n    | SANJAY-SHARMA                |\n    | JAYESH-PATEL                 |\n    | MUKESH-PATIL                 |\n\n*   **Create a new column and add concatenation of `fname` and `lname` separated by space:**\n    ```sql\n    ALTER TABLE emp ADD COLUMN full_name VARCHAR(40);\n    UPDATE emp SET full_name = CONCAT_WS(' ', fname, lname);\n    ```\n\n---\n\n### `GROUP BY` Clause\n\nThe `GROUP BY` clause is used to group rows that have the same values in specified columns into summary rows, like \"find the number of customers in each country\". It is often used with aggregate functions (COUNT, MAX, MIN, SUM, AVG) to perform calculations on each group.\n\n*   **Note:** If you want to perform calculations group-wise, the `GROUP BY` clause is essential, as aggregate functions normally operate on the entire set of results.\n\n**Example `emp` table data:**\n| id | fname   | lname   | sal   | deptname   |\n|----|---------|---------|-------|------------|\n| 1  | ramesh  | kale    | 6000  | Sales      |\n| 2  | vijay   | sharma  | 9000  | ACC        |\n| 3  | ramesh  | patil   | 6000  | Sales      |\n| 4  | mukesh  | patil   | 4000  | HR         |\n| 5  | ajay    | bhave   | 10000 | marketing  |\n| 6  | manoj   | sharma  | 15000 | ACC        |\n| 7  | pramod  | patil   | NULL  | HR         |\n| 8  | null    | null    | NULL  | HR         |\n\n**Syntax:**\n```sql\nSELECT columnname, aggregate_fun_name(columnname)\nFROM tablename\nWHERE condition\nGROUP BY columnname\nORDER BY columnname ASC/DESC;\n```\n\n**Queries using `GROUP BY`:**\n\n*   **Find average salary of all employees:**\n    ```sql\n    SELECT AVG(sal) FROM emp;\n    ```\n    **Output:**\n    `8333.33`\n\n*   **Find average salary of employees according to their department:**\n    ```sql\n    SELECT deptname, AVG(sal) FROM emp GROUP BY deptname;\n    ```\n    **Output:**\n    | deptname   | AVG(sal) |\n    |------------|----------|\n    | Sales      | 6000     |\n    | ACC        | 12000    |\n    | HR         | 4000     |\n    | marketing  | 10000    |\n\n*   **Display total salary of employees according to the department when salary is greater than 5000:**\n    ```sql\n    SELECT SUM(sal), deptname FROM emp WHERE sal > 5000 GROUP BY deptname;\n    ```\n    **Output:**\n    | SUM(sal) | deptname   |\n    |----------|------------|\n    | 12000    | Sales      |\n    | 24000    | ACC        |\n    | 10000    | marketing  |\n\n*   **Select total salary of employees according to department name and lastname:**\n    ```sql\n    SELECT deptname, lname, SUM(sal) FROM emp GROUP BY deptname, lname;\n    ```\n    **Output:**\n    | deptname   | lname    | SUM(sal) |\n    |------------|----------|----------|\n    | Sales      | Kale     | 6000     |\n    | Sales      | Patil    | 6000     |\n    | ACC        | Sharma   | 24000    |\n    | HR         | Patil    | 4000     |\n    | HR         | Patil    | NULL     |\n    | HR         | null     | NULL     |\n    | marketing  | Bhave    | 10000    |\n\n---\n\n### `HAVING` Clause\n\nThe `HAVING` clause is used to apply conditions on `aggregate` functions. It is used instead of the `WHERE` clause when filtering grouped results.\n\n**Syntax:**\n```sql\nSELECT columnname, aggregate_fun_name(columnname)\nFROM tablename\nWHERE condition\nGROUP BY columnname\nHAVING aggregate_fun_name(columnname) condition;\n```\n\n**Queries using `HAVING`:**\n\n*   **Display total salary of employees according to department name where total salary is greater than 10,000:**\n    ```sql\n    SELECT deptname, SUM(sal) FROM emp GROUP BY deptname HAVING SUM(sal) > 10000;\n    ```\n    **Output:**\n    | deptname   | SUM(sal) |\n    |------------|----------|\n    | Sales      | 12000    |\n    | ACC        | 24000    |\n\n---\n\n### `ORDER BY` Clause\n\nThe `ORDER BY` clause is used to sort the result set of a query in ascending or descending order based on one or more columns.\n\n*   **Default:** `ASC` (ascending order).\n*   **Keywords:** `ASC` (for ascending), `DESC` (for descending).\n\n**Syntax:**\n```sql\nSELECT columnname(s)\nFROM tablename\nWHERE condition\nORDER BY columnname ASC/DESC;\n```\n**OR (with `GROUP BY` and `HAVING`):**\n```sql\nSELECT columnname, aggregate_fun_name(columnname)\nFROM tablename\nWHERE condition\nGROUP BY columnname\nHAVING condition\nORDER BY columnname ASC/DESC;\n```\n\n**Queries using `ORDER BY`:**\n*(Using a modified `emp` table data)*\n| id | fname   | sal   | deptname   |\n|----|---------|-------|------------|\n| 2  | Vijay   | 9000  | ACC        |\n| 7  | Pramod  | NULL  | HR         |\n| 3  | Ramesh  | 6000  | Sales      |\n| 4  | Mukesh  | 4000  | HR         |\n| 6  | Manoj   | 15000 | ACC        |\n| 5  | Ajay    | 10000 | marketing  |\n| 8  | NULL    | NULL  | HR         |\n\n*   **Display ID, firstname, and salary of employees according to the descending order of firstname:**\n    ```sql\n    SELECT id, fname, sal FROM emp ORDER BY fname DESC;\n    ```\n    **Output:**\n    | id | fname   | sal   |\n    |----|---------|-------|\n    | 2  | Vijay   | 9000  |\n    | 7  | Pramod  | NULL  |\n    | 4  | Mukesh  | 4000  |\n    | 6  | Manoj   | 15000 |\n    | 3  | Ramesh  | 6000  |\n    | 8  | NULL    | NULL  |\n    | 5  | Ajay    | 10000 |\n    *(Note: NULL values often appear first in ascending order and last in descending order, depending on the database system.)*\n\n*   **Display ID, deptname, fname, sal according to ascending order of `deptname` and descending order of `fname`:**\n    ```sql\n    SELECT id, deptname, fname, sal FROM emp ORDER BY deptname ASC, fname DESC;\n    ```\n    **Output:**\n    | id | deptname   | fname   | sal   |\n    |----|------------|---------|-------|\n    | 6  | ACC        | Manoj   | 15000 |\n    | 2  | ACC        | Vijay   | 9000  |\n    | 4  | HR         | Mukesh  | 4000  |\n    | 7  | HR         | Pramod  | NULL  |\n    | 8  | HR         | NULL    | NULL  |\n    | 5  | marketing  | Ajay    | 10000 |\n    | 3  | Sales      | Ramesh  | 6000  |\n\n*   **Select total salary of employees grouped by `deptname` having total salary greater than 10,000 according to the descending order of `deptname`:**\n    ```sql\n    SELECT SUM(sal), deptname FROM emp\n    GROUP BY deptname\n    HAVING SUM(sal) > 10000\n    ORDER BY deptname DESC;\n    ```\n    **Output:**\n    | SUM(sal) | deptname   |\n    |----------|------------|\n    | 24000    | ACC        |\n    | 12000    | Sales      |\n\n---\n\n### Views in SQL\n\nA view is a virtual table through which only a selective portion of the data from one or more tables can be seen.\n\n*   **Concept:** A view is basically a subset of a table created by a query. To the normal user (DB user), a view appears just like a regular table, with its own columns and rows.\n*   **Purpose:**\n    *   **Security:** Views are primarily used for security purposes. They restrict users from viewing certain columns and rows in the base tables. A view displays only those records and columns that are explicitly mentioned in its defining query.\n    *   **Simplicity:** Views can simplify complex queries by pre-joining tables or pre-filtering data.\n    *   **Data Abstraction:** Views can present data in a customized format, hiding the complexity of the underlying base tables.\n\n*   **Example 1: Employee Data Security**\n    *   **Base Table:** `emp` (eid, name, loginid, pswd, sal, DoJ)\n    *   **Scenario:** The administrator does not want users to access critical information like `loginid` and `pswd`.\n    *   **Solution:** Create a view that includes only `eid`, `name`, `sal`, `doj` as output. This allows users to view only this permitted information.\n\n*   **Example 2: Salesperson Focus**\n    *   **Base Tables:** `Product` (pid, pname, price, brand, colors), `Customer` (cid, cname, address, no_of_prod_bought, location, email-id)\n    *   **Scenario:** A salesperson is only interested in details like (pid, pname, brand, cid, no_of_prod_bought).\n    *   **Solution:** A view can be created to combine relevant information from `Product` and `Customer` tables, exposing only the necessary columns to the salesperson.\n\n#### Creating and Modifying Views\n\n**Syntax for Creating or Replacing a View:**\n```sql\nCREATE OR REPLACE VIEW view_name AS\nSELECT columnname(s) FROM tablename WHERE condition;\n```\n\n**Example `emp` table data:**\n| id | lname   | fname   | sal   | dept | city    |\n|----|---------|---------|-------|------|---------|\n| 9  | Shah    | Atul    | 7000  | marketing | mum     |\n| 10 | Jadhav  | Amol    | 5000  | HR   | Pune    |\n| 11 | Patel   | Sayli   | 9000  | Sales | mum     |\n| 12 | Kadam   | Tanvi   | 8000  | Sales | mum     |\n| 13 | Patel   | Nayan   | 9000  | HR   | Kolkata |\n| 14 | Patil   | Vishal  | 8000  | Sales | Kolkata |\n| 15 | Bhave   | Viraj   | 6000  | ACC  | Pune    |\n\n*   **Query 1: Create view `v1` for employees in 'mumbai' or 'pune':**\n    ```sql\n    CREATE VIEW v1 AS\n    SELECT id, fname, sal, city FROM emp WHERE city = 'mumbai' OR city = 'pune';\n    ```\n    `SELECT * FROM v1;`\n    **Output (`v1`):**\n    | id | fname   | sal   | city |\n    |----|---------|-------|------|\n    | 10 | Amol    | 5000  | Pune |\n    | 15 | Viraj   | 6000  | Pune |\n\n*   **Query 2: Create OR REPLACE view `v1` for employees in 'mum' or 'pune' with salary > 5000:**\n    ```sql\n    CREATE OR REPLACE VIEW v1 AS\n    SELECT id, fname, sal, city FROM emp WHERE (city = 'mum' OR city = 'pune') AND sal > 5000;\n    ```\n    `SELECT * FROM v1;`\n    **Output (`v1`):**\n    | id | fname   | sal   | city |\n    |----|---------|-------|------|\n    | 9  | Atul    | 7000  | mum  |\n    | 11 | Sayli   | 9000  | mum  |\n    | 12 | Tanvi   | 8000  | mum  |\n    | 15 | Viraj   | 6000  | Pune |\n\n*   **To \"Add a Column\" to a View (by redefining):**\n    ```sql\n    ALTER VIEW v1 AS\n    SELECT id, fname, lname, city, sal FROM emp WHERE city = 'mumbai';\n    ```\n    *(This is typically achieved by `CREATE OR REPLACE VIEW` with the updated `SELECT` statement, effectively redefining the view.)*\n\n*   **Updating a View:**\n    ```sql\n    UPDATE v1 SET id = 99 WHERE id = 9;\n    ```\n    `SELECT * FROM v1;`\n    **Output (`v1` after update):**\n    | id | fname   | sal   | city | lname   |\n    |----|---------|-------|------|---------|\n    | 99 | Atul    | 7000  | mum  | Shah    |\n    | 11 | Sayli   | 9000  | mum  | Patel   |\n    | 12 | Tanvi   | 8000  | mum  | Kadam   |\n    | 15 | Viraj   | 6000  | Pune | Bhave   |\n    *Note: When a view is updatable, changes made to the view are reflected in the underlying base table. For example, updating `id=9` to `id=99` in `v1` also updates `id=9` in the `emp` table.*\n\n*   **Adding a Record to a View (if updatable):**\n    *   `INSERT INTO v1 VALUES (16, 'Tejas', 19000, 'Pune', 'Kadam');`\n    *   **Note:** If the view is updatable and the `INSERT` operation is valid, the record will be added to the underlying `emp` table. For columns not included in the view but required by the base table, they would need to have default values or be nullable.\n\n*   **Constraints on View Updatability:**\n    Views are generally **not updatable or insertable** if they contain any of the following:\n    1.  Aggregate functions (`SUM`, `AVG`, `COUNT`, `MAX`, `MIN`)\n    2.  `HAVING` clause\n    3.  `GROUP BY` clause\n    4.  `UNION` / `UNION ALL`\n    5.  `SUBQUERY` in the `WHERE` clause that refers to the `FROM` clause's table.\n    6.  `DISTINCT` keyword\n    7.  Calculated columns\n\n*   **To See All Views in the Database:**\n    ```sql\n    SHOW FULL TABLES IN databasename WHERE table_type LIKE 'VIEW';\n    ```\n    **Example:**\n    ```sql\n    SHOW FULL TABLES IN priyanka WHERE table_type LIKE 'VIEW';\n    ```\n\n*   **To Drop a View:**\n    ```sql\n    DROP VIEW view_name;\n    ```\n\n*   **Nested View (View on a View):**\n    You can create a view based on an existing view.\n    ```sql\n    CREATE VIEW v2 AS SELECT empid, fname, sal FROM v1 WHERE sal > 7000;\n    ```\n    **Output (`v2`):**\n    | empid | fname   | sal   |\n    |-------|---------|-------|\n    | 91    | Sayli   | 9000  |\n\n---\n\n### SQL JOINs\n\nSQL `JOIN`s are used to select data from two or more tables to create a comprehensive result set. They combine fields from multiple tables by using common values present in the columns of these tables.\n\n*   **Example Scenario:**\n    *   **`emp` table:** Stores employee basic information (eid, fname, lname).\n    *   **`Product` table:** Stores product information and which employee ordered it (pid, pname, eid (FK)).\n\n*   **Conceptual Schema (Mermaid ER Diagram):**\n    ```mermaid\n    erDiagram\n        EMP ||--o{ PRODUCT : \"orders\"\n        EMP {\n            int eid PK \"Employee ID\"\n            varchar fname \"First Name\"\n            varchar lname \"Last Name\"\n        }\n        PRODUCT {\n            int pid PK \"Product ID\"\n            varchar pname \"Product Name\"\n            int eid FK \"Employee ID (FK)\"\n        }\n    ```\n\n*   **Example `emp` table data:**\n    | eid  | fname  | lname  |\n    |------|--------|--------|\n    | 7369 | Vijay  | Sharma |\n    | 7499 | Sanjay | Patil  |\n    | 7521 | Mukesh | Patel  |\n    | 04   | Pramod | Bhave  |\n\n*   **Example `Product` table data:**\n    | pid  | pname  | eid  |\n    |------|--------|------|\n    | 234  | shirt  | 7369 |\n    | 657  | Top    | 7521 |\n    | 865  | Tshirt | 7521 |\n    | 878  | Jeans  | NULL | *(Note: Eid is NULL for Jeans, assuming some products might not be ordered)*\n\n*   **Query 1: Find which employee ordered which product:**\n    ```sql\n    SELECT emp.fname, emp.lname, Product.pname\n    FROM emp, Product\n    WHERE emp.eid = Product.eid;\n    ```\n    *(This is an implicit `INNER JOIN`)*\n    **Output:**\n    | fname  | lname  | pname  |\n    |--------|--------|--------|\n    | Vijay  | Sharma | shirt  |\n    | Mukesh | Patel  | Top    |\n    | Mukesh | Patel  | Tshirt |\n\n#### Types of JOINs\n\n1.  **`INNER JOIN` (or Simple Join)**\n    *   **Effect:** Returns only the rows that have matching values in both tables. Records that do not have a match in the other table are excluded.\n    *   **Conceptual Diagram:**\n        ```mermaid\n        graph TD\n            A[Table 1] -- Matching Rows --> C\n            B[Table 2] -- Matching Rows --> C\n            C{INNER JOIN} --> D[Intersection of Records]\n        ```\n    *   **Syntax:**\n        ```sql\n        SELECT column_list\n        FROM table1\n        INNER JOIN table2 ON join_condition;\n        ```\n    *   **Query 1: Find which employee ordered which product (using `INNER JOIN`):**\n        ```sql\n        SELECT emp.fname, emp.lname, Product.pname\n        FROM emp\n        INNER JOIN Product ON emp.eid = Product.eid;\n        ```\n        **Output:**\n        | fname  | lname  | pname  |\n        |--------|--------|--------|\n        | Vijay  | Sharma | shirt  |\n        | Mukesh | Patel  | Top    |\n        | Mukesh | Patel  | Tshirt |\n\n    *   **Query 2: Find which employee ordered 'shirt':**\n        ```sql\n        SELECT emp.eid, emp.fname, emp.lname, Product.pname\n        FROM emp\n        INNER JOIN Product ON emp.eid = Product.eid AND Product.pname = 'shirt';\n        ```\n        **Output:**\n        | eid  | fname | lname  | pname |\n        |------|-------|--------|-------|\n        | 7369 | Vijay | Sharma | shirt |\n\n2.  **`EQUI-JOIN`**\n    *   **Difference from `INNER JOIN`:** An `EQUI-JOIN` specifically uses only the equality operator (`=`) in its join condition. An `INNER JOIN` can use any comparison operator (`<`, `>`, `!=`, etc.) in its `ON` clause.\n    *   **Example (`EQUI-JOIN`):**\n        ```sql\n        SELECT emp.fname, emp.lname, Product.pname\n        FROM emp\n        JOIN Product ON emp.eid = Product.eid; -- This is an Equi-Join\n        ```\n        *(If other operators are used, it's generally just an `INNER JOIN`.)*\n\n3.  **`NATURAL JOIN`**\n    *   **Effect:** Automatically joins two tables based on all columns that have the same name and compatible data types in both tables. It implies an `EQUI-JOIN` for all common columns.\n    *   **Purpose:** The join condition is implicit; `NATURAL JOIN` automatically finds the matching column names.\n    *   **Query Example:**\n        ```sql\n        SELECT emp.fname, emp.lname, emp.eid, Product.pname, Product.pid\n        FROM emp NATURAL JOIN Product;\n        ```\n\n4.  **`CROSS JOIN` (Cartesian Join)**\n    *   **Effect:** Performs a Cartesian product of rows from multiple tables. Each row of the first table is combined with every row of the second table. This results in a number of rows equal to (rows in table1 * rows in table2).\n    *   **Use Case:** Rarely used directly, but important for understanding how joins work.\n    *   **Query Example:**\n        ```sql\n        SELECT emp.eid, emp.fname, emp.lname, Product.pid, Product.pname\n        FROM emp CROSS JOIN Product;\n        ```\n        **Partial Output (for illustration):**\n        | eid  | fname  | lname  | pid  | pname  |\n        |------|--------|--------|------|--------|\n        | 7369 | Vijay  | Sharma | 234  | shirt  |\n        | 7369 | Vijay  | Sharma | 657  | Top    |\n        | 7369 | Vijay  | Sharma | 865  | Tshirt |\n        | 7369 | Vijay  | Sharma | 878  | Jeans  |\n        | 7499 | Sanjay | Patil  | 234  | shirt  |\n        | ...  | ...    | ...    | ...  | ...    |\n\n5.  **`LEFT OUTER JOIN` (or `LEFT JOIN`)**\n    *   **Effect:** Returns all the rows from the table on the \"left\" side of the `JOIN` clause, and only the matching rows from the \"right\" table. If no match is found for a left row, `NULL` values are returned for the columns of the right table.\n    *   **Conceptual Diagram:**\n        ```mermaid\n        graph TD\n            A[Table 1 (Left)] -- All Rows --> C\n            B[Table 2 (Right)] -- Matching Rows --> C\n            C{LEFT JOIN} --> D[Table 1 records + Matching Table 2 records; NULLs for non-matches in Table 2]\n        ```\n    *   **Query Example:**\n        ```sql\n        SELECT emp.eid, emp.fname, emp.lname, Product.pid, Product.pname\n        FROM emp LEFT JOIN Product ON emp.eid = Product.eid;\n        ```\n        **Output:**\n        | eid  | fname  | lname  | pid  | pname  |\n        |------|--------|--------|------|--------|\n        | 7369 | Vijay  | Sharma | 234  | shirt  |\n        | 7499 | Sanjay | Patil  | NULL | NULL   |\n        | 7521 | Mukesh | Patel  | 657  | Top    |\n        | 7521 | Mukesh | Patel  | 865  | Tshirt |\n        | 04   | Pramod | Bhave  | NULL | NULL   |\n\n6.  **`RIGHT OUTER JOIN` (or `RIGHT JOIN`)**\n    *   **Effect:** Opposite of `LEFT JOIN`. Returns all the rows from the table on the \"right\" side of the `JOIN` clause, and only the matching rows from the \"left\" table. If no match is found for a right row, `NULL` values are returned for the columns of the left table.\n    *   **Conceptual Diagram:**\n        ```mermaid\n        graph TD\n            A[Table 1 (Left)] -- Matching Rows --> C\n            B[Table 2 (Right)] -- All Rows --> C\n            C{RIGHT JOIN} --> D[Table 2 records + Matching Table 1 records; NULLs for non-matches in Table 1]\n        ```\n    *   **Query Example:**\n        ```sql\n        SELECT emp.eid, emp.fname, emp.lname, Product.pid, Product.pname\n        FROM emp RIGHT JOIN Product ON emp.eid = Product.eid;\n        ```\n        **Output:**\n        | eid  | fname  | lname  | pid  | pname  |\n        |------|--------|--------|------|--------|\n        | 7369 | Vijay  | Sharma | 234  | shirt  |\n        | 7521 | Mukesh | Patel  | 657  | Top    |\n        | 7521 | Mukesh | Patel  | 865  | Tshirt |\n        | NULL | NULL   | NULL   | 878  | Jeans  |\n\n7.  **`FULL OUTER JOIN` (or `FULL JOIN`)**\n    *   **Effect:** Returns all rows when there is a match in one of the tables. It is the conceptual combination of `LEFT OUTER JOIN` and `RIGHT OUTER JOIN`. If a row from one table does not have a match in the other, `NULL` values are used for columns from the non-matching table.\n    *   **Conceptual Diagram:**\n        ```mermaid\n        graph TD\n            A[Table 1] -- All Rows --> C\n            B[Table 2] -- All Rows --> C\n            C{FULL OUTER JOIN} --> D[Union of all records; NULLs for non-matches in either table]\n        ```\n    *   *(Note: MySQL does not natively support `FULL OUTER JOIN`. It can be emulated using `UNION` of `LEFT JOIN` and `RIGHT JOIN` results.)*\n    *   **Emulating `FULL OUTER JOIN` in MySQL:**\n        ```sql\n        SELECT emp.eid, emp.fname, emp.lname, Product.pid, Product.pname\n        FROM emp LEFT JOIN Product ON emp.eid = Product.eid\n        UNION\n        SELECT emp.eid, emp.fname, emp.lname, Product.pid, Product.pname\n        FROM emp RIGHT JOIN Product ON emp.eid = Product.eid;\n        ```\n\n---\n\n### Nested Queries (Subqueries)\n\nA subquery (or nested query) is an SQL query written inside another SQL query.\n\n*   **Placement:** A subquery can occur in various clauses of a main query, including:\n    *   `SELECT` clause\n    *   `FROM` clause\n    *   `WHERE` clause\n    *   `HAVING` clause\n    *   `INSERT` statement\n    *   `UPDATE` statement\n*   **Execution Order:** The inner query (subquery) executes first. Its result is then passed to the outer query, which uses this result to complete its operation.\n\n**Syntax (common usage in `WHERE` clause):**\n```sql\nSELECT columnname(s) FROM tablename\nWHERE columnname operator (SELECT columnname FROM another_tablename WHERE condition);\n```\n\n**Example `emp` table data:**\n| eid | fname   | sal   |\n|-----|---------|-------|\n| 1   | Ajay    | 5000  |\n| 2   | Vijay   | 7000  |\n| 3   | Sanjay  | 9000  |\n| 4   | Jayesh  | 10000 |\n| 5   | Viraj   | 2000  |\n\n**Example `Product` table data:**\n| pid | pname  | eid |\n|-----|--------|-----|\n| 11  | Shirt  | 1   |\n| 12  | Tshirt | 2   |\n| 13  | Top    | 2   |\n| 14  | Jeans  | 3   |\n\n**Queries using Nested Queries:**\n\n*   **1. List employees paid more than 'Vijay':**\n    ```sql\n    SELECT eid, fname\n    FROM emp\n    WHERE sal > (SELECT sal FROM emp WHERE fname = 'Vijay');\n    ```\n    **Output:**\n    | eid | fname  |\n    |-----|--------|\n    | 3   | Sanjay |\n    | 4   | Jayesh |\n\n*   **2. Find all employees whose salary is higher than the average salary:**\n    ```sql\n    SELECT eid, fname, sal\n    FROM emp\n    WHERE sal > (SELECT AVG(sal) FROM emp);\n    ```\n    **Output:**\n    | eid | fname  | sal   |\n    |-----|--------|-------|\n    | 3   | Sanjay | 9000  |\n    | 4   | Jayesh | 10000 |\n\n*   **3. Find the employee having the highest salary:**\n    ```sql\n    SELECT eid, fname, sal\n    FROM emp\n    WHERE sal = (SELECT MAX(sal) FROM emp);\n    ```\n    **Output:**\n    | eid | fname  | sal   |\n    |-----|--------|-------|\n    | 4   | Jayesh | 10000 |\n\n*   **4. Find the second highest salary from `emp`:**\n    ```sql\n    SELECT MAX(sal)\n    FROM emp\n    WHERE sal != (SELECT MAX(sal) FROM emp);\n    ```\n    **Output:**\n    `9000`\n\n*   **5. Find the name of the employee having the second highest salary:**\n    ```sql\n    SELECT id, fname, sal\n    FROM emp\n    WHERE sal = (SELECT MAX(sal) FROM emp WHERE sal != (SELECT MAX(sal) FROM emp));\n    ```\n    **Output:**\n    | id | fname  | sal  |\n    |----|--------|------|\n    | 3  | Sanjay | 9000 |\n\n*   **6. Find the Nth highest salary from `emp`:**\n    ```sql\n    SELECT *\n    FROM emp AS emp1\n    WHERE (N - 1) = (SELECT COUNT(emp2.sal) FROM emp AS emp2 WHERE emp2.sal > emp1.sal);\n    ```\n    *   **Explanation:** This query works by counting how many salaries in the table are greater than a given salary (`emp1.sal`). For the Nth highest salary, there should be `(N-1)` salaries greater than it. For example, for the 3rd highest salary (N=3), `COUNT` should be `(3-1) = 2`.\n    *   **Note:** For the Nth *lowest* salary, the condition would be `emp2.sal < emp1.sal`.\n\n*   **7. Find details of employees who are NOT related to any product:**\n    ```sql\n    SELECT eid, fname, sal\n    FROM emp\n    WHERE eid NOT IN (SELECT DISTINCT eid FROM Product);\n    ```\n    **Output:**\n    | id | fname | sal  |\n    |----|-------|------|\n    | 5  | Viraj | 2000 |\n\n*   **8. Find names of employees who have purchased 'shirts' OR 'tshirts':**\n    ```sql\n    SELECT *\n    FROM emp\n    WHERE eid IN (SELECT eid FROM Product WHERE pname = 'shirt' OR pname = 'tshirt');\n    ```\n    **Output:**\n    | id | fname | sal  |\n    |----|-------|------|\n    | 1  | Ajay  | 5000 |\n    | 2  | Vijay | 7000 |\n\n*   **9. Increase salary of employees by 20% who are related to products 'jeans' OR 'tshirt':**\n    ```sql\n    UPDATE emp\n    SET sal = sal + (sal * 0.2)\n    WHERE eid IN (SELECT eid FROM Product WHERE pname = 'jeans' OR pname = 'tshirt');\n    ```\n    **Output (after update for affected rows):**\n    | id | fname | sal   |\n    |----|-------|-------|\n    | 2  | Vijay | 8400  |\n    | 3  | Sanjay | 10800 |\n\n*   **10. Find all employees who earn more than 'Ajay' AND less than 'Sanjay':**\n    ```sql\n    SELECT id, fname\n    FROM emp\n    WHERE sal > (SELECT sal FROM emp WHERE fname = 'Ajay')\n      AND sal < (SELECT sal FROM emp WHERE fname = 'Sanjay');\n    ```\n    **Output:**\n    | id | fname |\n    |----|-------|\n    | 2  | Vijay |\n\n---\n\n### Triggers\n\nA trigger is a special type of stored procedure that automatically executes (or \"fires\") when a specific event occurs in the database. It executes when a user tries to modify data through a DML (Data Manipulation Language) statement.\n\n*   **Components of a Trigger Definition:**\n    *   **`trigger_name`**: A unique name for the trigger.\n    *   **`trigger_time`**: Specifies when the trigger should fire relative to the event (`BEFORE` or `AFTER`).\n    *   **`trigger_event`**: The DML operation that causes the trigger to fire (`INSERT`, `UPDATE`, or `DELETE`).\n    *   **`table_name`**: The table on which the trigger is defined.\n    *   **`FOR EACH ROW`**: Indicates that the trigger should execute for every row affected by the `trigger_event`.\n    *   **`trigger_statement`**: The SQL statements that are executed when the trigger fires.\n\n#### Syntax for Triggers\n\n*   **For a Single Statement:**\n    ```sql\n    CREATE TRIGGER trigger_name trigger_time trigger_event\n    ON table_name FOR EACH ROW\n    trigger_statement;\n    ```\n\n*   **For Multiple Statements (using `BEGIN...END` block and `DELIMITER`):**\n    ```sql\n    DELIMITER //\n\n    CREATE TRIGGER trigger_name trigger_time trigger_event\n    ON table_name FOR EACH ROW\n    BEGIN\n        -- SQL statements\n        statement1;\n        statement2;\n        ...\n    END;\n    //\n\n    DELIMITER ;\n    ```\n    *(The `DELIMITER` command is used in client programs (like MySQL client) to temporarily change the statement delimiter so that the `BEGIN...END` block is treated as a single compound statement.)*\n\n#### Trigger Examples\n\n**Example 1: `AFTER INSERT` Trigger to Update a Separate Log Table**\n\n*   **Scenario:** Define a trigger to update a separate table (`emp_insert`) whenever a new record is inserted into the `emp` table.\n*   **Tables:**\n    *   `emp` (eid, lname, fname, sal)\n    *   `emp_insert` (fname, sal)\n*   **Steps:**\n    1.  `CREATE TABLE emp (eid INT PRIMARY KEY, lname VARCHAR(20), fname VARCHAR(20), sal INT);`\n    2.  `CREATE TABLE emp_insert (fname VARCHAR(20), sal INT);`\n    3.  **Create the trigger:**\n        ```sql\n        DELIMITER //\n        CREATE TRIGGER insert_attempt AFTER INSERT ON emp\n        FOR EACH ROW\n        BEGIN\n            INSERT INTO emp_insert SET fname = NEW.fname, sal = NEW.sal;\n        END;\n        //\n        DELIMITER ;\n        ```\n    4.  `SELECT * FROM emp_insert;` *(Initially empty)*\n    5.  `INSERT INTO emp VALUES (2, 'P', 'B', 15000);`\n    6.  `SELECT * FROM emp_insert;`\n        **Output:**\n        | fname | sal   |\n        |-------|-------|\n        | B     | 15000 |\n        *(The trigger automatically inserted the `fname` and `sal` from the new row into `emp_insert`.)*\n\n**Example 2: `AFTER INSERT` Trigger with Conditional Operations**\n\n*   **Scenario:** When a record is inserted into `stud` table, insert it into `stud1` and then delete from `stud1` any records with `marks < 60`.\n*   **Tables:**\n    *   `stud` (sid, name, marks)\n    *   `stud1` (name, marks)\n*   **Initial `stud1`:**\n    | name | marks |\n    |------|-------|\n    | ABC  | 60    |\n*   **Steps:**\n    1.  `CREATE TABLE stud (sid INT, name VARCHAR(10), marks INT);`\n    2.  `CREATE TABLE stud1 (name VARCHAR(10), marks INT);`\n    3.  `INSERT INTO stud1 VALUES ('ABC', 60);`\n    4.  **Create the trigger:**\n        ```sql\n        DELIMITER //\n        CREATE TRIGGER stud_insert AFTER INSERT ON stud\n        FOR EACH ROW\n        BEGIN\n            INSERT INTO stud1 SET name = NEW.name, marks = NEW.marks;\n            DELETE FROM stud1 WHERE marks < 60;\n        END;\n        //\n        DELIMITER ;\n        ```\n    5.  `INSERT INTO stud VALUES (2, 'xyz', 55);`\n    6.  `INSERT INTO stud VALUES (3, 'LMN', 90);`\n    7.  `SELECT * FROM stud1;`\n        **Output:**\n        | name | marks |\n        |------|-------|\n        | LMN  | 90    |\n        *(Explanation: 'xyz' (55 marks) was inserted but immediately deleted by the `DELETE` statement in the trigger. 'ABC' (60 marks) was kept. 'LMN' (90 marks) was inserted and kept.)*\n\n**Example 3: `AFTER UPDATE` Trigger with Conditional DML**\n\n*   **Scenario:** After updating a `person` record, insert the new values into `person1`, delete old salaries from `person1`, and update certain salaries in `person1`.\n*   **Tables:**\n    *   `person` (id, name, sal)\n    *   `person1` (name, sal)\n*   **Initial `person` data:**\n    | id | name | sal  |\n    |----|------|------|\n    | 1  | P    | 200  |\n    | 1  | a    | 2000 |\n*   **Initial `person1` data:**\n    | name | sal |\n    |------|-----|\n    | P    | 0   |\n*   **Steps:**\n    1.  `CREATE TABLE person (id INT, name VARCHAR(10), sal INT);`\n    2.  `CREATE TABLE person1 (name VARCHAR(10), sal INT);`\n    3.  `INSERT INTO person VALUES (1, 'P', 200), (1, 'a', 2000);`\n    4.  `INSERT INTO person1 VALUES ('P', 0);`\n    5.  **Create the trigger:**\n        ```sql\n        DELIMITER //\n        CREATE TRIGGER to_after_update AFTER UPDATE ON person\n        FOR EACH ROW\n        BEGIN\n            INSERT INTO person1 SET name = NEW.name, sal = NEW.sal;\n            DELETE FROM person1 WHERE sal < 5000;\n            UPDATE person1 SET sal = 9000 WHERE sal = OLD.sal;\n        END;\n        //\n        DELIMITER ;\n        ```\n    6.  `INSERT INTO person VALUES (2, 'a', 5500);` *(This is an insert, not an update, so trigger doesn't fire yet)*\n    7.  `UPDATE person SET name = 't' WHERE name = 'a';` *(This is the UPDATE that fires the trigger)*\n        **Output (`person` after update):**\n        | id | name | sal  |\n        |----|------|------|\n        | 1  | P    | 200  |\n        | 1  | t    | 2000 |\n        | 2  | a    | 5500 |\n        **Output (`person1` after trigger):**\n        | name | sal   |\n        |------|-------|\n        | P    | 9000  |\n        | t    | 9000  |\n        | a    | 5500  |\n\n**Example 4: `BEFORE INSERT` Trigger for Data Validation/Modification**\n\n*   **Scenario:** Before inserting into a `sample` table, check the `age` and automatically set the `adult` status.\n*   **Table:** `sample` (id, age, adult)\n*   **Steps:**\n    1.  `CREATE TABLE sample (id INT, age INT, adult INT);`\n    2.  **Create the trigger:**\n        ```sql\n        DELIMITER //\n        CREATE TRIGGER adult_check BEFORE INSERT ON sample\n        FOR EACH ROW\n        BEGIN\n            IF NEW.age > 18 THEN\n                SET NEW.adult = 1; -- Set adult to 1 if age is over 18\n            ELSE\n                SET NEW.adult = 0; -- Set adult to 0 otherwise\n            END IF;\n        END;\n        //\n        DELIMITER ;\n        ```\n    3.  `INSERT INTO sample (id, age) VALUES (2, 22);`\n    4.  `INSERT INTO sample (id, age) VALUES (1, 12);`\n    5.  `SELECT * FROM sample;`\n        **Output:**\n        | id | age | adult |\n        |----|-----|-------|\n        | 2  | 22  | 1     |\n        | 1  | 12  | 0     |\n        *(The `BEFORE INSERT` trigger correctly sets the `adult` column based on the `age` provided.)*",
    "Module 5: Transactions & Concurrency Control": "# 5. Relational DB Design\n\n## Redundancy\n\n**Redundancy** means the duplication of data. It occurs when the same piece of information is stored multiple times in tables unnecessarily within a database.\n\n**Example: Student Table**\n\nConsider the following `Student` table:\n\n| Roll No | Name | Age | BranchID | BName | HoDCnt | HOD Name |\n| :------ | :--- | :-- | :------- | :---- | :----- | :------- |\n| 1       | A    | 18  | 101      | Comp  | 123    | ABC      |\n| 2       | B    | 20  | 102      | I.T.  | 456    | XYZ      |\n| 3       | C    | 19  | 103      | EXTC  | 8910   | PQR      |\n| 4       | D    | 20  | 104      | Mech  | 569    | LMN      |\n| 6       | E    | 21  | 104      | Mech  | 569    | LMN      |\n| 7       | F    | 19  | 105      | M.E.I.T. | 223    | EFG      |\n| 8       | G    | 18  | 105      | M.E.I.T. | 223    | EFG      |\n| 9       | H    | 19  | 101      | Comp  | 123    | ABC      |\n\nIn this example, all information related to students of the college is collected in one table. This table is not normalized, leading to redundancy. For instance, `BranchID`, `BName`, `HoDCnt`, and `HOD Name` are repeated whenever multiple students belong to the same branch.\n\n## Anomalies in DBMS\n\n**Anomalies** are problems that occur when a database is not normalized. There are three types of anomalies:\n\n1.  ### Insertion Anomaly\n    An **insertion anomaly** occurs when certain data cannot be inserted into the database without the presence of other data.\n    *   **Example:** If a new student is admitted to the college but has not yet been assigned to any branch, we would not be able to insert their data into the `Student` table if the `BranchID` field does not allow null values. This is because the branch-related information (like `BName`, `HoDCnt`, `HOD Name`) must also be provided, even though it's not directly related to the student's initial enrollment.\n\n2.  ### Deletion Anomaly\n    A **deletion anomaly** occurs when the deletion of some unwanted data unintentionally causes the deletion of some wanted data.\n    *   **Example:** Suppose the college decides to close the M.E.I.T. department (BranchID 105). If we delete all rows having `BranchID` 105, we would also lose all information about the students who were assigned to that department. This is problematic if we still need to retain student records even after a department closes.\n\n3.  ### Updation / Modification Anomaly\n    An **updation/modification anomaly** occurs when a single piece of data needs to be updated, and these changes must be reflected in all places where that data is stored. Failure to update all occurrences can lead to inconsistencies.\n    *   **Example:** If the HOD (Head of Department) for the Mech department (BranchID 104) changes from 'LMN' to 'UVW', we would need to update the `HOD Name` for all students in the Mech department. If we forget to change the name in any of the relevant rows, it could lead to conflicts, showing two different HODs for the same department.\n\n## Normalization\n\n**Normalization** is the process of organizing data in a database to avoid data redundancy and the three types of anomalies discussed above.\n\n## Functional Dependency (FD)\n\nA **Functional Dependency** in a database serves as a constraint between two sets of attributes.\n\nIn a particular relation `R`, attribute `Y` is **functionally dependent** on attribute `X` (denoted `X → Y`) if every value of `X` in the given relation has exactly one value of `Y`.\n\nThe functional dependency is represented as:\n\n`X → Y`\n\nWhere:\n*   `X` is known as the **determinant**.\n*   `Y` is **functionally dependent** on `X`.\n\nThis implies that `X` functionally determines `Y`.\n\n### Functional Dependency Chart\n\nA **Functional Dependency Chart** is the graphical representation of functional dependencies among attributes in any relation.\n\n**Steps to create an FD Chart:**\n1.  Identify the **primary key** attributes.\n2.  Draw a rectangle and write all primary key attributes inside it.\n3.  Write all **non-primary key** attributes outside the rectangle.\n4.  Use arrows to show functional dependencies among attributes.\n\n**Example: Employee Relation**\n\nConsider the `Employee` relation:\n\n| Eid | name  | Salary |\n| :-- | :---- | :----- |\n| 1   | Aadi  | 16000  |\n| 2   | Manoj | 16000  |\n| 3   | Sandi | 10000  |\n| 4   | Vikas | 10000  |\n| 5   | Manoj | 16000  |\n\nIn this relation, `Eid` is the **Primary Key (PK)**. If you want to know the name and salary of any employee, you can easily find this information if you have their `Eid`. Therefore, `name` and `salary` attributes depend upon the `Eid` attribute.\n\nHere, `Eid` is `X`, and `(name, salary)` is `Y`. The determinant is `Eid`.\nIf `X` (Eid) has value 5, then `Y` (name, salary) has (Manoj, 16000), which gives a unique value. Therefore, `X` uniquely determines `Y`.\nThus, we have the functional dependency: `Eid → (name, salary)`.\n\nOn the other hand, `salary → name` cannot be an FD. For a salary of 16000, we have two different names (Aadi, Manoj).\n\n**Functional Dependency Chart for Employee:**\n\n```mermaid\ngraph TD\n    A[Eid] --> B(name)\n    A --> C(salary)\n```\n\n## Types of Functional Dependency\n\n### 1) Partial Dependency and Fully Functional Dependency\n\nThese concepts apply when there are **multiple attributes** in the Primary Key.\n\n*   **Partial Dependency:**\n    Suppose the Primary Key has more than one attribute. Let `A` be a non-prime key attribute. If `A` is not dependent upon *all* primary key attributes, then a **partial dependency** exists.\n\n*   **Fully Functional Dependency:**\n    Let `A` be a non-prime key attribute. If the value of `A` is dependent upon *all* primary key attributes, then `A` is said to be **fully functionally dependent**.\n\n**Example: Student Relation (name, course as PK)**\n\nConsider a `Student` relation having `(name, course)` as prime key attributes and `(grade, fee, address)` as non-prime key attributes:\n\n| name    | address  | course | fee  | grade |\n| :------ | :------- | :----- | :--- | :---- |\n| Andheri | Parel    | DBMJ   | 800  | A     |\n| Parel   | Parel    | DBMJ   | 800  | B     |\n| Lader   | Thane    | DBMJ   | 800  | A     |\n| Thane   | Andheri  | IT     | 500  | B     |\n| Virar   | IT       | 500  | A     |\n\n**Functional Dependencies:**\n*   `name → address`: `address` is partially dependent on `name` (a part of the composite primary key `(name, course)`).\n*   `course → fee`: `fee` is partially dependent on `course` (a part of the composite primary key `(name, course)`).\n*   `(name, course) → grade`: `grade` is fully functionally dependent on `(name, course)`.\n\n**Functional Dependency Chart for Student (Page 6 view):**\n\n```mermaid\ngraph TD\n    subgraph Primary Keys\n        PK_Name[name]\n        PK_Course[course]\n    end\n\n    PK_Name --> Address(address)\n    PK_Course --> Fee(fee)\n    PK_Course --> Sem(sem)\n\n    PK_Name -- part of composite key --> Grade\n    PK_Course -- part of composite key --> Grade\n    subgraph Composite Key\n        Grade((grade))\n    end\n```\n*Note: The chart above reconstructs the partial dependencies explicitly shown in the notes.*\n\nAs shown in the figure, `address` and `fee` are partially dependent because you can find the `address` of the student by their `name`, and `fee` of any `course` by the `course` name. `grade` is fully functionally dependent because you can find the `grade` of any student in a particular `course` if you know both the `name` of the student and the `course` name.\n\n### 2) Transitive and Non-Transitive Dependency\n\n*   **Transitive Dependency:**\n    It occurs due to a dependency between non-prime key attributes. Suppose in a relation `R`:\n    *   `X → Y` (`Y` depends upon `X`)\n    *   `Y → Z` (`Z` depends upon `Y`)\n    Then `X → Z` (`Z` depends upon `X`).\n    Therefore, `Z` is said to be transitively dependent upon `X`.\n\n*   **Non-Transitive Dependency:**\n    Any functional dependency which is not transitive is known as a **non-transitive dependency**.\n\n**Example: Student Relation (roll_no as PK)**\n\nConsider a `Student` relation having `Roll_no` as the prime key attribute and `(name, semester, hostel)` as non-prime key attributes.\n\n**Functional Dependency Chart for Student (Page 7 view):**\n\n```mermaid\ngraph TD\n    A[Roll no] --> B(name)\n    A --> C(semester)\n    C --> D(hostel)\n```\n\nHere:\n*   `hostel` is transitively dependent upon `Roll_no` because `Roll_no → semester` and `semester → hostel`. So, `Roll_no → hostel`.\n*   The `semester` of any student can be found by their `Roll_no`.\n*   The `hostel` can be found by the `semester` (for each semester, there is a different hostel).\n*   `name` is non-transitively dependent upon `Roll_no` (`Roll_no → name`).\n\n### 3) Single-Valued and Multivalued Dependency (MVD)\n\n*   **Single-Valued Dependency:**\n    In any relation `R`, if for a particular value of `X`, `Y` has a single value, then it is known as a **single-valued dependency** (`X → Y`).\n\n*   **Multivalued Dependency (MVD):**\n    In any relation `R`, if for a particular value of `X`, `Y` has more than one value, then it is known as a **multivalued dependency** (denoted `X →→ Y`).\n\n**Rules for MVD:**\n1.  A table should have at least 3 columns for it to have a multivalued dependency.\n2.  For a relation `R(A, B, C)`, if there is a multivalued dependency between `A →→ B` and `A →→ C`, then `B` and `C` should be independent of each other.\n\n**Example: ID, Teacher, Class, Days Relation**\n\nConsider the relation shown below:\n\n| ID | Teacher | Class | Days |\n| :-- | :------ | :---- | :--- |\n| 11 | Sam     | Comp  | 3    |\n| 12 | John    | Comp  | 6    |\n| 11 | Sam     | EXTC  | 3    |\n| 12 | John    | Mech  | 5    |\n| 13 | Mick    | Mech  | 2    |\n\n**Functional Dependencies:**\n*   `ID → Teacher`: This is a single-valued dependency because each `ID` corresponds to a unique `Teacher`.\n\n**Multivalued Dependencies:**\n*   `Teacher →→ Class`: A `Teacher` can teach more than one `Class`.\n*   `Class →→ Days`: A `Class` can be held on more than one `Day`.\n\n**Functional Dependency Chart for ID, Teacher, Class, Days:**\n\n```mermaid\ngraph TD\n    ID -- Single-valued --> Teacher\n    Teacher -- Multi-valued --> Class\n    Class -- Multi-valued --> Days\n```\n\n## Normalization (Continued)\n\n### Prime Attribute vs. Non-Prime Attribute\n\n*   **Prime Attribute:** An attribute which is a part of the **candidate key** is known as a prime attribute.\n*   **Non-Prime Attribute:** An attribute which is not a part of the prime key is said to be a non-prime attribute.\n\n### 1) First Normal Form (1NF)\n\nAs per the rule of the First Normal Form, an attribute (column) of a table cannot hold multiple values. It should hold only **atomic (single)** values.\n\n### 2) Second Normal Form (2NF)\n\nA relation is in **2NF** if it is in **1NF** and all non-primary key attributes must be **fully functionally dependent** upon the primary key attribute.\nA relation is in 2NF if and only if it has no partial dependency.\n\n### 3) Third Normal Form (3NF)\n\nA relation must be in **2NF**, and there is no **transitive dependency** for non-prime key attributes.\n\n### 4) Boyce-Codd Normal Form (BCNF)\n\n**BCNF** is a strict form of 3NF. A relation is in BCNF if and only if all determinants are **candidate keys**.\n\n### 5) Fourth Normal Form (4NF)\n\nA relation will be in **4NF** if it is in **BCNF** and has no **multivalued dependency**.\n\n---\n\n### Normalization Example: Student Relation\n\nConsider the following `Student` relation:\n\n| name | address | course | grade | fee | sem | hostel | duration |\n| :--- | :------ | :----- | :---- | :-- | :-- | :----- | :------- |\n| xyz  | dadar   | C1     | A+    | 9000 | UI  | H7     | 10       |\n| xyz  | dadar   | C3     | B1    | 8000 | III | H3     | 11       |\n| PQR  | Parel   | C2     | B1    | 5000 | V   | H4     | 06       |\n| ABC  | mumbai  | C2     | B1    | 5000 | V   | H5     | 09       |\n| ABC  | Andheri | C1     | A1    | 9000 | UI  | H3     | 09       |\n\n#### 1) 1NF Conversion\n\nIn 1NF, each cell must contain an atomic value. If multiple values exist (e.g., a student taking multiple courses), they must be represented by separate rows. The candidate key here is `{name, course}`.\n\n**Table: Student (in 1NF)**\n\n| name | address | course | grade | fee  | sem | hostel | duration |\n| :--- | :------ | :----- | :---- | :--- | :-- | :----- | :------- |\n| xyz  | dadar   | C1     | A+    | 9000 | UI  | H7     | 10       |\n| xyz  | dadar   | C3     | B1    | 8000 | III | H3     | 11       |\n| PQR  | Parel   | C2     | B1    | 5000 | V   | H4     | 06       |\n| ABC  | Andheri | C2     | B1    | 5000 | V   | H5     | 09       |\n| ABC  | Andheri | C1     | A1    | 9000 | UI  | H3     | 09       |\n\n#### 2) 2NF Conversion\n\nA relation is in 2NF if it is in 1NF and there are no partial dependencies.\n\n**Generic 2NF Diagram:**\n\n```mermaid\ngraph TD\n    subgraph Primary Key\n        A[A]\n        B[B]\n    end\n\n    A --- C[C]\n    B --- C\n    A --- D[D]\n    D -.-> A\n    D -.-> B\n```\n_Here, `(A, B)` is the key. `C` is fully functionally dependent on `(A, B)`. `D` is partially dependent on `A` (or `B`). To achieve 2NF, `D` (and `A`) would be in one table, and `C` (and `A, B`) in another._\n\nFrom our `Student` 1NF table, we identified the following FDs:\n*   `{name} → address` (Partial dependency, as `name` is part of PK `{name, course}`)\n*   `{course} → {fee, sem}` (Partial dependency, as `course` is part of PK `{name, course}`)\n*   `{name, course} → {grade, duration, hostel}` (Fully functional dependency)\n\nTo eliminate partial dependencies, we decompose the table into multiple tables:\n\n**Table 1 (R1): `Student_Address`** (Candidate Key: `{name}`)\n\n| name | address |\n| :--- | :------ |\n| XYZ  | dadar   |\n| PQR  | parel   |\n| ABC  | andheri |\n\n**Table 2 (R2): `Course_Info`** (Candidate Key: `{course}`)\n\n| course | fee  | sem |\n| :----- | :--- | :-- |\n| C1     | 9000 | UI  |\n| C2     | 5000 | V   |\n| C3     | 8000 | III |\n\n**Table 3 (R3): `Student_Course_Details`** (Candidate Key: `{name, course}`)\n\n| name | course | grade | duration | hostel |\n| :--- | :----- | :---- | :------- | :----- |\n| xyz  | C1     | A+    | 10       | H7     |\n| xyz  | C3     | B1    | 11       | H3     |\n| PQR  | C2     | B1    | 06       | H4     |\n| ABC  | C2     | B1    | 09       | H5     |\n| ABC  | C1     | A1    | 09       | H3     |\n\n#### 3) 3NF Conversion\n\nA relation is in 3NF if it is in 2NF and there are no transitive dependencies.\n\n**Generic 3NF Diagram:**\n\n```mermaid\ngraph TD\n    X[X*] --> Y(Y)\n    Y --> Z(Z)\n```\n_Here, `X*` is the primary key. `Z` is transitively dependent on `X` through `Y`._\n\nFrom our 2NF tables:\n*   Table 1 (`Student_Address`) and Table 3 (`Student_Course_Details`) already satisfy 3NF (no transitive dependencies visible).\n*   In Table 2 (`Course_Info`): `{course} → {sem}` and `{sem} → {fee}`. This implies `{course} → {fee}` is a transitive dependency.\n\nTo eliminate transitive dependency in `Course_Info`, we decompose it further:\n\n**Table 4 (R4): `Course_Semester`** (Candidate Key: `{course}`)\n\n| Course | sem |\n| :----- | :-- |\n| C1     | UI  |\n| C2     | V   |\n| C3     | III |\n\n**Table 5 (R5): `Semester_Fee`** (Candidate Key: `{sem}`)\n\n| sem | fee  |\n| :-- | :--- |\n| UI  | 9000 |\n| V   | 5000 |\n| III | 8000 |\n\nAfter 3NF, the relations are: R1, R3, R4, R5.\n\n#### 4) BCNF Check\n\nFor BCNF, every determinant must be a candidate key.\n*   In R1 (`name → address`), `name` is PK. BCNF satisfied.\n*   In R4 (`course → sem`), `course` is PK. BCNF satisfied.\n*   In R5 (`sem → fee`), `sem` is PK. BCNF satisfied.\n*   We need to check R3 (`name, course, grade, duration, hostel`) for BCNF. Assuming `(name, course)` is the only candidate key and no other FDs exist where a non-key attribute determines another attribute or a part of the key.\n\n#### 5) 4NF Conversion\n\nA relation is in 4NF if it is in BCNF and has no multivalued dependency.\n\nLet's assume the following multivalued dependencies exist in R3 based on the notes:\n*   `{name} →→ {course}`\n*   `{name} →→ {duration}` (This would imply `name` multi-determines `duration`, and `name` multi-determines `course`)\n\nBased on the rules for MVD decomposition and the provided final tables, the decomposition continues:\n\nSeparate tables created during 4NF conversion:\n\n**Table 6 (R6): `Hostel_Grade`** (Candidate Key: `{hostel}`)\n\n| hostel | grade |\n| :----- | :---- |\n| H7     | A+    |\n| H3     | B1    |\n| H4     | B1    |\n| H5     | B1    |\n| H3     | A1    |\n\n**Table 7 (R7, shown as `CK {name, course}`): `Name_Course`**\n_Note: The note shows a `duration` column here, which conflicts with `CK {name, course}`. Assuming this is an intermediate representation or intended as a combined table for `name`, `course`, and `duration` for MVD analysis._\n\n| name | course | duration |\n| :--- | :----- | :------- |\n| xyz  | C1     | 10       |\n| xyz  | C3     | 11       |\n| PQR  | C2     | 06       |\n| ABC  | C2     | 09       |\n| ABC  | C1     | 09       |\n\n**Table 8 (R8): `Name_Course`** (Candidate Key: `{name, course}`)\n\n| name | Course |\n| :--- | :----- |\n| xyz  | C1     |\n| xyz  | C3     |\n| PQR  | C2     |\n| ABC  | C2     |\n| ABC  | C1     |\n\n**Table 9 (R9): `Course_Duration`** (Candidate Key: `{course, duration}`)\n\n| Course | duration |\n| :----- | :------- |\n| C1     | 10       |\n| C3     | 11       |\n| C2     | 06       |\n| C2     | 09       |\n| C1     | 09       |\n\nThe final tables are: `{R1, R4, R5, R6, R8, R9}`.\nThese tables are:\n1.  `Student_Address (name, address)`\n2.  `Course_Semester (Course, sem)`\n3.  `Semester_Fee (sem, fee)`\n4.  `Hostel_Grade (hostel, grade)`\n5.  `Name_Course (name, Course)`\n6.  `Course_Duration (Course, duration)`",
    "Module 6: Log-Based Recovery & Database Security": "Welcome to this comprehensive study guide on Transaction Management and Concurrency, a critical module in Database Management Systems. This guide covers fundamental concepts, properties, concurrency control mechanisms, and recovery techniques essential for understanding how databases maintain integrity and consistency in multi-user environments.\n\n---\n\n# Module 6: Transaction Management and Concurrency\n\n## Transaction Concept\n\nA **transaction** is a fundamental unit of work in a database system. It represents a single logical operation or a set of operations that are performed as a single, atomic unit. Transactions are typically initiated by programs written in high-level languages, and they are responsible for performing manipulation language operations.\n\n### Operations of Transactions\n\nThe primary operations involved in a transaction include:\n\n| Operation | Description                                                                                                   |\n| :-------- | :------------------------------------------------------------------------------------------------------------ |\n| 1) Read(X) | Read operation is used to read the value of 'X' from the DB and store it in a buffer in main memory.        |\n| 2) Modify(X) | It changes the value of item 'X' in the main memory.                                                         |\n| 3) Write(X) | It is used to write the modified value from main memory to storage.                                           |\n\n## Transaction States\n\nA transaction in a database can exist in one of several states during its lifecycle. The following diagram illustrates these states and the transitions between them:\n\n```mermaid\ngraph TD\n    Begin --> Active;\n    Active -- last statement of transaction --> PartiallyCommitted;\n    PartiallyCommitted -- system checks success --> Committed;\n    PartiallyCommitted -- normal execution no longer possible or system checks fail --> Failed;\n    Active -- normal execution no longer possible or system checks fail --> Failed;\n    Failed -- restart or kill --> Aborted;\n    Committed --> end;\n    Aborted --> end;\n```\n\n### Explanation of Transaction States\n\n*   **Begin Transaction**: A marker that specifies the start of transaction execution.\n*   **End Transaction**: A marker that specifies the end of a transaction.\n*   **Commit**: A signal to specify that the transaction has been successfully completed and its changes will not be undone.\n*   **Rollback**: A signal to specify that the transaction has been unsuccessful, and all temporary changes made to the DB should be undone. A committed transaction cannot be undone.\n\n1.  **Active**: In this state, the transaction is being executed. This is the initial state of every transaction.\n2.  **Partially Committed**: The transaction enters this state after the last statement of the transaction has been executed.\n3.  **Committed**: The transaction enters this state after successful completion of its operations and when system checks have issued a commit signal.\n4.  **Failed**: The transaction moves from a partially committed or active state to a failed state when it is discovered that normal execution can no longer proceed, or system checks fail.\n5.  **Aborted**: This is the state after a transaction has been rolled back due to failure, and the DB has been restored to its state before the transaction began.\n    *   If any checks fail and the transaction reaches a failed state, the recovery manager rolls back all its write operations on the DB to bring it back to its original state prior to execution.\n    *   Transactions in this state are called aborted.\n    *   The DB recovery module can select one of two operations after a transaction aborts:\n        *   **a) Restart**: It can restart the transaction only if the transaction was aborted due to hardware or software errors that were not caused by the internal logic of the transaction. A restarted transaction is considered a new transaction.\n        *   **b) Kill the transaction**: The transaction was killed because the input data was bad or the desired data was not found in the DB.\n\n## Transaction Properties (ACID Properties)\n\nTo ensure consistency and integrity of data, it is very important for DB systems to maintain the following properties:\n\n1.  **Atomicity**:\n    *   Atomicity ensures that a transaction is treated as a single, indivisible unit of work. Either all of its operations are completed successfully, or none of them are. If a transaction fails at any point, the database must be returned to its state before the transaction began.\n    *   **Example**: Consider a transaction `T1` that transfers Rs. 50 from account A to account B.\n\n        ```\n        T1: Read(A);\n            A := A - 50;\n            Write(A);\n            Read(B);\n            B := B + 50;\n            Write(B);\n        ```\n        Suppose that just before the execution of `T1`, the values of accounts A and B are Rs. 1000 and Rs. 2000 respectively. Now suppose that, during the execution of `T1`, a failure occurs that prevents `T1` from completing its execution successfully. Specifically, a failure happened after the `Write(A)` operation but before the `Write(B)` operation.\n        In this case, the values of accounts A and B reflected in the DB are Rs. 950 and Rs. 2000. The system \"destroyed\" Rs. 50 as a result of this failure. The sum A+B is no longer preserved. We term such a state an **inconsistent state**.\n        This state is, however, replaced by the **consistent state**, where the value of account A is Rs. 950 and the value of account B is Rs. 2000.\n        *   **Basic idea behind ensuring atomicity**: The DB system keeps track of the old values of any data on which a transaction performs a write. If the transaction does not complete its execution, the DB system restores the old values to make it appear as though the transaction never executed.\n        *   **Ensuring Atomicity** is the responsibility of the DB system itself; specifically, it is handled by a component called the **transaction management component**.\n\n2.  **Consistency**:\n    *   Consistency ensures that a transaction brings the database from one valid state to another. It preserves database invariants (e.g., integrity constraints). If the database was consistent before a transaction, it must be consistent after the transaction completes.\n    *   **Example**: If the sum of A and B is invariant, then after a transfer from A to B, A+B must remain unchanged. If money could be created or destroyed by the transaction, it would be inconsistent. It can be easily verified that if the DB is consistent before an execution of the transaction, the DB remains consistent after the execution of the transaction.\n    *   **Ensuring consistency for an individual transaction** is the responsibility of the application programmer who codes the transaction.\n\n3.  **Isolation**:\n    *   Isolation ensures that the concurrent execution of multiple transactions appears as if each transaction were executing in isolation. The intermediate states of one transaction are not visible to other concurrent transactions.\n    *   **Example**: \"Even though multiple transactions may execute concurrently, the system guarantees that, for every pair of transactions `Ti` and `Tj`, it appears to `Ti` that either `Tj` finished execution before `Ti` started or `Tj` started execution after `Ti` finished. Thus, each transaction is unaware of other transactions executing concurrently in the system.\"\n        The DB is temporarily inconsistent while a transaction to transfer funds from A to B is executing (e.g., after A is deducted but before B is increased). If a second concurrently running transaction reads A and B at this intermediate point and computes A+B, it will observe an inconsistent value. Furthermore, if this second transaction then performs updates on A and B based on the inconsistent values that it read, the DB may be left in an inconsistent state, even after both transactions have completed.\n    *   A way to avoid the problem of concurrently executing transactions is to execute transactions serially (i.e., one after the other).\n    *   **Ensuring the isolation property** is the responsibility of a component of the DB system called the **concurrency control component**.\n\n4.  **Durability**:\n    *   Durability ensures that once a transaction has been committed, its changes are permanent and will survive any subsequent system failures (e.g., power outages, crashes).\n    *   **Ensuring durability** is the responsibility of a software component of the DB system called the **recovery-management component**.\n\n## Concurrent Execution\n\nThe transaction processing system allows multiple transactions to run concurrently. This can cause several complications with the consistency of the data.\n\nTo avoid these complications, it is often better to run transactions serially. However, there are reasons for allowing concurrent transactions:\n\n1.  **Improved Throughput and Resource Utilization**:\n    *   A transaction consists of many steps, some involving I/O activities (e.g., reading from disk) and some CPU activities (e.g., computation). The CPU and disk in a computer system can operate in parallel. This parallelism can be used to run multiple transactions simultaneously.\n    *   When one transaction is performing a read/write operation on disk, another transaction can be running in the CPU. This increases the processor and disk utilization.\n\n2.  **Reduces Waiting Time of Transactions**:\n    *   There may be some short and some long transactions running on a system. If transactions are running serially, the other transactions have to wait for the previous one to finish. If transactions are accessing different parts of the DB, it is better to run them concurrently. But if they are accessing the same DB, it can be difficult.\n    *   When transactions run concurrently, the interaction among transactions must be maintained to control data consistency.\n\n### Serial Schedule\n\nA **serial schedule** consists of a set of transactions that are executing serially (one after another) or concurrently. A serial schedule represents the chronological order in which the instructions are executed in the system (i.e., execution sequences of transactions).\n\n**Example**: Consider transactions `T1` and `T2` that transfer funds from account A to account B.\nSuppose the current value of A is 500 and the current value of B is 1000.\n\n**T1 operations**:\n*   `Read(A)`\n*   `A := A - 50`\n*   `Write(A)`\n*   `Read(B)`\n*   `B := B + 50`\n*   `Write(B)`\n\n**T2 operations**:\n*   `Read(A)`\n*   `A := A - 100`\n*   `Write(A)`\n*   `Read(B)`\n*   `B := B + 100`\n*   `Write(B)`\n\n#### Serial Schedule 1 (T1 followed by T2)\n\n| T1              | T2              | Operations                   | Values             |\n| :-------------- | :-------------- | :--------------------------- | :----------------- |\n| `Read(A)`       |                 | `A=500`                      | `A=500`            |\n| `A:=A-50`       |                 | `A=500-50=450`               | `A=450`            |\n| `Write(A)`      |                 |                              | `A=450`            |\n| `Read(B)`       |                 | `B=1000`                     | `B=1000`           |\n| `B:=B+50`       |                 | `B=1000+50=1050`             | `B=1050`           |\n| `Write(B)`      |                 |                              | `B=1050`           |\n|                 | `Read(A)`       | `A=450`                      | `A=450`            |\n|                 | `A:=A-100`      | `A=450-100=350`              | `A=350`            |\n|                 | `Write(A)`      |                              | `A=350`            |\n|                 | `Read(B)`       | `B=1050`                     | `B=1050`           |\n|                 | `B:=B+100`      | `B=1050+100=1150`            | `B=1150`           |\n|                 | `Write(B)`      |                              | `B=1150`           |\n| **Final State** | **A+B = 350 + 1150 = 1500** | (A+B=1500 is preserved) | `A=350, B=1150`    |\n\n#### Serial Schedule 2 (T2 followed by T1)\n\n| T1              | T2              | Operations                   | Values             |\n| :-------------- | :-------------- | :--------------------------- | :----------------- |\n|                 | `Read(A)`       | `A=500`                      | `A=500`            |\n|                 | `A:=A-100`      | `A=500-100=400`              | `A=400`            |\n|                 | `Write(A)`      |                              | `A=400`            |\n|                 | `Read(B)`       | `B=1000`                     | `B=1000`           |\n|                 | `B:=B+100`      | `B=1000+100=1100`            | `B=1100`           |\n|                 | `Write(B)`      |                              | `B=1100`           |\n| `Read(A)`       |                 | `A=400`                      | `A=400`            |\n| `A:=A-50`       |                 | `A=400-50=350`               | `A=350`            |\n| `Write(A)`      |                 |                              | `A=350`            |\n| `Read(B)`       |                 | `B=1100`                     | `B=1100`           |\n| `B:=B+50`       |                 | `B=1100+50=1150`             | `B=1150`           |\n| `Write(B)`      |                 |                              | `B=1150`           |\n| **Final State** | **A+B = 350 + 1150 = 1500** | (A+B=1500 is preserved) | `A=350, B=1150`    |\n\n**Conclusion**: After changing the sequence, the addition of A+B is preserved in both cases. So, serial schedule 1 is equal to serial schedule 2 in terms of final consistent state.\n\n## Concurrent Schedule\n\nIn a multi-transaction environment, serial schedules are considered as benchmarks. The execution sequence of instructions within a transaction cannot be changed, but two transactions can have their instructions executed in a random fashion. This execution causes no harm if two transactions are mutually independent and working on different segments of data. However, if there are two transactions working on the same data, then the results may vary. This ever-varying result may bring the DB to an inconsistent state.\n\nTo resolve this problem, we allow parallel execution of a transaction schedule. When multiple transactions are executing concurrently in the system, the schedule is said to be a **concurrent schedule**. If two transactions are executing concurrently, the operating system will perform a context switch, execute the second transaction, and then switch back to the first transaction.\n\n### Concurrent Schedule 1 (Equivalent to Serial)\n\n| T1        | T2        |\n| :-------- | :-------- |\n| `Read(A)` |           |\n| `A:=A-50` |           |\n| `Write(A)`|           |\n|           | `Read(A)` |\n|           | `A:=A-100`|\n|           | `Write(A)`|\n| `Read(B)` |           |\n| `B:=B+50` |           |\n| `Write(B)`|           |\n|           | `Read(B)` |\n|           | `B:=B+100`|\n|           | `Write(B)`|\n\n**Output after Concurrent Schedule 1**:\n*   A = 350\n*   B = 1150\n*   A+B = 1500 (preserved)\n\nThe output of the above schedule is equivalent to a serial schedule (as A+B=1500 is preserved) that leads the DB to a consistent state.\n\n### Concurrent Schedule 2 (Not Equivalent to Serial)\n\n| T1        | T2        |\n| :-------- | :-------- |\n| `Read(A)` |           |\n| `A:=A-50` |           |\n|           | `Read(A)` |\n|           | `A:=A-100`|\n|           | `Write(A)`|\n| `Write(A)`|           |\n| `Read(B)` |           |\n| `B:=B+50` |           |\n| `Write(B)`|           |\n|           | `Read(B)` |\n|           | `B:=B+100`|\n|           | `Write(B)`|\n\n**Output after Concurrent Schedule 2**:\n*   A = 350 (from T1's A=500-50, but T2 overwrites A before T1 writes it)\n*   B = 1150\n*   A+B = 1500 (but the order of operations leads to a different intermediate state, and potentially incorrect final state if the actual A from T1's write was lost)\n\nIf we try to run the above schedule, consistency is not provided as (A+B=1500) is not preserved in this case due to lost updates or incorrect intermediate reads.\n\n**Note**: If we have a serial schedule, consistency is always provided. But if we have a concurrent schedule, consistency may or may not be provided.\n\n### Serializability in DBMS\n\nWhen multiple transactions run concurrently, it may give rise to inconsistency of the DB. **Serializability** is a concept that helps to identify which non-serial schedules are correct, meaning they will maintain the consistency of the DB.\n\n### Serializable Schedule\n\nIf a given non-serial schedule of 'n' transactions is found to be equivalent to some serial schedule of 'n' transactions, then it is called a **serializable schedule**.\n\n### Difference between Serial Schedule and Serializable Schedule\n\n| Feature              | Serial Schedule                                                                         | Serializable Schedule                                                              |\n| :------------------- | :-------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------- |\n| **Concurrency**      | Only one transaction is allowed to execute at a time (no concurrency is allowed).       | Multiple transactions can execute simultaneously (concurrency is allowed).         |\n| **Effectiveness**    | Always consistent, but less efficient in terms of resource utilization and throughput.  | Generally better than serial schedules as they improve resource utilization and CPU throughput, while maintaining consistency. |\n| **Equivalence**      | Each transaction executes completely before the next one starts.                        | A non-serial schedule whose outcome is equivalent to the outcome of some serial schedule. |\n\n### Types of Serializability\n\nThere are two main types of serializability:\n\n1.  **Conflict Serializability**\n2.  **View Serializability**\n\n---\n\n### 1) Conflict Serializability\n\nA given schedule can be converted into a serial schedule by swapping its non-conflicting operations. If this is possible, it is called a **conflict serializable schedule**.\n\n**Conflicting operations**:\nTwo operations are called as conflicting operations if all the following conditions hold true for them:\n\n1.  Both operations must belong to different transactions.\n2.  Both operations must operate on the same data item.\n3.  At least one of the two operations is a write operation.\n\n**Conflict equivalent**:\nIf we can transform schedule 'S' into schedule 'S'' by a series of swaps of non-conflicting instructions, we say that S and S' are **conflict equivalent**.\n\n**Example**: Consider the following schedule:\n\n| T1    | T2    |\n| :---- | :---- |\n| R1(A) |       |\n| W1(A) |       |\n|       | R2(A) |\n| R1(B) |       |\n| W1(B) |       |\n\nIn this schedule, `W1(A)` and `R2(A)` are called conflicting operations because all the above conditions hold true for them (different transactions, same data item A, one is a write).\n\n#### Concurrent Schedule Conflict Serializable Example\n\nLet's illustrate how swapping non-conflicting operations can lead to a conflict-serializable schedule.\n\n**Initial Schedule 1**:\n```mermaid\ngraph TD\n    subgraph T1\n        op1_t1[R(A)]\n        op2_t1[W(A)]\n        op3_t1[R(B)]\n        op4_t1[W(B)]\n    end\n    subgraph T2\n        op1_t2[R(A)]\n        op2_t2[W(A)]\n        op3_t2[R(B)]\n        op4_t2[W(B)]\n    end\n    op1_t1 --> op2_t1;\n    op2_t1 --> op1_t2;\n    op1_t2 --> op2_t2;\n    op2_t2 --> op3_t1;\n    op3_t1 --> op4_t1;\n    op4_t1 --> op3_t2;\n    op3_t2 --> op4_t2;\n```\n\nHere, `W(A)` of `T1` and `R(A)` of `T2` conflict. `W(A)` of `T2` and `R(B)` of `T1` conflict. `W(B)` of `T1` and `R(B)` of `T2` conflict.\n\nLet's assume a simplified scenario where we can swap non-conflicting operations:\n\n**Scenario for Swapping (Conceptual)**:\nWe have an initial schedule where operations are interleaved. We can move non-conflicting operations past each other to transform it into a serial schedule.\n\n```mermaid\ngraph TD\n    subgraph Original Schedule\n        T1_R_A[R(A) - T1] --> T1_W_A[W(A) - T1]\n        T1_W_A --> T2_R_A[R(A) - T2]\n        T2_R_A --> T2_W_A[W(A) - T2]\n        T2_W_A --> T1_R_B[R(B) - T1]\n        T1_R_B --> T1_W_B[W(B) - T1]\n        T1_W_B --> T2_R_B[R(B) - T2]\n        T2_R_B --> T2_W_B[W(B) - T2]\n    end\n\n    subgraph Swapped Schedule (Illustrative serial form: T1 then T2)\n        T1_R_A_s[R(A) - T1] --> T1_W_A_s[W(A) - T1]\n        T1_W_A_s --> T1_R_B_s[R(B) - T1]\n        T1_R_B_s --> T1_W_B_s[W(B) - T1]\n        T1_W_B_s --> T2_R_A_s[R(A) - T2]\n        T2_R_A_s --> T2_W_A_s[W(A) - T2]\n        T2_W_A_s --> T2_R_B_s[R(B) - T2]\n        T2_R_B_s --> T2_W_B_s[W(B) - T2]\n    end\n```\n\nThe handwritten notes show a visual representation of `T1` and `T2` operations and how they might be reordered. The key idea is that if an interleaved schedule can be transformed into a serial schedule by only swapping non-conflicting operations, it is conflict serializable.\n\n**Conflict Serializable Example from Notes**:\nConsider a schedule with operations for T1 and T2.\n(1) Initial interleaving:\n```\nT1: R(A), W(A)\nT2:        R(A)\nT1: R(B), W(B)\nT2:        R(B), W(B)\n```\nIn this sequence, `W1(A)` and `R2(A)` are conflicting operations.\nSuppose we swap `R2(A)` and `R1(B)`. Since they operate on different data items, they are non-conflicting.\n(2) After swapping `R2(A)` and `R1(B)`:\n```\nT1: R(A), W(A), R(B), W(B)\nT2:        R(A),       R(B), W(B)\n```\nThis is not a clean serial schedule yet, but it's an intermediate step. The diagram from the notes implies a visual representation of this reordering process.\n\nThe actual method to check conflict serializability is using a **precedence graph (or serializability graph)**.\n\n#### How to Check Conflict Serializability using a Precedence Graph\n\n1.  Create a node for each transaction participating in the schedule.\n2.  For every pair of conflicting operations (from different transactions, on the same data item, at least one write operation) where one operation appears before the other in the schedule:\n    *   Draw a directed edge from the transaction that executed the earlier operation to the transaction that executed the later conflicting operation.\n3.  If the precedence graph contains a cycle, the schedule is **not conflict serializable**. If there is no cycle, the schedule **is conflict serializable**.\n\n**Example 1**: Check whether the given schedule 'S' is conflict serializable or not.\n\n| T1    | T2    | T3    | T4    |\n| :---- | :---- | :---- | :---- |\n| R(A)  |       |       |       |\n|       | R(A)  |       |       |\n|       |       | R(A)  |       |\n|       |       |       | R(A)  |\n| W(B)  |       |       |       |\n|       | W(A)  |       |       |\n|       |       | R(B)  |       |\n|       |       |       | W(B)  |\n\n**Conflicting Operations and Edges**:\n*   `R1(A)` vs `W2(A)` (no `W2(A)` in schedule)\n*   `W1(B)` vs `R3(B)` -> T1 -> T3\n*   `W1(B)` vs `W4(B)` -> T1 -> T4\n*   `R2(A)` vs `W2(A)` (no `W2(A)` in schedule)\n*   `R3(B)` vs `W4(B)` -> T3 -> T4\n\n**Precedence Graph**:\n```mermaid\ngraph LR\n    T1 --> T3;\n    T1 --> T4;\n    T3 --> T4;\n```\n\n**Conclusion**: Since there exists no cycle in the precedence graph, therefore the given schedule 'S' is **conflict serializable**.\n\n**Example 2**: Check conflict serializability for the following schedule:\n\n| T1    | T2    |\n| :---- | :---- |\n| R(A)  |       |\n| A=A-10|       |\n|       | R(A)  |\n|       | Temp = 0.2*A |\n|       | W(A)  |\n| W(A)  |       |\n|       | R(B)  |\n| R(B)  |       |\n| B=B+10|       |\n|       | B=B+Temp |\n| W(B)  |       |\n|       | W(B)  |\n\nWe focus on read and write operations for the precedence graph:\n\n| T1    | T2    |\n| :---- | :---- |\n| R(A)  |       |\n|       | R(A)  |\n|       | W(A)  |\n| W(A)  |       |\n|       | R(B)  |\n| R(B)  |       |\n| W(B)  |       |\n|       | W(B)  |\n\n**Conflicting Operations and Edges**:\n*   `R1(A)` vs `W2(A)` -> T1 -> T2\n*   `W2(A)` vs `W1(A)` -> T2 -> T1\n*   `R2(B)` vs `W1(B)` (no `R2(B)` before `W1(B)`)\n*   `R1(B)` vs `W2(B)` -> T1 -> T2\n*   `W1(B)` vs `W2(B)` -> T1 -> T2\n\n**Precedence Graph**:\nThe critical conflicts are `W2(A)` vs `W1(A)` (T2->T1) and `R1(A)` vs `W2(A)` (T1->T2).\n```mermaid\ngraph LR\n    T1 --> T2;\n    T2 --> T1;\n```\n\n**Conclusion**: Since a cycle (`T1 <--> T2`) is present, the schedule is **not conflict serializable**.\n\n---\n\n### 2) View Serializability\n\nIf a given schedule is found to be **view equivalent** to some serial schedule, then it is called a **view serializable schedule**.\n\n**View equivalent schedule**:\nConsider two schedules `S1` and `S2`, each consisting of two transactions `Ti` and `Tj`. `S1` and `S2` are called view equivalent if the following three conditions hold true for them:\n\n1.  **Initial Read**:\n    *   **Condition**: If transaction `Ti` reads a data item `X` from the DB initially in schedule `S1`, then in schedule `S2` also `Ti` must perform an initial read of the data item `X` from the DB. This is true for all data items.\n    *   **Rule**: Initial read must be the same for all items.\n\n2.  **Updated Read**:\n    *   **Condition**: If a transaction `Ti` reads a data item `X` that has been updated by transaction `Tj` in schedule `S1`, then in schedule `S2` also transaction `Ti` must read the same data item that has been updated by transaction `Tj`.\n    *   **Rule**: Write-read sequence must be the same.\n\n3.  **Final Writes**:\n    *   **Condition**: If data item `X` has been updated at last by transaction `Ti` in schedule `S1`, then in schedule `S2` also the data item `X` must be updated at last by transaction `Ti`. This must be true for all data items.\n    *   **Rule**: Final writes must be the same for all items.\n\n**Example**: Check view serializability for `S1` and `S2` schedules.\n\n| Operation | S1        | S2        |\n| :-------- | :-------- | :-------- |\n| Initial Read | T1 reads X initially | T1 reads X initially |\n| Final Write | T2 writes X last | T2 writes X last |\n| Write-Read | T1 writes Y, T2 reads Y | T1 writes Y, T2 reads Y |\n\nBased on the handwritten notes, an example schedule is given to test these conditions. Let's assume the schedules `S1` and `S2` (not fully detailed in text but implied by the check).\n\n**Example Schedule (from notes, implied for view equivalence check)**:\n\nAssume `S1` and `S2` involves `T1` and `T2` and data items `A`, `B`.\n\n**Schedule S1**:\n| T1    | T2    |\n| :---- | :---- |\n| R(A)  |       |\n| W(A)  |       |\n|       | R(A)  |\n|       | W(A)  |\n| R(B)  |       |\n| W(B)  |       |\n|       | R(B)  |\n|       | W(B)  |\n\n**Schedule S2**:\n| T1    | T2    |\n| :---- | :---- |\n|       | R(A)  |\n|       | W(A)  |\n|       | R(B)  |\n|       | W(B)  |\n| R(A)  |       |\n| W(A)  |       |\n| R(B)  |       |\n| W(B)  |       |\n\nLet's assume the analysis for `S1` and `S2` (from the handwritten notes, which states `S1` is T1 then T2 operations and `S2` is T2 then T1 operations from the image):\n\n*   **Initial Read**:\n    *   For `A`: `R1(A)` from DB in S1. `R2(A)` from DB in S2. `R1(A)` in S2 reads after `W2(A)`.\n    *   This implies there might be an issue depending on which \"initial\" read is considered.\n*   **Updated Read**:\n    *   `R2(A)` in S1 reads `W1(A)`. In S2, `R1(A)` reads `W2(A)`. These are different sequences.\n*   **Final Writes**:\n    *   `W1(B)` is the final write for `B` in S1. `W2(B)` is the final write for `B` in S2. These are different.\n\nThe notes state \"As it satisfies all 3 conditions, therefore both schedules are view equivalent.\" This conclusion might be based on a different example set or a different interpretation of the conditions. Based on the provided example data on page 22, the conditions seem to be different for S1 and S2. Let's re-evaluate based *strictly* on the table on page 22:\n\n**S1**:\n| T1    | T2    |\n| :---- | :---- |\n| R(A)  |       |\n| W(A)  |       |\n| R(B)  |       |\n| W(B)  |       |\n\n**S2**:\n| T1    | T2    |\n| :---- | :---- |\n|       | R(A)  |\n|       | W(A)  |\n|       | R(B)  |\n|       | W(B)  |\n\nThe handwritten notes for this specific S1/S2 don't contain operations for T1 and T2 interacting, only showing operations in T1 for S1, and operations in T2 for S2. Assuming there was an intended full schedule in the notes, it's difficult to reconstruct accurately without it. *However, I will use the final conclusion from the notes that \"it satisfies all 3 conditions therefore both the schedules are view equivalent.\"*\n\n#### How to Check View Serializability\n\nThere are three methods to check view serializability:\n\n1.  **Conflict Serializability Check**:\n    *   If a given schedule is conflict serializable, then it is surely view serializable.\n    *   **Note**: All conflict serializable schedules are view serializable, but not all view serializable schedules are conflict serializable (i.e., view serializable is a superset of conflict serializable).\n\n    **Example 1**: Check view serializability for a schedule `S` using Method 1.\n    (This is the same as the conflict serializability example 1 from above, page 19)\n\n    | T1    | T2    | T3    | T4    |\n    | :---- | :---- | :---- | :---- |\n    | R(A)  |       |       |       |\n    |       | R(A)  |       |       |\n    |       |       | R(A)  |       |\n    |       |       |       | R(A)  |\n    | W(B)  |       |       |       |\n    |       | W(B)  |       |       |\n    |       |       | W(B)  |       |\n    |       |       |       | W(B)  |\n\n    Let's reconstruct based on the precedence graph from page 24. It has `W(B)` for T2, T3, T4.\n\n    | T1    | T2    | T3    | T4    |\n    | :---- | :---- | :---- | :---- |\n    | R(A)  |       |       |       |\n    |       | R(A)  |       |       |\n    |       |       | R(A)  |       |\n    |       |       |       | R(A)  |\n    | W(B)  |       |       |       |\n    |       | W(B)  |       |       |\n    |       |       | W(B)  |       |\n    |       |       |       | W(B)  |\n\n    **Conflicting Operations and Edges (based on page 24 diagram)**:\n    *   `W1(B)` vs `W2(B)` -> T1 -> T2\n    *   `W1(B)` vs `W3(B)` -> T1 -> T3\n    *   `W1(B)` vs `W4(B)` -> T1 -> T4\n    *   `W2(B)` vs `W3(B)` -> T2 -> T3\n    *   `W2(B)` vs `W4(B)` -> T2 -> T4\n    *   `W3(B)` vs `W4(B)` -> T3 -> T4\n\n    **Precedence Graph**:\n    ```mermaid\n    graph LR\n        T1 --> T2;\n        T1 --> T3;\n        T1 --> T4;\n        T2 --> T3;\n        T2 --> T4;\n        T3 --> T4;\n    ```\n    **Conclusion**: As no cycle is present, it is conflict serializable and therefore it is **view serializable** also.\n\n2.  **No Blind Write**:\n    *   A **blind write** is a write operation that is not preceded by a read operation on the same data item within the same transaction.\n    *   **Rule**: If a schedule contains no blind writes, it is a view serializable. If blind-write is present, it may or may not be view serializable.\n\n    **Example 2**: (From page 25) Check view serializability for a schedule using blind write check.\n\n    | T1    | T2    | T3    |\n    | :---- | :---- | :---- |\n    | R(A)  |       |       |\n    | W(A)  |       |       |\n    |       | R(A)  |       |\n    |       | W(A)  |       |\n    |       |       | R(A)  |\n\n    **Precedence Graph (from page 25)**:\n    ```mermaid\n    graph LR\n        T1 --> T2;\n        T2 --> T1;\n    ```\n    **Conclusion**: A cycle exists, therefore not conflict serializable. It `may or may not` be view serializable.\n\n    Checking blind writes: `W1(A)` is not a blind write because `R1(A)` precedes it. `W2(A)` is not a blind write because `R2(A)` precedes it.\n    The notes indicate `W(A)` is a blind write in this context, which implies the full transaction for `T1` or `T2` might not have been shown or that `W(A)` in T2 happened before `R(A)` in T2 or `W(A)` in T1 happened before `R(A)` in T1.\n    Given the structure, if `T1`'s `W(A)` is before `T1`'s `R(A)`, it's a blind write. Same for `T2`.\n    Based on the notes' flow, the presence of a blind write means it *may or may not* be view serializable.\n\n3.  **Drawing Dependency Graph**:\n    *   By using the three view equivalence conditions above, write all dependencies and then draw a graph using those dependencies.\n    *   If there is no cycle, then the schedule is view serializable; otherwise, not.\n\n    **Example**: (From page 26-27, for a schedule T1, T2, T3)\n    Let's assume the schedule `S`:\n    | T1    | T2    | T3    |\n    | :---- | :---- | :---- |\n    | R(A)  |       |       |\n    |       | W(A)  |       |\n    | W(A)  |       |       |\n    |       |       | R(A)  |\n\n    **Dependencies**:\n    i) Transaction T1 firstly reads A, and Trans. T3 firstly updates A. Therefore T1 must be executed before T3.\n       Dependency: `T1 -> T3`\n    ii) Final updation on A is made by Trans. T1. Therefore T1 must execute after all other transactions.\n        Dependency: `(T2, T3) -> T1`\n    iii) There exists no write sequence (which means no `Wi(X)` followed by `Rj(X)` from a different transaction `j` that then writes `X` as `Wk(X)` from another transaction `k`).\n\n    **Precedence Graph (from page 26, for conflict serializability)**:\n    ```mermaid\n    graph LR\n        T1 --> T3;\n        T2 --> T1;\n        T3 --> T1;\n    ```\n    **Conclusion**: A cycle exists (`T1 <-> T3 -> T1` and `T2 -> T1`), so it is **not conflict serializable**. It *may or may not* be view serializable.\n\n    **Dependency Graph (for view serializability, from page 27)**:\n    This graph is based on all dependencies (initial read, updated read, final write).\n    Given the dependencies `T1 -> T3` and `(T2, T3) -> T1`:\n    ```mermaid\n    graph LR\n        T1 --> T3;\n        T2 --> T1;\n        T3 --> T1;\n    ```\n    **Conclusion**: No cycle exists in this graph, therefore it **is view serializable**.\n    *(Note: This contradicts the conflict serializability finding, reinforcing that view serializability is broader. The graph in the notes for view serializability does not show T2->T1 and T3->T1, only T1->T3. This implies that for this specific graph, other dependencies either did not form edges or were ignored for specific reasons for the view serializability check)*. Let's strictly draw the graph as it is on page 27 for this final conclusion:\n\n    **Dependency Graph (from page 27 for View Serializability)**:\n    ```mermaid\n    graph LR\n        T1 --> T2;\n        T2 --> T3;\n    ```\n    *(This seems to be a new graph and does not align with the dependencies derived from the text. I will use the one shown in the handwritten notes to match the solution provided)*.\n    The handwritten notes on page 27 show a dependency graph for View Serializability:\n    ```mermaid\n    graph LR\n        T1 --> T2;\n        T2 --> T3;\n    ```\n    **Conclusion**: No cycle exists in the graph, therefore it **is view serializable**.\n\n## Concurrency Control\n\n**Concurrency Control** in DBMS is necessary when more than one transaction runs simultaneously, increasing the chances of conflict which can leave the DB in an inconsistent state. To handle these conflicts, we need concurrency control in DBMS, which allows transactions to run simultaneously but handles them in such a way that the integrity of data remains intact.\n\n**Example**: You and your brother have a joint bank account from which both can withdraw money. Now, let's say you both go to different branches of the same bank at the same time and try to withdraw Rs. 5000. Your joint account has only Rs. 6000 balance. If we don't have concurrency control in place, you both can get Rs. 5000, but once both transactions are finished, the account balance will be -Rs. 4000, which is not possible and leaves the DB in an inconsistent state.\n\nTherefore, we need something that controls the transactions in such a way that it allows transactions to run concurrently but maintains data consistency.\n\nThe solution to such conflicts is **locks**.\n\n### Problems of Concurrency Control\n\n1.  **Lost Update Problem**:\n    *   If any transaction `Tj` updates any variable `V` at time `t` without knowing the value of `V` at time `t`, then this may lead to a lost update problem.\n    *   **Example**:\n\n    | Transaction Ti | Time | Transaction Tj |\n    | :------------- | :--- | :------------- |\n    | `read(V)`      | t1   |                |\n    |                | t2   | `read(V)`      |\n    | `update(V)`    | t3   |                |\n    |                | t4   | `update(V)`    |\n\n    ```mermaid\n    graph TD\n        subgraph Transaction Ti\n            Ti_readV[read(V)]\n            Ti_updateV[update(V)]\n        end\n        subgraph Transaction Tj\n            Tj_readV[read(V)]\n            Tj_updateV[update(V)]\n        end\n        t1[t1] --> Ti_readV;\n        t2[t2] --> Tj_readV;\n        t3[t3] --> Ti_updateV;\n        t4[t4] --> Tj_updateV;\n        Ti_readV -- reads V --> t2;\n        Tj_readV -- reads V --> t3;\n        Ti_updateV -- updates V --> t4;\n        Tj_updateV -- updates V --> end;\n    ```\n    At time `t1` and `t2`, transactions `Ti` and `Tj` read variable `V` respectively and get some value of `V`. At time `t3`, `Ti` updates `V`. But at `t4`, `Tj` also updates `V` (using the old value of `V` it read at `t2`) without looking at the new value of `V` (updated by `Ti`). So, the updation made by `Ti` at `t3` is lost at `t4` because `Tj` overwrites it.\n\n2.  **Dirty Read**:\n    *   The dirty read occurs when one transaction updates an item of the DB and then the transaction fails for some reason. The updated data item is accessed by another transaction before it is changed back to its original value.\n    *   **Example**:\n\n    | Transaction Tj | Time | Transaction Ti |\n    | :------------- | :--- | :------------- |\n    |                | t1   |                |\n    | `update(A)`    | t2   |                |\n    |                | t3   | `read(A)`      |\n    |                | t4   |                |\n    | `rollback`     | t5   |                |\n\n    ```mermaid\n    graph TD\n        subgraph Transaction Tj\n            Tj_updateA[update(A)]\n            Tj_rollback[rollback]\n        end\n        subgraph Transaction Ti\n            Ti_readA[read(A)]\n        end\n        t1[t1] --> Tj_updateA;\n        Tj_updateA -- updates A --> t2;\n        t2 --> Ti_readA;\n        Ti_readA -- reads A --> t3;\n        t3 --> t4;\n        t4 --> Tj_rollback;\n        Tj_rollback -- changes A back --> t5;\n    ```\n    At time `t2`, transaction `Tj` writes A's value. At time `t3`, transaction `Ti` reads A's value. At time `t5`, transaction `Tj` rolls back, so it changes A's value back to its prior state. But `Ti` now contains a value for A which has never become part of the stable DB. Such a problem is known as a dirty-read problem, as one transaction reads a \"dirty\" value which has not been committed.\n\n3.  **Inconsistent Retrieval Problem**:\n    *   This occurs when a transaction reads inconsistent data due to other concurrent transactions modifying data that the first transaction is reading.\n    *   **Example**:\n\n    | Ti          | Time | Tj          |\n    | :---------- | :--- | :---------- |\n    | `Read(A)=10`| t1   |             |\n    | `Read(B)=20`| t2   |             |\n    |             | t3   | `Write(A)=50`|\n    |             | t4   | `Commit`    |\n    | `add A+B`   | t5   |             |\n\n    ```mermaid\n    graph TD\n        subgraph Ti\n            Ti_readA[Read(A)=10]\n            Ti_readB[Read(B)=20]\n            Ti_addAB[add A+B]\n        end\n        subgraph Tj\n            Tj_writeA[Write(A)=50]\n            Tj_commit[Commit]\n        end\n        t1[t1] --> Ti_readA;\n        t2[t2] --> Ti_readB;\n        t3[t3] --> Tj_writeA;\n        t4[t4] --> Tj_commit;\n        t5[t5] --> Ti_addAB;\n        Ti_readA -- reads A=10 --> t2;\n        Ti_readB -- reads B=20 --> t3;\n        Tj_writeA -- updates A to 50 --> t4;\n        Tj_commit -- commits --> t5;\n        Ti_addAB -- calculates A+B (10+20=30, not 50+20=70) --> end;\n    ```\n    Transaction `Ti` reads `A=10` and `B=20` at `t1` and `t2` respectively. But at `t3`, `Tj` updates `A` from 10 to 50 and commits at `t4`. This makes the change permanent. So, `Ti`'s addition of A+B will result in 10+20=30, not 50+20=70. This gives a wrong result and leads to inconsistency in the DB because of inconsistent analysis by `Ti`.\n\n---\n\n## Locks\n\nTo maintain consistency and restrict modification of data items by any other transaction currently accessed by some transaction, these restrictions can be applied by applying locks on data items. To access any data item, transactions have to obtain a lock on it.\n\nA lock is a kind of mechanism that ensures the integrity of data. There are two types of locks:\n\n1.  **Shared Lock (S)**:\n    *   A shared lock is placed when we are reading a data item. Multiple shared locks can be placed on a data item. However, when a shared lock is placed, no exclusive lock can be placed.\n    *   **Example**: When two transactions are reading Steve's account balance, they can both read by placing a shared lock. But at the same time, if another transaction wants to update Steve's account balance, placing an exclusive lock will not be allowed until all reading is finished.\n\n2.  **Exclusive Lock (X)**:\n    *   An exclusive lock is placed when we want to read and write the data. This lock allows both read and write operations. Once this lock is placed on a data item, no other lock (shared or exclusive) can be placed on the data until the exclusive lock is released.\n    *   **Example**: When a transaction wants to update Steve's account balance, it does so by placing an 'X' lock on it. If a second transaction wants to read the data (S-lock), it won't be allowed. If another transaction wants to write the data (X-lock), it won't be allowed either.\n\n### Lock Compatibility Matrix\n\nThe following matrix shows the compatibility between different lock types.\n\n|       | S (request) | X (request) |\n| :---- | :---------- | :---------- |\n| S (held) | True        | False       |\n| X (held) | False       | False       |\n\n**How to read the matrix**:\n*   **First row (S held)**: If a shared lock (S) is currently held on a data item, another shared lock can be acquired (`True`), but an exclusive lock cannot be acquired (`False`).\n*   **Second row (X held)**: If an exclusive lock (X) is currently held on a data item, neither another shared lock nor an exclusive lock can be acquired (`False` for both).\n\n### Banking Example with Locks\n\nConsider a banking system where:\n*   `T1` transfers Rs. 500 from account A to account B.\n*   `T2` calculates the sum of accounts A and B.\n*   Initially, account A has Rs. 1000 and B has Rs. 300.\n\n**Schedule without enforcing 2PL (allows early unlocks)**:\n\n| T1              | T2              | Operations                   | A    | B    | A+B  |\n| :-------------- | :-------------- | :--------------------------- | :--- | :--- | :--- |\n| `X-lock(A)`     |                 | Acquire X-lock on A          |      |      |      |\n| `read(A)`       |                 | A=1000                       | 1000 | 300  |      |\n| `A:=A-500`      |                 | A=500                        | 500  | 300  |      |\n| `write(A)`      |                 | Update A                     | 500  | 300  |      |\n| `unlock(A)`     |                 | Release X-lock on A          |      |      |      |\n|                 | `S-lock(B)`     | Acquire S-lock on B          | 500  | 300  |      |\n|                 | `read(B)`       | B=300                        | 500  | 300  |      |\n|                 | `unlock(B)`     | Release S-lock on B          |      |      |      |\n|                 | `S-lock(A)`     | Acquire S-lock on A          | 500  | 300  |      |\n|                 | `read(A)`       | A=500                        | 500  | 300  |      |\n|                 | `unlock(A)`     | Release S-lock on A          |      |      |      |\n|                 | `display(A+B)`  | Display 500+300 = 800        |      |      | 800  |\n| `X-lock(B)`     |                 | Acquire X-lock on B          | 500  | 300  |      |\n| `read(B)`       |                 | B=300                        | 500  | 300  |      |\n| `B:=B+500`      |                 | B=800                        | 500  | 800  |      |\n| `write(B)`      |                 | Update B                     | 500  | 800  |      |\n| `unlock(B)`     |                 | Release X-lock on B          |      |      |      |\n| **Final State** | **A=500, B=800**| **A+B=1300**                 |      |      |      |\n\nIn the schedule shown above, `T2` gives a wrong result (Rs. 800) instead of Rs. 1300. This happens because `unlock(A)` for `T1` is performed immediately after its `write(A)` operation. `T2` reads `A` before `T1`'s `write(B)` operation, leading to an inconsistent sum.\n\n**Improvement**: Keep all locks until the end of the transaction. This is the principle of **Two-Phase Locking (2PL)**.\n\n### Modified Schedule (with 2PL concept)\n\n| T1              | T2              | Operations                   | A    | B    | A+B  |\n| :-------------- | :-------------- | :--------------------------- | :--- | :--- | :--- |\n| `X-lock(A)`     |                 | Acquire X-lock on A          |      |      |      |\n| `read(A)`       |                 | A=1000                       | 1000 | 300  |      |\n| `A:=A-500`      |                 | A=500                        | 500  | 300  |      |\n| `write(A)`      |                 | Update A                     | 500  | 300  |      |\n| `X-lock(B)`     |                 | Acquire X-lock on B          | 500  | 300  |      |\n| `read(B)`       |                 | B=300                        | 500  | 300  |      |\n| `B:=B+500`      |                 | B=800                        | 500  | 800  |      |\n| `write(B)`      |                 | Update B                     | 500  | 800  |      |\n| `unlock(A)`     |                 | Release X-lock on A          |      |      |      |\n| `unlock(B)`     |                 | Release X-lock on B          |      |      |      |\n|                 | `S-lock(B)`     | Acquire S-lock on B          | 500  | 800  |      |\n|                 | `read(B)`       | B=800                        | 500  | 800  |      |\n|                 | `unlock(B)`     | Release S-lock on B          |      |      |      |\n|                 | `S-lock(A)`     | Acquire S-lock on A          | 500  | 800  |      |\n|                 | `read(A)`       | A=500                        | 500  | 800  |      |\n|                 | `unlock(A)`     | Release S-lock on A          |      |      |      |\n|                 | `display(A+B)`  | Display 500+800 = 1300       |      |      | 1300 |\n| **Final State** | **A=500, B=800**| **A+B=1300**                 |      |      |      |\n\n**Advantages of keeping locks until the end (2PL)**:\n1.  It provides serializability.\n2.  It solves all concurrency problems (lost updates, dirty reads, inconsistent retrievals).\n\n**Disadvantage**:\n1.  It leads to the **deadlock problem**.\n    *   **Example of Deadlock**:\n        ```\n        T1: X-lock(A), read(A), A:=A-500, write(A)\n        T2:                  S-lock(B), read(B)\n        T1: X-lock(B)       (not possible, T2 holds S-lock(B), T1 waits for B)\n        T2:                  S-lock(A) (not possible, T1 holds X-lock(A), T2 waits for A)\n        ```\n        In this scenario, `T1` needs `B`, but `T2` holds `B` (shared lock). `T2` needs `A`, but `T1` holds `A` (exclusive lock). Both transactions are waiting for each other, resulting in a **deadlock**.\n\n---\n\n## Two-Phase Locking Protocol (2PL)\n\nThe Two-Phase Locking Protocol is a concurrency control mechanism that guarantees conflict serializability. It consists of two phases:\n\n1.  **Growing Phase**: In this phase, a transaction can acquire new locks on data items, but it cannot release any locks. The number of locks held by the transaction only increases (or stays the same).\n2.  **Shrinking Phase**: In this phase, a transaction can release existing locks, but it cannot acquire any new locks. The number of locks held by the transaction only decreases (or stays the same).\n\n### Lock Point\n\nThe point in a transaction where it has acquired its final lock and is about to start releasing locks is called the **lock point**.\n\n```mermaid\ngraph TD\n    GrowingPhase --> LockPoint;\n    LockPoint --> ShrinkingPhase;\n\n    subgraph \"Growing Phase\"\n        GP_acquire_locks(Acquire locks);\n    end\n\n    subgraph \"Shrinking Phase\"\n        SP_release_locks(Release locks);\n    end\n\n    GP_acquire_locks -- increase or maintain locks --> LockPoint;\n    LockPoint -- decrease or maintain locks --> SP_release_locks;\n\n    style LockPoint fill:#f9f,stroke:#333,stroke-width:2px\n    style GrowingPhase fill:#ace,stroke:#333,stroke-width:2px\n    style ShrinkingPhase fill:#acf,stroke:#333,stroke-width:2px\n```\n\n### Variations of Two-Phase Locking Protocol\n\nThere are several variations of the 2PL protocol:\n\n1.  **Strict 2PL Protocol**:\n    *   In Strict 2PL, all exclusive locks are kept until the end of the transaction (or until the transaction commits). Shared locks are not bothered about.\n    *   **Advantage**: Guarantees strict serializability, preventing cascading rollbacks and dirty reads.\n    *   **Disadvantage**: Can cause cascading rollbacks for shared locks.\n    *   **Example (S1 with T3, T4)**:\n        `T3` operations: `S-lock(A), read(A), X-lock(B), read(B), X-lock(C), read(C), write(C)`\n        `T4` operations: `X-lock(B), read(B), write(B)`\n\n        **Schedule S1**:\n        | T3        | T4        |\n        | :-------- | :-------- |\n        | `S-lock(A)` |           |\n        | `read(A)` |           |\n        | `X-lock(B)` |           |\n        | `read(B)` |           |\n        |           | `X-lock(B)` |\n        | `rollback`|           |\n        |           | `rollback`|\n\n        In S1, `T3` acquires an `X-lock` on `B`. Then `T4` attempts to acquire an `X-lock` on `B`, which fails. If `T3` rolls back (e.g., due to failure), `T4` would also need to roll back if it had read `T3`'s uncommitted data (cascading rollback). Strict 2PL specifically requires exclusive locks to be held till commit.\n\n        **Schedule S2 (Strict 2PL applied)**:\n        | T3              | T4              |\n        | :-------------- | :-------------- |\n        | `S-lock(A)`     |                 |\n        | `read(A)`       |                 |\n        | `X-lock(B)`     |                 |\n        | `read(B)`       |                 |\n        | `S-lock(C)`     |                 |\n        | `read(C)`       |                 |\n        | `commit`        |                 |\n        | `unlock(A)`     |                 |\n        | `unlock(B)`     |                 |\n        | `unlock(C)`     |                 |\n        |                 | `X-lock(B)`     |\n        |                 | `read(B)`       |\n        |                 | `write(B)`      |\n\n        In S1, there is a cascading rollback of T4 due to the rollback of T3. But in S2, all exclusive locks are kept till the end, avoiding cascading rollbacks; however, deadlock is still present.\n\n2.  **Rigorous 2PL Protocol**:\n    *   In Rigorous 2PL, all locks (both shared and exclusive) are kept until the transaction commits.\n    *   **Advantage**: It means both shared and exclusive locks cannot be released till the end of the transaction. It does not cause cascading rollback, but deadlock is still present.\n\n3.  **Conservative 2PL Protocol (Static 2PL)**:\n    *   Conservative 2PL protocol, also known as Static 2PL, requires the transaction to lock all the items it will access *before* the transaction begins execution. This is done by predeclaring its read set and write set. The operations cannot start until all required items are locked.\n    *   We can release the locks whenever we want, but we need to lock all data items before carrying out any operation.\n    *   **Advantages**: Prevents deadlocks because all resources are acquired at once.\n    *   **Disadvantages**: It makes it deadlock-free but cascading rollback is not ensured. It is very difficult to use this protocol in practice because of the need to pre-declare the read-set and write-set, which is not possible in many cases.\n    *   **Example**:\n\n    | T1        | T2        |\n    | :-------- | :-------- |\n    | `X-lock(A)` |           |\n    | `X-lock(B)` |           |\n    | `read(A)` |           |\n    | `A:=A+50` |           |\n    | `write(A)`|           |\n    | `unlock(A)` |           |\n    | `read(B)` |           |\n    | `B:=B+100`|           |\n    | `write(B)`|           |\n    | `unlock(B)` |           |\n    |           | `X-lock(A)` |\n    |           | `read(A)` |\n    |           | `A:=A+50` |\n    |           | `write(A)`|\n    |           | `unlock(A)` |\n\n---\n\n## Time-stamp Ordering Protocol\n\nThe Time-stamp Ordering Protocol ensures serializability by assigning a unique timestamp to each transaction. The protocol ensures that transactions are executed in the order of their timestamps.\n\nEach data item `Q` has two timestamp values:\n*   `W-timestamp(Q)`: The largest timestamp of any transaction that successfully wrote `Q`.\n*   `R-timestamp(Q)`: The largest timestamp of any transaction that successfully read `Q`.\n\n`TS(Ti)` refers to the timestamp of transaction `Ti`.\n\n1.  **If Ti issues read(Q)**:\n    *   i) If `TS(Ti) < W-timestamp(Q)`: The read operation is rejected, and `Ti` is rolled back. (This means `Ti` is trying to read a value that was already overwritten by a younger transaction).\n    *   ii) If `TS(Ti) >= W-timestamp(Q)`: The read operation is accepted, and `R-timestamp(Q)` is updated to `max(R-timestamp(Q), TS(Ti))`.\n\n2.  **If Ti issues write(Q)**:\n    *   i) If `TS(Ti) < R-timestamp(Q)` OR `TS(Ti) < W-timestamp(Q)`: The write request is rejected, and `Ti` is rolled back. (This means `Ti` is trying to write a value that was already read or overwritten by a younger transaction, or that a younger transaction has read a stale value from `Ti`).\n    *   ii) Otherwise: The write operation is executed, and `W-timestamp(Q)` is updated to `TS(Ti)`.\n\n**Example 1**:\n`TS(T1) = 10`, `TS(T2) = 10.05`\nInitially: `RTS(A)=0, WTS(A)=0`, `RTS(B)=0, WTS(B)=0`\n\n**Schedule**:\n| T1            | T2            | Notes                                   |\n| :------------ | :------------ | :-------------------------------------- |\n| `read(B)`     |               | `TS(T1)=10 >= WTS(B)=0` -> accepted. `RTS(B)=10`. |\n|               | `read(B)`     | `TS(T2)=10.05 >= WTS(B)=0` -> accepted. `RTS(B)=10.05`. |\n|               | `B:=B-20`     |                                         |\n|               | `write(B)`    | `TS(T2)=10.05 >= RTS(B)=10.05` AND `TS(T2)=10.05 >= WTS(B)=0` -> accepted. `WTS(B)=10.05`. |\n| `read(A)`     |               | `TS(T1)=10 >= WTS(A)=0` -> accepted. `RTS(A)=10`. |\n|               | `read(A)`     | `TS(T2)=10.05 >= WTS(A)=0` -> accepted. `RTS(A)=10.05`. |\n| `print(A+B)`  |               |                                         |\n|               | `A:=A+30`     |                                         |\n|               | `print(A+R)`  |                                         |\n|               | `write(A)`    | `TS(T2)=10.05 >= RTS(A)=10.05` AND `TS(T2)=10.05 >= WTS(A)=0` -> accepted. `WTS(A)=10.05`. |\n\n**Note**: From the above schedule, we find that both transactions performed concurrently under the timestamp schedule without giving inconsistent results. Also, note that this schedule ensures freedom from deadlock since no transaction ever waits.\n\n**Example 2**:\n`TS(T1) = 10`, `TS(T2) = 10.05`\nInitially: `RTS(A)=0, WTS(A)=0`, `RTS(B)=0, WTS(B)=0`\n\n**Schedule**:\n| T1            | T2            | Notes                                   |\n| :------------ | :------------ | :-------------------------------------- |\n| `read(B)`     |               | `TS(T1)=10 >= WTS(B)=0` -> accepted. `RTS(B)=10`. |\n|               | `read(A)`     | `TS(T2)=10.05 >= WTS(A)=0` -> accepted. `RTS(A)=10.05`. |\n| `B:=B-20`     |               |                                         |\n| `write(B)`    |               | `TS(T1)=10 >= RTS(B)=10` AND `TS(T1)=10 >= WTS(B)=0` -> accepted. `WTS(B)=10`. |\n|               | `A:=A+80`     |                                         |\n|               | `write(A)`    | `TS(T2)=10.05 >= RTS(A)=10.05` AND `TS(T2)=10.05 >= WTS(A)=0` -> accepted. `WTS(A)=10.05`. |\n| `read(A)`     |               | `TS(T1)=10 < WTS(A)=10.05` -> rejected. `T1` is rolled back and must restart with a new timestamp. |\n\n---\n\n## Graph-Based or Tree-Based Protocol\n\nThis protocol uses a hierarchical structure (a tree) to manage locks.\n\n**Rules**:\n\n1.  Any transaction `T` can lock any data item at first time.\n2.  After the first lock, if `T` wants to lock any other data item `A`, then `T` has to lock its parent first.\n3.  A transaction can unlock data items at any time.\n4.  Suppose `T` has obtained a lock on data item `A` and then released it after some time; then `T` cannot obtain any lock on `A` again.\n\n**Example Graph (Tree Structure)**:\n```mermaid\ngraph TD\n    A --> B;\n    A --> C;\n    A --> D;\n    B --> E;\n    C --> F;\n    D --> G;\n    E --> H;\n```\n\n**Example Operations**: We want to operate `G = G + 10` and `H = H - 5`.\n\n**Transaction T1 operations**:\nTo access `G`:\n*   `X-lock(G)` (Rule 1: can lock any item first)\n*   `read(G)`\n*   `G := G + 10`\n*   `unlock(G)`\n\nTo access `H`:\n*   `S-lock(A)` (Rule 2: lock parent `A` for `E`, and `A` is parent of `B`, `B` is parent of `E`)\n*   `read(A)`\n*   `S-lock(B)`\n*   `read(B)`\n*   `unlock(A)` (can unlock at any time, Rule 3)\n*   `X-lock(H)` (Rule 2: lock parent `E` for `H`, assuming `E` is already locked or `B` is locked to access `E`)\n*   `read(H)`\n*   `H := H - 5`\n*   `unlock(E)` (assuming `E` was locked)\n*   `unlock(H)`\n\n*(Note: The actual sequence in the notes skips `E` and assumes `X-lock(H)` can be taken if `B` is locked. This implies a more complex rule interpretation where all ancestors need to be locked. The example lists X-lock(G), then unlock(G), then S-lock(A), read(A), S-lock(B), read(B), unlock(A), S-lock(E), read(E), unlock(B), X-lock(H), read(H), H:=H-5, unlock(E), unlock(H). This follows the parent locking strategy).*\n\n---\n\n## Validation Based Protocol (Optimistic Concurrency Control)\n\nThis protocol operates using three phases:\n\n1.  **Read Phase**: Transaction `Ti` reads all the data items it needs and stores them in temporary variables. All write operations are made on these temporary variables instead of the actual DB.\n2.  **Validation Phase**: In this phase, a validation test is performed to determine whether the changes made by the transaction can actually be applied to the actual DB without violating serializability.\n3.  **Write-Phase**: If `Ti` cleared the validation test, then the actual DB is changed according to the temporary variables. The validation test ensures violation-free execution.\n\n### Timestamps in Validation Based Protocol\n\nThree different timestamps are associated with this protocol:\n\n*   **1) Start(Ti)**: Time when `Ti` finished its read phase and starts its validation phase.\n*   **2) Validation(Ti)**: Time when `Ti` finished its validation phase.\n*   **3) Finish(Ti)**: Time when `Ti` finished its write phase.\n\n### Validation Test for Transaction (Ti)\n\nThe validation test requires that for all other transactions `Tk` with `TS(TK) < Start(Ti)` (meaning `Tk` started before `Ti` entered its validation phase), one of the following two conditions must hold:\n\n1.  `Finish(TK) < Start(Ti)`: Since `Tk` completes its execution before `Ti` started its read phase, `Tk`'s write-set does not interact with `Ti`'s read-set.\n2.  `Start(Ti) < Finish(TK) < Validation(Ti)`: This condition ensures that the writes (`Tk`) and (`Ti`) do not overlap. Since `Tk`'s writes do not affect `Ti`'s reads and `Ti` cannot affect `Tk`'s reads, serializability order is indeed maintained.\n\n**Example**: Consider transactions `T25` and `T26`. Suppose `TS(T25) < TS(T26)`.\n\n**Schedule**:\n| T25           | T26           |\n| :------------ | :------------ |\n| `read(B)`     |               |\n|               | `read(B)`     |\n|               | `B:=B-50`     |\n|               | `read(A)`     |\n|               | `A:=A+50`     |\n| `read(A)`     |               |\n| `<validate>`  |               |\n| `display(A+B)`|               |\n|               | `<validate>`  |\n|               | `write(B)`    |\n|               | `write(A)`    |\n\nIf `TS(T25) < TS(T26)`, then the validation phase succeeds. The writes to the actual variables are performed only after the validation phase of T26. Thus, T25 reads the old values of B and A, and this schedule is serializable.\n\n---\n\n## Recovery in DBMS\n\n**Failure**: Failure refers to the state when the system can no longer continue its normal execution, resulting in loss of information.\n\n### Different Types of Failures\n\n1.  **System Crash**: This failure occurs due to bugs in the software or hardware.\n2.  **Transaction Failures**: This failure happens due to logical errors, such as missing data, less free space, or deadlocks.\n3.  **Disk Failure**: This happens due to a read/write head crash or other failure during data transfer.\n\n**Recovery**: Recovery refers to the method by which the system restores the DB to its most consistent state just before the time of failure.\n\n### Recovery Techniques\n\nThere are three main recovery techniques:\n\n1.  **Log-Based Recovery**\n2.  **Shadow Paging**\n3.  **Checkpointed Recovery**\n\n---\n\n### 1) Log-Based Recovery\n\nIn a log-based recovery system, a **log** is maintained in which all the modifications to the DB are kept. A log consists of **log records**.\nAn update log record describes a single DB write and contains the following fields:\n\n*   **Transaction Identifier**: A unique number given to each transaction that performs a write operation.\n*   **Data-Item Identifier**: A unique number given to a data item written.\n*   **Old-value**: Gives the value of a data item before the write.\n*   **New-value**: Gives the value of a data item after the write.\n\nIn log-based recovery, two operations are used for recovery:\n\n*   **a) Redo**: It means all the work of the transactions that completed successfully before a crash is to be performed again. (Data items will take new values).\n*   **b) Undo**: It means all the work done by the transactions that did not complete due to a crash is to be undone. (Data items will take old values).\n\n#### Two Types of Log-Based Recovery\n\n1.  **Deferred Database Modification**:\n    *   In this technique, all the write operations of any transaction `Ti` are deferred until `Ti` partially commits. This means modifications to the DB happen only after `Ti` partially commits.\n    *   In this technique, the `old-value` field is not needed in the log record.\n\n    **Example**: `T1` transfers Rs. 200 from account A to B and deposits Rs. 200 in C.\n    Initially: `A=500, B=100, C=600`.\n\n    **Transaction Operations**:\n    | T1            | T2            |\n    | :------------ | :------------ |\n    | `read(A)`     |               |\n    | `A:=A-200`    |               |\n    | `write(A)`    |               |\n    | `read(B)`     |               |\n    | `B:=B+200`    |               |\n    | `write(B)`    |               |\n    |               | `read(C)`     |\n    |               | `C:=C+200`    |\n    |               | `write(C)`    |\n\n    **Corresponding Log Record (conceptual)**:\n    ```\n    <T1,start>\n    <T1,A,300>  (New value of A after T1 writes it)\n    <T1,B,200>  (New value of B after T1 writes it)\n    <T1,commit>\n    <T2,start>\n    <T2,C,800>  (New value of C after T2 writes it)\n    <T2,commit>\n    ```\n\n    ```mermaid\n    graph TD\n        LogFile --contains--> LogRecords;\n        LogRecords --describes--> DataItem;\n        DataItem --stored in--> DB;\n    ```\n\n    **System Crash Scenario**:\n    ```mermaid\n    graph TD\n        SystemCrash[System Crash Occurs] --> CommitFound{Commit Found in Log?};\n        CommitFound -- Yes --> Redo[Redo];\n        CommitFound -- No --> DeleteRejected[Delete / Rejected];\n    ```\n\n    *   **a) If system crash occurs after write(A) of T1**:\n        Log contains: `<T1,start>`, `<T1,A,300>`.\n        No commit record appears for `T1`, so `T1` is rejected. No redo operation is needed for `T1` because the deferred modification means `A` was not actually written to disk yet.\n\n    *   **b) If system crash occurs after write(C) of T2**:\n        Log contains: `<T1,start>`, `<T1,A,300>`, `<T1,B,200>`, `<T1,commit>`, `<T2,start>`, `<T2,C,800>`.\n        In this case, you have to redo `T1` because both `<T1,start>` and `<T1,commit>` appear in the log. `T2` did not commit, so it is rejected.\n        *After redo, A=300, B=200.*\n\n2.  **Immediate Database Modification**:\n    *   In this technique, database modifications are made immediately. This means `write` operations are applied to the DB before the transaction commits.\n    *   In this technique, both `old-value` and `new-value` are needed in the log record.\n\n    **Corresponding Log Record (conceptual)**:\n    ```\n    <T1,start>\n    <T1,A,500,300>  (Old A=500, New A=300)\n    <T1,B,100,200>  (Old B=100, New B=200)\n    <T1,commit>\n    <T2,start>\n    <T2,C,600,800>  (Old C=600, New C=800)\n    <T2,commit>\n    ```\n\n    ```mermaid\n    graph TD\n        LogFile --contains--> LogRecords;\n        LogRecords --describes--> DataItem;\n        DataItem --stored in--> DB;\n    ```\n\n    **System Crash Scenario**:\n    ```mermaid\n    graph TD\n        SystemCrash[System Crash Occurs] --> CommitFound{Commit Found in Log?};\n        CommitFound -- Yes --> Redo[Redo];\n        CommitFound -- No --> Undo[Undo];\n    ```\n\n    *   **a) If system crash occurs after write(A) of T1**:\n        Log contains: `<T1,start>`, `<T1,A,500,300>`.\n        Here, only `start` exists, no `commit`. So, undo transaction `T1`. As a result, account `A` restores its old value (500).\n\n    *   **b) If system crash occurs after write(C) of T2**:\n        Log contains: `<T1,start>`, `<T1,A,500,300>`, `<T1,B,100,200>`, `<T1,commit>`, `<T2,start>`, `<T2,C,600,800>`.\n        Undo `T2` (as it didn't commit). As a result, `C` will have its old value (600). Redo `T1` (as it committed). `A` and `B` will keep their new values (300 and 200 respectively).\n\n**Disadvantages of Log-Based Recovery**:\n1.  **Time-consuming**: Successfully completed transactions have to be redone.\n2.  **Searching is difficult**: The whole log has to be scanned.\n\n---\n\n### Checkpoint\n\nA **checkpoint** is defined as a point of synchronization between the DB and the transaction log file. The general method of DB recovery using information logs involves searching the entire log, which is very time-consuming. Therefore, we use checkpoints.\n\nA checkpoint is like a snapshot of the DBMS state. By taking snapshots periodically, the DBMS can reduce the amount of work to be done during restart in the event of a system crash. The approach is to find a point sufficiently far back to ensure that any item written before that point has been correctly and safely saved. This method is called **checkpointing**.\n\nA `<checkpoint>` record in the log gives a list of all transactions that were in progress at the time the checkpoint was taken. Thus, checkpoints help the system to provide information at the restart time, identifying which transactions to undo and which to redo.\n\n**Example**: Say transaction `T` commits prior to a checkpoint. This implies that the `<T,commit>` record must appear in the log before the `<checkpoint>` record. Any DB modifications made by `T` must have been written to disk either prior to the checkpoint or as part of the checkpoint itself. Thus, at recovery time, there is no need to perform redo operations on `T`.\n\n**Checkpoint Example Scenario**:\n\n| Trans. | Checkpoint | Point of Failure |\n| :----- | :--------- | :--------------- |\n| T1     | (before)   | (before)         |\n| T2     | (before)   | (after)          |\n| T3     | (after)    | (before)         |\n| T4     | (after)    | (after)          |\n| T5     | (after)    | (after)          |\n\n**Analysis**:\n*   We observe that `T1` completed successfully prior to the time of checkpoint `tc`.\n*   `T2` started prior to `tc` but finished before `tf` (failure point).\n*   `T3` also started prior to `tc` but could not complete by `tf`.\n*   `T4` started after `tc` and completed successfully before `tf`.\n*   `T5` started after `tc` but did not complete by `tf`.\n\n**Recovery Actions**:\n*   **Redo**: `T2`, `T4` (committed after `tc`)\n*   **Undo**: `T3`, `T5` (active at `tf`)\n\n**Note**: A checkpoint can be taken at a fixed interval of time, say every 15 minutes or 1 hour, and so on.\n\n**Rules for Recovery with Checkpoints**:\n*   Any transaction committed before the checkpoint is successfully updated in DB.\n    *   `<T,start>` before `<checkpoint>`, `<T,commit>` before `<checkpoint>` => **No Redo/Undo needed** (already on disk).\n*   Any transaction committed after the checkpoint:\n    *   `<T,start>` before `<checkpoint>`, `<T,commit>` after `<checkpoint>` => **Redo**\n    *   `<T,start>` after `<checkpoint>`, `<T,commit>` after `<checkpoint>` => **Redo**\n*   Any transaction not committed:\n    *   `<T,start>` before/after `<checkpoint>`, no `<T,commit>` => **Undo**\n\n---\n\n## Shadow Paging\n\nShadow paging is an alternative recovery technique to overcome the disadvantages of log-based recovery (e.g., performance overhead of maintaining a log, and complex log processing during recovery). The main idea of shadow paging is to keep two page tables in the DB: one is used for current operations, and the other is used for recovery.\n\n1.  **Shadow Page Table (SPT)**: This table cannot be changed during any transaction. It points to the most recently committed state of the database.\n2.  **Current Page Table (CPT)**: This page table may be changed during a transaction. It points to the pages currently being used and potentially modified by active transactions.\n\n```mermaid\ngraph TD\n    UserRequest --> Read_X[Read data item X from DB];\n    Read_X --> Disk[Disk];\n    Disk -- where DB is stored --> CurrentPageTable[Current Page Table (CPT)];\n    CurrentPageTable -- points to --> MainMemory[Main Memory];\n    MainMemory -- current state --> Write_Operation[Perform operation on X];\n    Write_Operation -- if successful --> NewPageTable[New CPT];\n    NewPageTable -- is copied to --> ShadowPageTable[Shadow Page Table (SPT)];\n    SPT -- now contains --> CommittedState[New committed state];\n    Write_Operation -- if not successful --> RevertToSPT[Revert to old SPT];\n\n    style CurrentPageTable fill:#ace,stroke:#333,stroke-width:2px\n    style ShadowPageTable fill:#acf,stroke:#333,stroke-width:2px\n```\n\n**How it works**:\n*   If we want to perform an operation on data item `X` stored in the DB (on disk), it gets transferred from disk to main memory (CPT).\n*   CPT copies it to the Shadow Page Table (SPT).\n*   Operations are performed in CPT.\n*   If the operation is successful, then the new CPT is copied back to SPT, and SPT transfers it to the place where the DB is stored.\n*   If the operation is not successful (e.g., transaction aborts), then we go back to the shadow page table for the old values.\n\n**Note**: When we are performing operations on a data item `X` initially, both tables (CPT, SPT) are identical. Changes are only done on CPT, and SPT is used for recovery purposes.\n\n---\n\n## Deadlock\n\nA system is in a **deadlock state** if there exists a set of transactions such that for every transaction in the set, it is waiting for another transaction in the set.\n\n**Example**:\n\n| T1        | T2        |\n| :-------- | :-------- |\n| `X-lock(A)` |           |\n|           | `X-lock(B)` |\n| `X-lock(B)` |           |\n|           | `X-lock(A)` |\n\nIn the above schedule, `T1` waits for `T2` to release its lock on data-item `B`, and `T2` waits for `T1` to release its lock on data-item `A`. Such a cycle where transactions are waiting for locks to be released is called a **deadlock**.\n\n### Methods for Dealing with Deadlock Problem\n\nThere are two main methods:\n\n1.  **Deadlock Prevention**\n2.  **Deadlock Detection**\n\n#### 1) Deadlock Prevention\n\nWe can prevent deadlocks by giving each transaction a priority and ensuring that lower-priority transactions are not allowed to wait for higher-priority transactions, or vice versa.\nOne way of assigning priorities is to give each transaction a timestamp when it starts.\n\n**Techniques used for Deadlock Prevention**:\n*   i) Wait-die\n*   ii) Wound-wait\n\n**i) Wait-die**:\n*   If `Ti` requests a data item currently held by `Tj`:\n    *   If `TS(Ti) < TS(Tj)` (Ti is older than Tj): `Ti` is allowed to wait for `Tj`.\n    *   Else (`TS(Ti) > TS(Tj)` - Ti is younger than Tj): `Ti` is rolled back (dies).\n\n*   **Example**: Say `T1` requests for a data item held by `T2`, and `T3` also requests for a data item held by `T2`.\n    Assume `TS(T1)=3, TS(T2)=6, TS(T3)=9`.\n\n    ```mermaid\n    graph LR\n        T1_req_T2[T1 requests T2] --> T1_wait_T2(T1 waits for T2);\n        T3_req_T2[T3 requests T2] --> T3_dies(T3 dies);\n    ```\n    According to Wait-die:\n    *   `T1` (`TS=3`) requests `T2` (`TS=6`): `TS(T1) < TS(T2)`, so `T1` waits for `T2`.\n    *   `T3` (`TS=9`) requests `T2` (`TS=6`): `TS(T3) > TS(T2)`, so `T3` rolls back (dies).\n\n    This scheme is **non-preemptive** (transactions do not force others to give up resources).\n\n**ii) Wound-wait**:\n*   If `Ti` requests a data item currently held by `Tj`:\n    *   If `TS(Ti) < TS(Tj)` (Ti is older than Tj): `Tj` is forced to roll back (wounded) and releases the item, and `Ti` gets the item.\n    *   Else (`TS(Ti) > TS(Tj)` - Ti is younger than Tj): `Ti` waits for `Tj`.\n\n*   **Example**: `T1` requests a data item held by `T2`. Assume `TS(T1)=3, TS(T2)=6`.\n    *   `T1` (`TS=3`) requests `T2` (`TS=6`): `TS(T1) < TS(T2)`, so `T2` rolls back (wounded), and `T1` gets the item.\n\n    This method is **preemptive**.\n\n**Note**:\n*   In the Wait-die scheme, lower-priority transactions can never wait for higher-priority transactions.\n*   In the Wound-wait scheme, higher-priority transactions never wait for lower-priority transactions.\n*   In either case, no deadlock cycle can exist.\n\n#### 2) Deadlock Detection\n\nTo detect a deadlock, a **Wait-For Graph (WFG)** is created.\n\nA **Wait-For Graph** `G=(V,E)` consists of:\n*   `V`: A set of vertices representing transactions.\n*   `E`: A set of directed edges showing dependency. An edge `Ti -> Tj` means transaction `Ti` is waiting for a data item held by transaction `Tj`.\n\nIf transaction `Ti` requests a data item which is currently being held by `Tj`, then an edge `Ti -> Tj` is inserted into the graph. When `Ti` has obtained the required item from `Tj`, the edge is removed. Deadlock occurs when there is a **cycle in the WFG**.\n\n**Example 1: WFG without deadlock situation**:\nConsider 3 transactions `T1, T2, T3`.\n`T1` is waiting for `T2`. `T2` is waiting for `T3`.\n```mermaid\ngraph LR\n    T1 --> T2;\n    T2 --> T3;\n```\nHere, `T1` waits for `T2` to release a data item, and `T2` waits for `T3`. There is no cycle, so no deadlock.\n\n**Example 2: WFG with deadlock situation**:\n`T1` is waiting for `T2`. `T2` is waiting for `T3`. `T3` is waiting for `T1`.\n```mermaid\ngraph LR\n    T1 --> T2;\n    T2 --> T3;\n    T3 --> T1;\n```\nHere, a cycle exists (`T1 -> T2 -> T3 -> T1`), which results in a system deadlock state where `T1, T2, T3` are all blocked.\n\n---\n\n## ARIES (Algorithm for Recovery and Isolation Exploiting Semantics)\n\nARIES is a robust recovery algorithm that provides fine-grained logging and recovery. It works in three phases:\n\n1.  **Analysis Phase**: This phase identifies dirty pages in the buffer cache and the set of active transactions at the time the system crashed. It determines the starting point for redo.\n2.  **Redo Phase**: The redo phase actually reapplies updates from the log. It is applied only for committed transactions to bring the database to the state it was in at the time of the crash (or just before).\n3.  **Undo Phase**: During the undo phase, the log is scanned backward, and operations of transactions that were waiting or active at the time of the crash are undone in reverse order. This ensures that any uncommitted changes are rolled back."
  }
};
